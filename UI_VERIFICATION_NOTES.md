# ملاحظات التحقق البصري

تم التحقق من ظهور بوابة FAMILY العامة بالعربية واتجاه RTL في المعاينة، بما في ذلك العناوين والأزرار وبطاقات مصدر الحقيقة والحماية. كما تم تحويل الانتقالات الداخلية التي كانت تستخدم إعادة تحميل كاملة إلى توجيه داخل التطبيق.

لم تُراجع الشاشات المحمية ببيانات فعلية في هذه الجلسة؛ لأن المعاينة انتقلت إلى بوابة مصادقة عند محاولة الوصول إليها، ولم يتم تنفيذ أي تسجيل دخول أو إدخال بيانات شخصية. لقطات المعاينة الآلية للهاتف التقطت حالة التحميل الهيكلية قبل اكتمال جلسة التطبيق، ولذلك لا تُعد دليلًا على محتوى شاشة مصادق عليه.

تؤكد فحوص TypeScript وVitest وبناء الإنتاج سلامة بناء المكونات والمسارات.

تم لاحقًا تفعيل اتصال المتصفح الشخصي بموافقة المستخدم. اكتمل تحميل جلسة FAMILY المحمية، وظهر غلاف RTL والسايدبار واسم مساحة العمل وعناصر الثيم والخصوصية، من دون تنفيذ أي عملية مالية أو تعديل بيانات.

تم فتح صفحة الاستثمارات داخل الجلسة نفسها. ظهرت بطاقات ملخص الحيازات، وتبويبا «الحيازات» و«الأدوات»، والإجراءات السريعة لتسجيل صفقة وتحديث الأسعار ومراجعة التوزيع، مع حالة فراغ صحيحة عند عدم وجود حيازات منشورة. لم يتم تفعيل وضع العرض التجريبي ولم تُنشأ أو تُعدل بيانات مالية.

تم فتح شاشة التداول الجديدة ضمن الجلسة المحمية. ظهرت تبويبات «تسجيل صفقة» و«الحيازات» و«سعر يدوي»، كما ظهرت حالة آمنة تطلب تجهيز حساب وأداة قبل أي عملية. لم يُدخل المستخدم أي نموذج، ولم يُنشر أو يُعتمد أي قيد.

أعاد المتصفح تحميل عناصر التنقل بصورة صحيحة بعد تغيير المسار؛ لم يُسجل خطأ تشغيلي في شاشة التداول ولم يتم تنفيذ إجراء من القائمة.

تم فتح شاشة التقييم والعملات وتحقق ظهور مسار تحديث Yahoo Finance وخيار الإدخال اليدوي وفجوات التقييم. أظهرت البيانات الحالية حالة «لا توجد فجوات تقييم حاليًا». لم يُضغط زر تحديث Yahoo ولم يُحفظ سعر يدوي.

تم فتح شاشة الموافقات والقرارات داخل الجلسة المحمية. ظهرت قائمة الطلبات ونافذة تفاصيل مستقلة وسجل قرارات، وكلها تعرض حالة فراغ دقيقة عند عدم وجود طلبات أو قرارات. لم يُعتمد أو يُرفض أو يُنفذ أي طلب.

تم التحقق من مفتاح الخصوصية ومن تبديل الوضع الليلي على شاشة الموافقات، وظهرت واجهة داكنة ذات تباين مقروء. أُعيد مفتاح الخصوصية والوضع اللوني إلى حالتهما السابقة (الفاتح وإظهار القيم) بعد الاختبار، من دون أي تعديل في السجلات المالية.

بعد تصحيح ترتيب Hooks في غلاف لوحة التحكم، أعيد تحميل شاشة الموافقات في الجلسة المصادق عليها بنجاح، وظهرت عناصرها كاملة من دون إعادة ظهور خطأ ترتيب Hooks.

فحص سجل المتصفح اللاحق أعاد السجل التاريخي الوحيد لخطأ ترتيب Hooks الذي قاد إلى الإصلاح؛ لم يظهر إدخال أحدث بالخطأ بعد إعادة تحميل شاشة الموافقات بنجاح.

تم فحص القشرة المتجاوبة بعرض هاتف 375px؛ بقي تحميل الهيكل ضمن العرض من دون تجاوز أفقي. وتستند مراجعة محتوى الشاشات المحمية إلى الجلسة المصادق عليها على سطح المكتب، إذ لا تسمح معاينة الهاتف المعزولة بإعادة استخدام جلسة المستخدم.

تم فتح `/admin/users` داخل جلسة مصادق عليها. ظهرت بطاقة المالك الرئيس ودور Super Admin، وجدول الحسابات ذات البريد الموثق، وضوابط منع خفض صلاحية المالك الرئيس، وسجل تدقيق مستقل لإدارة مستخدمي المنصة. لم يُنفذ أي تغيير على أدوار المستخدمين أثناء التحقق.

بعد توحيد سجلات هوية OAuth المكررة، أعيد فحص `/admin/users` داخل الجلسة. ظهر حساب موثق واحد للمالك الرئيس، واستقر دوره كـ Super Admin وعضويته كمالك لمساحة FAMILY، وظهر حدث تثبيت الملكية في سجل تدقيق المنصة.

بعد إعادة ربط الجلسة، تم التحقق من شاشة `/admin/users` المحدثة. ظهرت بطاقة دعوات المدير العام بحقل Gmail وحالة انتظار تسجيل Google وإجراء إلغاء، كما ظهر تدقيق إنشاء الدعوة. وُجدت دعوة أنشأها المستخدم داخل واجهته، ولم يُجر أي اعتماد أو رفض أو إلغاء من جانب المراجعة.

تم التحقق من لوحة `/data-quality` داخل جلسة مصادق عليها. ظهرت مؤشرات تغطية الأدوات والعملات والمصدر وحالة التقادم وملاحظة الحوكمة، وكانت بيانات المساحة الحالية بلا أدوات أو عملات تحتاج تحويلًا؛ لذلك عرضت اللوحة حالات صفرية واضحة من دون أي قيمة مالية أو زر تحديث أو تنفيذ تلقائي.


## تحقق الإصدار السوقي والبريدي — 28 أغسطس 2026

تم التقاط `/`, `/research/prices`, `/data-quality` و`/admin/users` بعرض سطح مكتب 1280px. ظهرت المسارات المحمية في حالة التحميل/بوابة الجلسة داخل المعاينة الآلية، لذلك لا تُعد اللقطات دليلًا على بيانات مستخدم مصادق عليها. لم تُدخل بيانات شخصية أو مالية ولم يُنفذ تحديث Yahoo أو إجراء إداري.

أضيفت في واجهة Dashboard بطاقة «مراقبة السوق» للأسعار المسجلة للحيازات وعناصر المراقبة مع روابط الأسعار وجودة البيانات. وتعرض صفحة الأسعار خيارًا صريحًا للمالك لتفعيل تنبيهات البريد السوقية؛ الحالة الافتراضية غير مفعلة. لا تُرسل رسالة عند مجرد فتح الصفحة أو عند غياب إعداد SMTP.

تحققت فحوص البناء والاختبارات من RTL والمسارات وتجميع المكون الجديد. لم يُنفذ اختبار بريد فعلي لأن `SMTP_SEND_ENABLED` متروك معطلًا، كما أن اختبار اتصال Gmail السابق لم ينجح؛ لم يترتب على ذلك أي إرسال.
