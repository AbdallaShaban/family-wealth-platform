# تقرير إكمال وتدقيق منصة FAMILY

**تاريخ التقرير:** 28 أغسطس 2026  
**نطاق التقرير:** مقارنة المهمة المرجعية `BGlK13ZmfHmwsfYdgMVS2Z` بالنسخة الدائمة الحالية، وتنفيذ إصلاحات الملكية والتهيئة والتحقق من جاهزية الإصدار.

## الخلاصة التنفيذية

النسخة الدائمة من **FAMILY** تتبع القرارات الأساسية للمهمة المرجعية، ثم توسعت فوقها بمصدر Yahoo Finance المجاني للأسهم والذهب والعملات، ومؤشرات تقادم الأسعار، وإشارات مراجعة غير تنفيذية، وواجهة عربية RTL قابلة للاستجابة، وخزنة وتصدير ونسخ احتياطي، ومهام تحديث سوق دورية محكومة. لا تسمح هذه الطبقات التحليلية بإنشاء صفقة أو إعادة تقييم أو قيد دفتر تلقائيًا.

استُكمل في هذه المراجعة عنصر الحوكمة المطلوب للحساب الأول، وأُصلحت فجوة سلامة فعلية ظهرت في قاعدة البيانات: كانت قيود التفرد المعرّفة في المخطط غير موجودة في القاعدة، ما سمح بتكرار سجلات هوية OAuth وتهيئة المساحة الشخصية. جرى توحيد السجلات المكررة التي ثبت أنها بلا بيانات مالية أو مراجع تشغيلية، مع الاحتفاظ بالسجل الأقدم، ثم استعادة القيود ومنع التكرار برمجيًا.

## المقارنة مع المحادثة المرجعية

| محور مرجعي | الحالة في الإصدار الدائم | نتيجة المراجعة |
|---|---|---|
| دفتر القيد المزدوج وعزل مساحة العمل | منشأ ومطبق في طبقات الخادم والقراءة والتدفقات | مكتمل؛ السعر والتنبيه والسيناريو لا يرحّل أي قيد. |
| نموذج هجين للتقييم ومصادر السوق | Yahoo Finance للأسهم والصناديق والذهب والعملات، مع Frankfurter بديلًا مرجعيًا | مكتمل؛ تحفظ حالة التأخر والمصدر والطابع الزمني. |
| الذهب والاستثمارات والتقييم | حيازات، أدوات، لقطات سعر، تحويل `GC=F`، ومسودة إعادة تقييم | مكتمل؛ الاعتماد الدفتري يدوي وصريح. |
| إشارات المخاطر والرسوم والضرائب | إشارات هبوط/مكسب/تقادم، واقتراحات رسوم وضرائب قابلة للتعديل | مكتمل؛ مراجعة فقط وليست نصيحة أو تنفيذًا آليًا. |
| التدفقات والميزانيات والديون والسيولة والزكاة | صفحات ومنطق حساب وتسويات وحالات فراغ | مكتمل ضمن نطاق المنتج الحالي. |
| الحوكمة والموافقات | فصل واجهة الموافقات، منع اعتماد المنشئ لنفسه، سجل تدقيق | مكتمل. |
| الخزنة والتصدير والنسخ الاحتياطي | مفاتيح وثائق مشفرة ومحدودة بالمساحة، JSON/CSV، ولقطة نسخة احتياطية | مكتمل؛ الاستعادة تمر بالمراجعة والتسوية ولا تتجاوز الدفتر. |
| RTL والخصوصية والهاتف وPWA | تنسيق عربي مركزي، إخفاء القيم، وضع فاتح/داكن، درج هاتف، وPWA | مكتمل ومتحقق منه بصريًا ضمن الجلسة المتاحة. |
| ملكية الحساب الأول وإدارة مستخدمي المنصة | سجل ملكية منصة وشاشة Super Admin وسجل تدقيق مستقل | أُنجز في هذه المراجعة. |

## سياسة المالك الرئيس (Super Admin)

يفرق النظام بين مفهومين متعمدين:

1. **مالك مساحة FAMILY:** يملك صلاحيات إدارة أعضاء ومساحة مالية محددة، ولا يكتسب تلقائيًا إدارة جميع مستخدمي التطبيق.
2. **مالك المنصة الرئيس / Super Admin:** يدار عبر جدول `platform_ownership` ذي السجل الوحيد، وهو المخوّل بإدارة الدور العام للمستخدمين.

تعمل سياسة التأسيس كما يأتي:

* لا تعتمد على بريد يرسله المتصفح أو قيمة ثابتة داخل الشفرة.
* تقبل فقط هوية صادرة من OAuth موثوق، تتضمن `loginMethod = google` وبريدًا ينتهي بـ `@gmail.com`.
* عند غياب سجل الملكية، يفوز أول حساب مؤهل يصل إلى عملية التأسيس؛ تحفظ القاعدة المستخدم الفائز وتمنحه دور `admin` العام.
* لا يمكن عبر واجهة أو إجراء الإدارة خفض دور المالك الرئيس، أو خفض آخر مدير عام، أو خفض دور المستخدم الذي ينفذ الإجراء على نفسه.
* تظل ترقية أو خفض الأدوار العامة منفصلة بالكامل عن عضويات مساحات FAMILY وسجلاتها المالية.

ثُبتت ملكية المنصة في القاعدة الحالية على أول هوية Google/Gmail مؤهلة تاريخيًا، وتملك كذلك دور مالك مساحة FAMILY الشخصية. لم يُحفظ عنوان البريد أو معرّف OAuth في هذا التقرير للحفاظ على الخصوصية.

## إصلاح سلامة الهوية والتهيئة

أظهر الفحص أن القاعدة كانت تسمح بمكررات رغم تعريف القيود في Drizzle. قبل أي إزالة، قيس وجود الحسابات والقيود والأحداث والعضويات والملفات الشخصية التابعة للسجلات المكررة. لم توجد حسابات أو أحداث مالية أو بيانات تابعة لمساحات العمل المكررة، ولذلك تم الاحتفاظ بالسجل الأقدم فقط لكل علاقة؛ لم يُحذف أي قيد دفتر أو معاملة أو حساب أو مستند.

بعد ذلك أُعيدت قيود تفرد OAuth وهوية المساحة والعضوية والملف، واستُبدل `onDuplicateKeyUpdate` في تهيئة FAMILY بفحص صريح داخل المعاملة قبل الإنشاء. يحمي ذلك النظام حتى لو تغيرت حالة القاعدة أو وصلت طلبات تسجيل دخول متزامنة.

## ما أُضيف في هذا الإصدار

| المكوّن | التغيير |
|---|---|
| `platform_ownership` | سجل ملكية منصة مفرد يثبت Super Admin الأول. |
| `platform_audit_events` | تدقيق مستقل لتثبيت المالك وتغييرات الأدوار العامة. |
| `server/platformOwnership.ts` | قواعد أهلية Gmail/Google وضوابط منع خفض المالك أو آخر مدير عام. |
| `server/platformAdminRouter.ts` | إجراءات خادمية محمية: قراءة المالك، الحسابات الموثقة، سجل الإدارة، وتغيير الدور مع تدقيق. |
| `server/db.ts` | تثبيت المالك الأول عند أول تسجيل دخول مؤهل، من دون اعتماد على قيمة عميل أو بريد ثابت. |
| `server/familyAccess.ts` | تهيئة آمنة تمنع تكرار مساحة المستخدم وعضويته وملفه. |
| `client/src/pages/PlatformAdministrationPage.tsx` | واجهة عربية لإدارة مستخدمي المنصة، منفصلة عن أعضاء مساحات FAMILY. |
| `server/platformOwnership.test.ts` | تغطية أهلية المالك وضوابط تغيير الدور. |

## التحقق المنفذ

| الفحص | النتيجة |
|---|---|
| فحص TypeScript | ناجح عبر `pnpm check`. |
| اختبارات Vitest | **26 ملف اختبار و74 اختبارًا ناجحًا** عبر `pnpm test`. |
| بناء الإنتاج | ناجح عبر `pnpm build`. |
| تحقق قاعدة البيانات | سجل ملكية واحد، وهوية OAuth واحدة، ومساحة شخصية وعضوية وملف واحد للحساب المؤسس، مع عودة قيود التفرد. |
| تحقق تفاعلي مصادق عليه | ظهرت شاشة `/admin/users` بالمالك المحمي، والحسابات الموثقة، وسجل تدقيق الإدارة. لم ينفذ الاختبار أي ترقية أو خفض للدور. |
| تحقق سجل المتصفح | خطأ ترتيب Hooks التاريخي من مراجعة سابقة لم يظهر بعد إصلاحه وإعادة تحميل الجلسة. |

## الحدود المقصودة

> الأسعار الواردة من Yahoo Finance قد تكون متأخرة أو غير متاحة. تعرض الواجهة المصدر والطابع الزمني وحالة التأخر، ولا تقدم هذه البيانات نصيحة مالية شخصية أو أمر شراء أو بيع.

> لا تنفذ مهمة التحديث الدوري أو الإشارات السوقية أو السيناريوهات أي صفقة أو قيد أو إعادة تقييم من تلقاء نفسها. تبقى كل حركة مالية ضمن إجراءات الدفتر المتوازن والموافقة البشرية عند انطباق السياسة.

## توصيات التطوير المستقبلية

| الأولوية | المقترح | القيمة المتوقعة |
|---|---|---|
| مرتفعة | إضافة دعوات مدير عام منفصلة عن دعوات أعضاء FAMILY، مع مراجعة ثنائية قبل الترقية. | تقليل مخاطر إدارة الأدوار الحساسة عند توسع الفريق. |
| مرتفعة | إضافة اختبار تكامل بقاعدة اختبار معزولة لتدفق OAuth الأول وتداخل تسجيلَي دخول متزامنين. | إثبات فعلي لاستمرارية قيود التفرد وسجل المالك في سيناريو المنافسة. |
| متوسطة | ربط الوثائق مشفرة بالكيانات المالية عبر واجهة اختيار سياقية. | تحسين التتبع بين البوليصة أو العقد والأصل أو الدين من دون كشف بيانات غير لازمة. |
| متوسطة | لوحة جودة بيانات تشرح تغطية الأسعار وتقادمها ومصدر كل لقطة. | مساعدة المستخدم على معرفة مدى اكتمال التقييم دون اتخاذ قرار آلي. |
| متوسطة | مصفوفة سياسات اعتماد قابلة للنسخ والإصدار والتجربة قبل النشر. | حوكمة أدق مع أثر تدقيقي أوضح. |
| منخفضة | تصدير تقرير إداري PDF قابل للتدقيق يخفي القيم عند تفعيل الخصوصية. | مشاركة آمنة مع مستشار أو عضو عائلة مفوض. |

## حالة التسليم

الإصدار الحالي جاهز للتشغيل ضمن الحدود الموثقة أعلاه، ونُشرت نقطة تحقق قابلة للاستعادة بعد الفحوص. ينصح باختبار تدفق دعوة مستخدم حقيقي في بيئة تشغيلية منفصلة قبل منح أي دور إداري إضافي.

## توسعات الحوكمة وجودة البيانات المنفذة لاحقًا

نُفذت التوصيات الثلاث الأولى الواردة أعلاه في تحديث مستقل. أضيف مسار دعوة مدير عام في شاشة `/admin/users`: يسجل المدير العام دعوة لبريد Gmail، ويجب أن يسجل صاحب البريد الدخول بواسطة Google بالبريد نفسه قبل تحول الدعوة إلى مرحلة المراجعة. لا تمنح المراجعة صلاحية تلقائية؛ يلزم قرار اعتماد صريح من **مدير عام ثالث مستقل عن منشئ الدعوة وصاحبها**، ثم يسجل كل انتقال في تدقيق المنصة. أزيلت الترقية المباشرة من جدول الحسابات حتى لا يمكن تجاوز هذا المسار.

أضيف كذلك اختبار تكامل OAuth متزامن بقاعدة SQLite مدمجة ومعزولة، ويستخدم دالة سياسة التأسيس نفسها التي تتصل في الإنتاج بعملية `INSERT IGNORE` الذرية. يثبت الاختبار أن محاولتي تسجيل دخول Google/Gmail مؤهلتين في الوقت نفسه تنتجان مالكًا واحدًا فقط. ويغطي اختبار SQL مستقل دورة الدعوة الكاملة من الإنشاء والتحقق إلى رفض اعتماد المنشئ أو المدعو ثم اعتماد المراجع المستقل وترقية الحساب. وأضيفت لوحة `/data-quality`، المتاحة من صفحة التقييم، لتعرض تغطية الأدوات والعملات والمصدر والتاريخ وحالة التأخر أو الفجوة من دون أي سعر أو كمية أو رصيد أو إجراء تلقائي.

أعيد تشغيل فحص TypeScript واختبارات Vitest وبناء الإنتاج بعد التحديث. النتيجة الحالية هي **26 ملف اختبار و77 اختبارًا ناجحًا**، وبناء إنتاج ناجح. تم التحقق المصادق عليه من واجهة الدعوات ولوحة الجودة؛ لم ينفذ المراجع أي اعتماد أو رفض أو إلغاء أو ترقية. لم يُنشأ أثناء المراجعة أي مستخدم تجريبي أو صفقة أو قيد مالي.


## ملحق الإصدار: السوق والبريد — 28 أغسطس 2026

أضيف في هذا الإصدار ملخص سوقي مخصص للوحة Dashboard يجمع الحيازات ذات الكمية الموجبة وعناصر قائمة المراقبة النشطة، ويزيل التكرار عندما تكون الأداة مملوكة ومراقبة معًا. يعرض الملخص الرمز واسم الأداة والسعر المسجل والعملة والمصدر والطابع الزمني وحالة `delayed` أو `stale` أو `unavailable`، ولا يعرض الكمية أو متوسط التكلفة أو قيمة الحيازة في هذا القسم. أضيفت روابط مباشرة إلى `/research/prices` و`/data-quality`.

أضيفت تفضيلات بريد سوقية اختيارية على مستوى مالك مساحة FAMILY، مع سجل تسليم مستقل وبصمة فريدة تمنع إعادة إرسال نفس مجموعة الإشارات عند إعادة محاولة Heartbeat. البريد لا يحتوي على سعر أو رصيد أو توصية شخصية أو زر تنفيذ، ولا ينشئ صفقة أو قيدًا أو إعادة تقييم. توجد حماية زمنية لمحاولات الفشل، وتبقى الدعوة مسجلة حتى إذا تعذر البريد مع نتيجة تسليم صريحة.

تستخدم دعوات المدير العام خدمة Nodemailer عبر Gmail SMTP الخادمية، وتقرأ `SMTP_USER` و`SMTP_PASS` و`SMTP_HOST` و`SMTP_PORT` من أسرار البيئة. أضيف مفتاح `SMTP_SEND_ENABLED`، وحالته الحالية **غير مفعلة** بناءً على قرار المستخدم. لذلك لم يُدّعَ أي تسليم فعلي، ولم يُرسل بريد اختبار. يتطلب الإطلاق لاحقًا إدخال App Password صحيحة ثم ضبط المفتاح إلى `yes` بعد موافقة المستخدم واجتياز اختبار اتصال SMTP المحمي.

| الفحص الحالي | النتيجة |
|---|---|
| `pnpm check` | ناجح |
| `pnpm test` | ناجح: 29 ملف اختبار و88 اختبارًا |
| `pnpm build` | ناجح |
| اختبار حماية SMTP | ناجح: الإيقاف يمنع الاتصال والإرسال |
| اختبار اتصال Gmail الفعلي | غير منفذ؛ الإعدادات غير صالحة/غير مفعلة حاليًا، ولم يُرسل بريد |
| تحديث Yahoo الدوري | مستمر عبر Heartbeat، مع تنبيهات بريدية اختيارية فقط بعد التفعيل والموافقة |

تبقى الأسعار من Yahoo Finance بيانات سوق متأخرة أو قابلة للانقطاع وليست مصدر الحقيقة المالي. يظل دفتر القيد المزدوج مصدر الحقيقة الوحيد، ولا يسمح مسار السوق أو Heartbeat أو البريد بإنشاء حركة مالية تلقائية.

## توصيات ما بعد الإصدار

يوصى أولًا بإدخال Gmail App Password من لوحة الأسرار فقط، ثم إبقاء `SMTP_SEND_ENABLED=no` إلى أن ينجح فحص الاتصال. بعد ذلك يُختبر إرسال دعوة واحدة إلى عنوان يوافق عليه المستخدم، ثم تُراقب نتائج التسليم الفعلية قبل تفعيل تنبيهات السوق.

يوصى لاحقًا بإضافة شاشة سجل تسليم بريد للمالك تعرض وقت المحاولة والحالة والسبب العام دون محتوى الرسالة، مع زر إعادة إرسال يدوي محكوم بحد زمني. كما يمكن إضافة اختيار مستلمين متعددين بعد تعريف سياسة موافقة منفصلة لكل مستخدم، مع الإبقاء على ملخص الإشارات خاليًا من الأسعار والأرصدة والتوصيات الشخصية.

أما على مستوى السوق، فمن المفيد لاحقًا إضافة مؤشرات زمنية لتغطية المصدر ونجاح التحديث، مع الاستمرار في الفصل الصريح بين بيانات القراءة والدفتر وعدم تحويل أي إشارة إلى أمر تداول أو إعادة تقييم تلقائي.
