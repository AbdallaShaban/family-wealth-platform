import DashboardLayout from "@/components/DashboardLayout";
import SensitiveValue from "@/components/SensitiveValue";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { BriefcaseBusiness, CircleAlert, Landmark, Loader2, Plus, TrendingUp } from "lucide-react";
import { useState } from "react";
import { toast } from "sonner";
import { useLocation } from "wouter";
import { trpc } from "@/lib/trpc";
import { formatMoney } from "@/lib/financialDisplay";

type AssetType = "equity" | "fund" | "bond" | "gold" | "real_estate" | "cash_equivalent" | "other";
const assetTypeLabel: Record<AssetType, string> = { equity: "سهم", fund: "صندوق", bond: "سند", gold: "ذهب", real_estate: "عقار", cash_equivalent: "ما يعادل النقد", other: "أخرى" };
const errorText = (error: unknown) => error instanceof Error ? error.message : "تعذر إكمال العملية الآن.";

export default function InvestmentsPageRedesign() {
  const utils = trpc.useUtils();
  const [, setLocation] = useLocation();
  const access = trpc.family.bootstrap.useQuery();
  const instruments = trpc.family.instruments.list.useQuery();
  const portfolio = trpc.family.portfolio.list.useQuery();
  const [name, setName] = useState("");
  const [symbol, setSymbol] = useState("");
  const [currency, setCurrency] = useState("EGP");
  const [assetType, setAssetType] = useState<AssetType>("equity");
  const create = trpc.family.instruments.create.useMutation({ onSuccess: () => { toast.success("تمت إضافة الأداة الاستثمارية ضمن نطاقك."); setName(""); setSymbol(""); void utils.family.instruments.list.invalidate(); }, onError: error => toast.error(errorText(error)) });
  const canAdvise = ["owner", "advisor"].includes(access.data?.membership.role || "");
  const holdings = portfolio.data ?? [];
  const valuedHoldings = holdings.filter(item => item.marketValue !== null && item.marketValue !== undefined);
  const needsQuote = holdings.filter(item => item.quoteStatus === "unavailable");
  const submit = (event: React.FormEvent) => { event.preventDefault(); create.mutate({ name, symbol: symbol || null, currency, assetType, isin: null }); };
  return <DashboardLayout><main className="mx-auto max-w-7xl space-y-6" dir="rtl">
    <header className="flex flex-col gap-4 border-b pb-6 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.15em] text-emerald-700">FAMILY / INVESTMENT CONTROL</p><h1 className="mt-2 text-3xl font-bold">الاستثمارات والحيازات</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-500">تظهر الحيازات من الصفقات المنشورة فقط. مصادر السوق والإشارات طبقات مراجعة، ولا تنشئ صفقة أو قيدًا أو توصية شخصية تلقائيًا.</p></div><div className="flex flex-wrap gap-2"><Button variant="outline" onClick={() => setLocation("/trades")}>تسجيل صفقة</Button><Button variant="outline" onClick={() => setLocation("/watchlist")}>تحديث الأسعار</Button><Button onClick={() => setLocation("/allocation")}>مراجعة التوزيع</Button></div></header>
    <section className="grid gap-4 sm:grid-cols-3"><Card className="fintech-surface-card"><CardHeader className="pb-3"><CardDescription>الحيازات المنشورة</CardDescription><CardTitle className="text-3xl">{portfolio.isLoading ? "—" : holdings.length}</CardTitle></CardHeader><CardContent className="text-xs text-muted-foreground">مراكز نشأت من صفقات فعلية في الدفتر.</CardContent></Card><Card className="fintech-surface-card"><CardHeader className="pb-3"><CardDescription>حيازات بسعر موثق</CardDescription><CardTitle className="text-3xl text-emerald-700">{portfolio.isLoading ? "—" : valuedHoldings.length}</CardTitle></CardHeader><CardContent className="text-xs text-muted-foreground">تدخل القيمة فقط عند وجود سعر سوق مؤرخ.</CardContent></Card><Card className="fintech-surface-card"><CardHeader className="pb-3"><CardDescription>تحتاج إلى سعر</CardDescription><CardTitle className="text-3xl text-amber-700">{portfolio.isLoading ? "—" : needsQuote.length}</CardTitle></CardHeader><CardContent className="text-xs text-muted-foreground">استخدم شاشة الأسعار لمراجعة المصدر والتاريخ.</CardContent></Card></section>
    <Tabs defaultValue="holdings" className="space-y-5"><TabsList className="investment-tabs-list"><TabsTrigger value="holdings">الحيازات</TabsTrigger><TabsTrigger value="instruments">الأدوات</TabsTrigger></TabsList><TabsContent value="holdings"><Card className="fintech-surface-card"><CardHeader><CardTitle className="flex items-center gap-2"><TrendingUp className="size-5 text-primary" />الحيازات الفعلية</CardTitle><CardDescription>الكمية ومتوسط التكلفة والقيمة تُستمد من الصفقات المنشورة والأسعار الموثقة.</CardDescription></CardHeader><CardContent>{portfolio.isLoading ? <p className="py-12 text-center text-sm text-muted-foreground">جارٍ تحميل الحيازات…</p> : portfolio.error ? <p className="rounded-xl border border-destructive/30 bg-destructive/10 p-4 text-sm text-destructive">{errorText(portfolio.error)}</p> : holdings.length ? <div className="overflow-x-auto"><table className="w-full min-w-[720px] text-right text-sm"><thead className="bg-slate-50 text-xs text-slate-600"><tr><th className="p-4">الأداة</th><th className="p-4">الكمية</th><th className="p-4">متوسط التكلفة</th><th className="p-4">القيمة</th><th className="p-4">حالة السعر</th></tr></thead><tbody className="divide-y">{holdings.map(position => <tr key={position.id}><td className="p-4"><strong>{position.instrumentName}</strong><p className="mt-1 text-xs text-muted-foreground">{position.symbol || "بدون رمز"} · {position.currency}</p></td><td className="p-4">{position.quantity}</td><td className="p-4"><SensitiveValue>{formatMoney(position.averageCost, position.costCurrency, 2)}</SensitiveValue></td><td className="p-4 font-semibold"><SensitiveValue>{position.marketValue ? formatMoney(position.marketValue, position.currency, 2) : "—"}</SensitiveValue></td><td className="p-4"><Badge variant={position.quoteStatus === "unavailable" ? "outline" : "secondary"}>{position.quoteStatus === "unavailable" ? "يتطلب سعرًا" : "متاح"}</Badge></td></tr>)}</tbody></table></div> : <div className="fintech-empty-state"><BriefcaseBusiness className="fintech-empty-state-icon" /><h3>لا توجد حيازات منشورة</h3><p>أضف أداة، ثم سجّل صفقة شراء صريحة ليظهر المركز ومتوسط التكلفة في هذه الصفحة.</p><Button className="mt-4" onClick={() => setLocation("/trades")}>بدء تسجيل صفقة</Button></div>}</CardContent></Card></TabsContent><TabsContent value="instruments"><section className="grid gap-6 lg:grid-cols-[.8fr_1.2fr]"><Card className="fintech-surface-card"><CardHeader><CardTitle className="flex items-center gap-2"><Plus className="size-5 text-primary" />إضافة أداة</CardTitle><CardDescription>تقتصر إضافة الأدوات وأسعارها على دور المالك أو المستشار.</CardDescription></CardHeader><CardContent><form onSubmit={submit} className="grid gap-4"><div className="grid gap-2"><Label htmlFor="instrument-name">الاسم</Label><Input id="instrument-name" value={name} onChange={event => setName(event.target.value)} disabled={!canAdvise} required minLength={2} /></div><div className="grid gap-3 sm:grid-cols-2"><div className="grid gap-2"><Label htmlFor="instrument-symbol">الرمز</Label><Input id="instrument-symbol" value={symbol} onChange={event => setSymbol(event.target.value.toUpperCase())} disabled={!canAdvise} maxLength={48} placeholder="مثال: GC=F" /></div><div className="grid gap-2"><Label htmlFor="instrument-currency">العملة</Label><Input id="instrument-currency" value={currency} onChange={event => setCurrency(event.target.value.toUpperCase())} disabled={!canAdvise} minLength={3} maxLength={3} required /></div></div><div className="grid gap-2"><Label>الفئة</Label><Select value={assetType} onValueChange={value => setAssetType(value as AssetType)} disabled={!canAdvise}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{Object.entries(assetTypeLabel).map(([value, label]) => <SelectItem key={value} value={value}>{label}</SelectItem>)}</SelectContent></Select></div><Button type="submit" disabled={!canAdvise || create.isPending}>{create.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}{canAdvise ? "حفظ الأداة" : "تتطلب صلاحية مستشار"}</Button></form></CardContent></Card><Card className="fintech-surface-card"><CardHeader><CardTitle className="flex items-center gap-2"><Landmark className="size-5 text-primary" />سجل الأدوات</CardTitle><CardDescription>وجود الأداة لا يعني وجود حيازة أو قيمة؛ كلاهما يتطلب صفقة وسعرًا موثقًا.</CardDescription></CardHeader><CardContent>{instruments.isLoading ? <p className="py-12 text-center text-sm text-muted-foreground">جارٍ تحميل الأدوات…</p> : instruments.error ? <p className="flex gap-2 rounded-xl border border-destructive/30 bg-destructive/10 p-4 text-sm text-destructive"><CircleAlert className="size-4 shrink-0" />{errorText(instruments.error)}</p> : instruments.data?.length ? <div className="overflow-x-auto"><table className="w-full min-w-[520px] text-right text-sm"><thead className="bg-slate-50 text-xs text-slate-600"><tr><th className="p-3">الاسم</th><th className="p-3">الرمز</th><th className="p-3">الفئة</th><th className="p-3">العملة</th></tr></thead><tbody className="divide-y">{instruments.data.map(item => <tr key={item.id}><td className="p-3 font-semibold">{item.name}</td><td className="p-3 font-mono text-xs">{item.symbol || "—"}</td><td className="p-3"><Badge variant="outline">{assetTypeLabel[item.assetType as AssetType] || item.assetType}</Badge></td><td className="p-3">{item.currency}</td></tr>)}</tbody></table></div> : <div className="fintech-empty-state"><Landmark className="fintech-empty-state-icon" /><h3>لا توجد أدوات استثمارية</h3><p>أضف أداة برمزها وبياناتها الأساسية، ثم سجّل سعرًا أو صفقة بصورة صريحة.</p></div>}</CardContent></Card></section></TabsContent></Tabs>
  </main></DashboardLayout>;
}
