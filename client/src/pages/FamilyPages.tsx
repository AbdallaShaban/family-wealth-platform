import DashboardLayout from "@/components/DashboardLayout";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Skeleton } from "@/components/ui/skeleton";
import { Textarea } from "@/components/ui/textarea";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { useAuth } from "@/_core/hooks/useAuth";
import { startLogin } from "@/const";
import { trpc } from "@/lib/trpc";
import FintechDashboard from "@/components/FintechDashboard";
import { ArrowDownLeft, ArrowLeftRight, ArrowUpRight, BadgeDollarSign, BookOpenCheck, BriefcaseBusiness, CalendarClock, CircleAlert, CreditCard, FileChartColumn, Landmark, Loader2, Plus, ReceiptText, ShieldCheck, Target, TrendingUp, Umbrella, UsersRound, WalletCards } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { useLocation } from "wouter";
import { Pie, PieChart, ResponsiveContainer, Tooltip } from "recharts";
import { toast } from "sonner";
import SensitiveValue from "@/components/SensitiveValue";
import { formatMoney } from "@/lib/financialDisplay";

type AccountType = "cash" | "bank" | "brokerage" | "wallet" | "credit" | "loan" | "asset";
type CashEvent = "deposit" | "withdrawal" | "income" | "expense";

const accountTypeLabel: Record<AccountType, string> = {
  cash: "نقدي", bank: "مصرفي", brokerage: "وساطة", wallet: "محفظة", credit: "بطاقة ائتمان", loan: "قرض", asset: "أصل",
};

const eventLabel: Record<string, string> = {
  opening_balance: "رصيد افتتاحي", deposit: "إيداع", withdrawal: "سحب", transfer: "تحويل", buy: "شراء", sell: "بيع", dividend: "توزيع نقدي", income: "دخل", expense: "مصروف", fee: "رسوم", tax: "ضريبة", adjustment: "تسوية", reversal: "عكس عملية",
};

function textError(error: unknown) {
  return error instanceof Error ? error.message : "حدث خطأ غير متوقع. حاول مجددًا.";
}

function money(value: string | null | undefined, currency: string) {
  return <SensitiveValue>{formatMoney(value, currency, 2)}</SensitiveValue>;
}

function dateTime(timestamp: number) {
  return new Intl.DateTimeFormat("ar-EG", { dateStyle: "medium", timeStyle: "short" }).format(new Date(timestamp));
}

function PageLoading() {
  return <div className="fintech-page-loading" dir="rtl"><Skeleton className="fintech-loading-title" /><div className="fintech-loading-cards"><Skeleton /><Skeleton /><Skeleton /></div><Skeleton className="fintech-loading-panel" /></div>;
}

function InlineError({ message }: { message: string }) {
  return <Card className="fintech-inline-error"><CardContent className="flex gap-3 p-5 text-sm"><CircleAlert className="mt-0.5 size-5 shrink-0" />{message}</CardContent></Card>;
}

function EmptyState({ icon: Icon, title, description, action }: { icon: typeof Landmark; title: string; description: string; action?: React.ReactNode }) {
  return <div className="fintech-empty-state"><Icon className="fintech-empty-state-icon" /><h3>{title}</h3><p>{description}</p>{action && <div className="mt-5">{action}</div>}</div>;
}

function useFamilyPermissions() {
  const bootstrap = trpc.family.bootstrap.useQuery();
  const role = bootstrap.data?.membership.role ?? "viewer";
  return { role, canEdit: ["owner", "advisor", "editor"].includes(role), canAdvise: ["owner", "advisor"].includes(role), isOwner: role === "owner" };
}

function CreateAccountDialog({ compact = false }: { compact?: boolean }) {
  const utils = trpc.useUtils();
  const access = useFamilyPermissions();
  const [open, setOpen] = useState(false);
  const [name, setName] = useState("");
  const [accountType, setAccountType] = useState<AccountType>("bank");
  const [currency, setCurrency] = useState("EGP");
  const [institution, setInstitution] = useState("");
  const [openingBalance, setOpeningBalance] = useState("");
  const create = trpc.family.accounts.create.useMutation({
    onSuccess: () => {
      toast.success("تم إنشاء الحساب وتسجيل رصيده الافتتاحي — إن وُجد — بقيد متوازن.");
      void utils.family.dashboard.invalidate();
      void utils.family.accounts.list.invalidate();
      void utils.family.ledger.recent.invalidate();
      setOpen(false); setName(""); setInstitution(""); setOpeningBalance("");
    },
    onError: error => toast.error(textError(error)),
  });
  const submit = (event: React.FormEvent) => {
    event.preventDefault();
    create.mutate({ name, accountType, currency, institution: institution || null, openingBalance: openingBalance || null, occurredAt: Date.now(), idempotencyKey: crypto.randomUUID() });
  };
  if (!access.canEdit) return <Button size={compact ? "sm" : "default"} variant="outline" disabled title="تتطلب إضافة حساب صلاحية محرر أو أعلى">لا تملك صلاحية إضافة حساب</Button>;
  return <Dialog open={open} onOpenChange={setOpen}>
    <DialogTrigger asChild><Button size={compact ? "sm" : "default"} className="gap-2 bg-emerald-700 hover:bg-emerald-800"><Plus className="size-4" />إضافة حساب</Button></DialogTrigger>
    <DialogContent dir="rtl" className="sm:max-w-lg"><DialogHeader><DialogTitle>إضافة حساب مالي</DialogTitle><DialogDescription>يُحفظ الحساب ضمن نطاقك فقط. الرصيد الافتتاحي اختياري ويُسجل كقيد متوازن قابل للتدقيق؛ أضف القروض والبطاقات من شاشة الديون والالتزامات.</DialogDescription></DialogHeader>
      <form onSubmit={submit} className="grid gap-4 pt-2">
        <div className="grid gap-2"><Label htmlFor="account-name">اسم الحساب</Label><Input id="account-name" value={name} onChange={e => setName(e.target.value)} placeholder="مثال: الحساب الجاري" required minLength={2} /></div>
        <div className="grid gap-2"><Label>نوع الحساب</Label><Select value={accountType} onValueChange={value => setAccountType(value as AccountType)}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{(["cash", "bank", "brokerage", "wallet", "asset"] as AccountType[]).map(type => <SelectItem value={type} key={type}>{accountTypeLabel[type]}</SelectItem>)}</SelectContent></Select></div>
        <div className="grid gap-2 sm:grid-cols-2"><div className="grid gap-2"><Label htmlFor="account-currency">العملة</Label><Input id="account-currency" value={currency} onChange={e => setCurrency(e.target.value.toUpperCase())} maxLength={3} minLength={3} required /></div><div className="grid gap-2"><Label htmlFor="opening-balance">الرصيد الافتتاحي</Label><Input id="opening-balance" inputMode="decimal" value={openingBalance} onChange={e => setOpeningBalance(e.target.value)} placeholder="اختياري" /></div></div>
        <div className="grid gap-2"><Label htmlFor="institution">الجهة المالية</Label><Input id="institution" value={institution} onChange={e => setInstitution(e.target.value)} placeholder="اختياري" /></div>
        <div className="flex justify-end gap-3 border-t pt-4"><Button type="button" variant="outline" onClick={() => setOpen(false)}>إلغاء</Button><Button type="submit" disabled={create.isPending}>{create.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}إنشاء الحساب</Button></div>
      </form>
    </DialogContent>
  </Dialog>;
}

function LegacyWealthDashboard() {
  const { user } = useAuth();
  const summary = trpc.family.dashboard.useQuery();
  const debtSummary = trpc.family.debts.list.useQuery();
  if (summary.isLoading) return <DashboardLayout><PageLoading /></DashboardLayout>;
  if (summary.error) return <DashboardLayout><InlineError message={textError(summary.error)} /></DashboardLayout>;
  const data = summary.data;
  const chartData = [
    ...(data?.accounts ?? []).filter(account => account.baseValue !== null && Number(account.baseValue) !== 0).map(account => ({ name: account.name, value: Number(account.baseValue), fill: "#0f766e" })),
    ...(data?.portfolio ?? []).filter(position => position.baseMarketValue !== null && Number(position.baseMarketValue) !== 0).map(position => ({ name: position.instrumentName, value: Number(position.baseMarketValue), fill: "#1d4ed8" })),
  ];
  return <DashboardLayout><div dir="rtl" className="mx-auto max-w-7xl space-y-7 pb-10">
    <header className="flex flex-col justify-between gap-4 border-b border-slate-200 pb-6 sm:flex-row sm:items-end"><div><p className="mb-2 flex items-center gap-2 text-xs font-semibold uppercase tracking-[.15em] text-emerald-700"><ShieldCheck className="size-4" />FAMILY / نطاق محمي</p><h1 className="text-3xl font-bold tracking-tight text-slate-950">مرحبًا، {user?.name || ""}</h1><p className="mt-2 text-sm text-slate-500">هذه النظرة مبنية على قيودك المالية المسجلة فقط، وليست تقديرًا أو محاكاة.</p></div><CreateAccountDialog /></header>
    <section className="grid gap-4 md:grid-cols-2 xl:grid-cols-4"><Card className="border-0 bg-slate-950 text-white shadow-xl"><CardHeader><CardDescription className="text-slate-300">صافي الثروة المقيم بالعملة الأساسية</CardDescription><CardTitle className="text-3xl text-white">{money(data?.netWorthBase, data?.workspace.baseCurrency || "EGP")}</CardTitle></CardHeader><CardContent className="text-sm text-slate-300">يشمل الأصول المقيمة والحيازات والالتزامات المسجلة عبر قيود دفتر الأستاذ.</CardContent></Card><Card><CardHeader className="pb-2"><CardDescription>الالتزامات المقيمة</CardDescription><CardTitle className="text-3xl text-red-700">{money(data?.liabilityBalanceBase, data?.workspace.baseCurrency || "EGP")}</CardTitle></CardHeader><CardContent className="text-sm text-slate-500">قروض وبطاقات ائتمان مسجلة ضمن مساحة FAMILY الحالية.</CardContent></Card><Card><CardHeader className="pb-2"><CardDescription>قيمة الاستثمار الموثقة</CardDescription><CardTitle className="text-3xl">{money(data?.investmentValueBase, data?.workspace.baseCurrency || "EGP")}</CardTitle></CardHeader><CardContent className={`text-sm ${Number(data?.unrealizedPnlBase || 0) >= 0 ? "text-emerald-700" : "text-red-700"}`}>ربح/خسارة غير محققة: {money(data?.unrealizedPnlBase, data?.workspace.baseCurrency || "EGP")}</CardContent></Card><Card><CardHeader className="pb-2"><CardDescription>حالة التقييم</CardDescription><CardTitle className="text-xl text-emerald-700">موثق وآمن</CardTitle></CardHeader><CardContent className="text-sm text-slate-500">{data?.unvaluedCurrencies.length || data?.unvaluedInstruments.length ? "توجد أرصدة أو حيازات بحاجة إلى سعر موثق قبل إدراجها في الإجمالي." : "كل الأرصدة والحيازات المسجلة قابلة للتقييم بالعملة الأساسية."}</CardContent></Card></section>
    <Card><CardHeader><CardTitle className="flex items-center gap-2"><CreditCard className="size-5 text-red-700" />الالتزامات الحالية</CardTitle><CardDescription>تفصيل الرصيد المستحق والأقساط الدنيا من عقود الديون وقيودها، داخل مساحة FAMILY النشطة فقط.</CardDescription></CardHeader><CardContent>{debtSummary.isLoading ? <Skeleton className="h-24" /> : debtSummary.error ? <InlineError message={textError(debtSummary.error)} /> : debtSummary.data?.length ? <div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{debtSummary.data.map(debt => <div key={debt.id} className="rounded-xl border bg-slate-50 p-4"><div className="flex items-center justify-between gap-3"><p className="font-semibold text-slate-900">{debt.name}</p><Badge variant={debt.status === "active" ? "secondary" : "outline"}>{debt.status === "active" ? "نشط" : debt.status === "paid" ? "مسدد" : "مؤرشف"}</Badge></div><p className="mt-3 text-lg font-semibold text-red-700">{money(debt.outstanding, debt.currency)}</p><p className="mt-1 text-xs text-slate-500">الحد الأدنى: {money(debt.minimumPayment, debt.currency)} · {debt.annualInterestRate}% سنويًا</p></div>)}</div> : <p className="text-sm text-slate-500">لا توجد التزامات مسجلة ضمن مساحة FAMILY الحالية.</p>}</CardContent></Card>
    <section className="grid gap-5 lg:grid-cols-[.85fr_1.15fr]"><Card><CardHeader><CardTitle className="flex items-center gap-2"><WalletCards className="size-5 text-emerald-700" />توزيع الثروة المقيمة</CardTitle><CardDescription>يتغير الرسم من الحسابات والحيازات ذات الأسعار الموثقة بعد تسجيلها.</CardDescription></CardHeader><CardContent className="h-64">{chartData.length ? <ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={chartData} dataKey="value" nameKey="name" innerRadius={54} outerRadius={84} paddingAngle={3} /><Tooltip formatter={(value: number) => formatMoney(String(value), data?.workspace.baseCurrency || "EGP", 2)} /></PieChart></ResponsiveContainer> : <EmptyState icon={WalletCards} title="لا توجد أرصدة مقيمة بعد" description="أضف حسابًا ورصيدًا افتتاحيًا أو سجل عملية مالية ليتشكل الرسم من واقع بياناتك." />}</CardContent></Card>
      <Card><CardHeader className="flex-row items-center justify-between"><div><CardTitle className="flex items-center gap-2"><BookOpenCheck className="size-5 text-emerald-700" />آخر العمليات</CardTitle><CardDescription>كل عملية منشورة تتصل بقيد متوازن وسجل تدقيق.</CardDescription></div></CardHeader><CardContent>{data?.recentEvents.length ? <div className="divide-y">{data.recentEvents.map(event => <div className="flex items-center justify-between gap-4 py-3" key={event.id}><div><p className="font-medium text-slate-800">{eventLabel[event.eventType] || event.eventType}</p><p className="mt-1 text-xs text-slate-500">{dateTime(event.occurredAt)}{event.memo ? ` · ${event.memo}` : ""}</p></div><p className="font-semibold text-slate-950">{money(event.grossAmount, event.currency)}</p></div>)}</div> : <EmptyState icon={BookOpenCheck} title="لا توجد عمليات مالية" description="ستظهر هنا الإيداعات والتحويلات والاستثمارات بعد تسجيلها، مع حالة القيد والتاريخ." />}</CardContent></Card>
    </section>
  </div></DashboardLayout>;
}

export function WealthDashboard() {
  return <FintechDashboard />;
}

export function FamilyHomeGate() {
  const { loading, isAuthenticated } = useAuth();
  const [, setLocation] = useLocation();
  if (loading) return <div className="min-h-screen bg-[#f3f0e9] p-8"><Skeleton className="mx-auto mt-24 h-72 max-w-4xl" /></div>;
  if (isAuthenticated) return <WealthDashboard />;
  return <main dir="rtl" className="min-h-screen overflow-hidden bg-[#f3f0e9] text-slate-950"><div className="mx-auto grid min-h-screen max-w-7xl lg:grid-cols-[1.1fr_.9fr]"><section className="flex flex-col justify-between p-7 sm:p-12 lg:p-16"><div className="flex items-center gap-3"><div className="flex size-10 items-center justify-center rounded-xl bg-emerald-700 text-white"><ShieldCheck className="size-5" /></div><div><p className="font-bold tracking-[.16em]">FAMILY</p><p className="text-xs text-slate-500">PRIVATE WEALTH CONTROL</p></div></div><div className="max-w-xl py-14"><p className="mb-5 text-sm font-semibold text-emerald-700">منظومة مالية شخصية محمية</p><h1 className="text-4xl font-bold leading-tight tracking-tight sm:text-6xl">الرؤية الواضحة تبدأ من دفتر <span className="text-emerald-700">موثوق</span>.</h1><p className="mt-6 max-w-lg text-base leading-8 text-slate-600">FAMILY يربط الحسابات والعمليات والحيازات والأهداف ضمن مصدر مالي واحد، مع صلاحيات على مستوى الخادم وسجل تدقيق للعمليات المنشورة.</p><div className="mt-9 flex flex-col gap-3 sm:flex-row"><Button size="lg" className="bg-emerald-700 px-7 hover:bg-emerald-800" onClick={() => startLogin()}>الدخول إلى مساحتك الآمنة</Button><Button size="lg" variant="outline" onClick={() => setLocation("/assessment")}>عرض التقييم الهندسي</Button></div></div><p className="text-xs leading-5 text-slate-500">لا تعرض هذه الصفحة أي رصيد أو بيانات مالية. تُنشأ مساحة FAMILY الخاصة بك بعد المصادقة.</p></section><aside className="relative bg-slate-950 px-7 py-12 text-white sm:px-12 lg:p-16"><div className="absolute inset-0 bg-[radial-gradient(circle_at_20%_20%,rgba(16,185,129,.23),transparent_28%),radial-gradient(circle_at_80%_75%,rgba(217,119,6,.16),transparent_30%)]" /><div className="relative flex h-full flex-col justify-between"><div className="border-b border-white/15 pb-6 text-xs font-semibold tracking-[.18em] text-emerald-300">FAMILY / CONTROL LAYER</div><div className="space-y-5"><div className="rounded-2xl border border-white/15 bg-white/5 p-5 backdrop-blur"><p className="text-sm text-slate-300">مصدر الحقيقة</p><p className="mt-2 text-2xl font-semibold">دفتر قيود متوازن</p><p className="mt-3 text-sm leading-6 text-slate-400">لا يتم تغيير الرصيد من الواجهة؛ كل عملية تُرحّل في دفتر القيود وتُسجّل في الأثر التدقيقي.</p></div><div className="grid grid-cols-1 gap-4 sm:grid-cols-2"><div className="rounded-xl bg-white/5 p-4"><p className="text-emerald-300">النطاق</p><p className="mt-2 font-semibold">معزول لكل مستخدم</p></div><div className="rounded-xl bg-white/5 p-4"><p className="text-emerald-300">التقييم</p><p className="mt-2 font-semibold">بأسعار موثقة فقط</p></div></div></div><p className="text-xs leading-5 text-slate-400">قد يطلب منك تسجيل الدخول لإكمال جلسة مصادقة آمنة قبل الوصول إلى أي بيانات أو عمليات.</p></div></aside></div></main>;
}

export function AccountsPage() {
  const query = trpc.family.accounts.list.useQuery();
  const workspace = trpc.family.bootstrap.useQuery();
  const baseCurrency = workspace.data?.workspace.baseCurrency || "EGP";
  return <DashboardLayout><div dir="rtl" className="mx-auto max-w-6xl space-y-6"><header className="flex flex-col gap-4 border-b pb-6 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.15em] text-emerald-700">FAMILY / Accounts</p><h1 className="mt-2 text-3xl font-bold">الحسابات</h1><p className="mt-2 text-sm text-slate-500">الأرصدة هنا ناتج قيود الدفتر، وليست حقولًا قابلة للتعديل يدويًا.</p></div><CreateAccountDialog /></header>{query.isLoading ? <PageLoading /> : query.error ? <InlineError message={textError(query.error)} /> : !query.data?.length ? <EmptyState icon={Landmark} title="لا توجد حسابات ضمن نطاقك" description="أضف حسابًا نقديًا أو مصرفيًا أو استثماريًا. يمكنك إدخال رصيد افتتاحي وسيُسجل بقيد مالي متوازن." action={<CreateAccountDialog />} /> : <Card><CardContent className="p-0"><div className="overflow-x-auto"><table className="w-full min-w-[720px] text-right text-sm"><thead className="bg-slate-50 text-xs text-slate-500"><tr><th className="p-4 font-medium">الحساب</th><th className="p-4 font-medium">النوع</th><th className="p-4 font-medium">الجهة</th><th className="p-4 font-medium">الرصيد</th><th className="p-4 font-medium">التقييم</th></tr></thead><tbody className="divide-y">{query.data.map(account => <tr key={account.id} className="hover:bg-emerald-50/40"><td className="p-4"><p className="font-semibold text-slate-900">{account.name}</p><p className="mt-1 text-xs text-slate-500">{account.currency}</p></td><td className="p-4"><Badge variant="secondary">{accountTypeLabel[account.accountType as AccountType] || account.accountType}</Badge></td><td className="p-4 text-slate-600">{account.institution || "—"}</td><td className="p-4 font-semibold">{money(account.balance, account.currency)}</td><td className="p-4">{account.baseValue !== null ? <span className="font-medium text-emerald-700">{money(account.baseValue, baseCurrency)}</span> : <span className="text-amber-700">يتطلب سعر صرف</span>}</td></tr>)}</tbody></table></div></CardContent></Card>}</div></DashboardLayout>;
}

export function LedgerPage() {
  const utils = trpc.useUtils();
  const access = useFamilyPermissions();
  const accounts = trpc.family.accounts.list.useQuery();
  const events = trpc.family.ledger.recent.useQuery();
  const [eventType, setEventType] = useState<CashEvent>("deposit");
  const [accountId, setAccountId] = useState("");
  const [amount, setAmount] = useState("");
  const [memo, setMemo] = useState("");
  const selected = accounts.data?.find(account => String(account.id) === accountId);
  const post = trpc.family.ledger.postCash.useMutation({ onSuccess: result => { toast.success(result.approvalRequired ? "تم تجميد العملية وإنشاء طلب اعتماد عائلي." : "تم نشر العملية وقيدها في الدفتر."); void utils.family.dashboard.invalidate(); void utils.family.accounts.list.invalidate(); void utils.family.ledger.recent.invalidate(); setAmount(""); setMemo(""); }, onError: error => toast.error(textError(error)) });
  const submit = (event: React.FormEvent) => { event.preventDefault(); if (!selected) return toast.error("اختر حسابًا أولًا."); post.mutate({ eventType, accountId: Number(accountId), amount, currency: selected.currency, occurredAt: Date.now(), memo: memo || null, idempotencyKey: crypto.randomUUID() }); };
  return <DashboardLayout><div dir="rtl" className="mx-auto max-w-6xl space-y-6"><header className="border-b pb-6"><p className="text-xs font-semibold uppercase tracking-[.15em] text-emerald-700">FAMILY / Ledger</p><h1 className="mt-2 text-3xl font-bold">العمليات والدفتر</h1><p className="mt-2 text-sm text-slate-500">لا تعدّل هذه الشاشة الرصيد مباشرةً؛ تنشئ عملية تُرحّل إلى قيد مدين/دائن متوازن.</p></header><div className="grid gap-6 lg:grid-cols-[.75fr_1.25fr]"><Card><CardHeader><CardTitle className="flex items-center gap-2"><BadgeDollarSign className="size-5 text-emerald-700" />تسجيل عملية نقدية</CardTitle><CardDescription>تحقق الخادم من الحساب والعملة والرصيد المتاح قبل النشر.</CardDescription></CardHeader><CardContent>{accounts.isLoading ? <Skeleton className="h-64" /> : accounts.data?.length ? <form onSubmit={submit} className="grid gap-4"><div className="grid gap-2"><Label>نوع العملية</Label><Select value={eventType} onValueChange={value => setEventType(value as CashEvent)} disabled={!access.canEdit}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="deposit">إيداع</SelectItem><SelectItem value="withdrawal">سحب</SelectItem><SelectItem value="income">دخل</SelectItem><SelectItem value="expense">مصروف</SelectItem></SelectContent></Select></div><div className="grid gap-2"><Label>الحساب</Label><Select value={accountId} onValueChange={setAccountId} disabled={!access.canEdit}><SelectTrigger><SelectValue placeholder="اختر حسابًا" /></SelectTrigger><SelectContent>{accounts.data.map(account => <SelectItem key={account.id} value={String(account.id)}>{account.name} — {account.currency}</SelectItem>)}</SelectContent></Select></div><div className="grid gap-2"><Label htmlFor="transaction-amount">المبلغ</Label><Input id="transaction-amount" inputMode="decimal" value={amount} onChange={e => setAmount(e.target.value)} disabled={!access.canEdit} required /></div><div className="grid gap-2"><Label htmlFor="transaction-memo">مذكرة</Label><Textarea id="transaction-memo" value={memo} onChange={e => setMemo(e.target.value)} disabled={!access.canEdit} maxLength={2000} placeholder="اختياري" /></div><Button type="submit" disabled={post.isPending || !access.canEdit}>{post.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}{access.canEdit ? "نشر العملية" : "تتطلب صلاحية محرر"}</Button></form> : <EmptyState icon={Landmark} title="أضف حسابًا قبل تسجيل العمليات" description="العملية يجب أن ترتبط بحساب مملوك لنطاقك المالي." action={<CreateAccountDialog />} />}</CardContent></Card><Card><CardHeader><CardTitle className="flex items-center gap-2"><ArrowLeftRight className="size-5 text-emerald-700" />العمليات المنشورة</CardTitle><CardDescription>العمليات الحديثة تُقرأ مباشرة من دفتر نطاقك.</CardDescription></CardHeader><CardContent>{events.isLoading ? <Skeleton className="h-64" /> : events.error ? <InlineError message={textError(events.error)} /> : events.data?.length ? <div className="divide-y">{events.data.map(event => <div className="flex items-center justify-between gap-4 py-4" key={event.id}><div className="flex items-center gap-3"><div className="rounded-full bg-emerald-50 p-2 text-emerald-700">{["withdrawal", "expense", "sell"].includes(event.eventType) ? <ArrowUpRight className="size-4" /> : <ArrowDownLeft className="size-4" />}</div><div><p className="font-semibold">{eventLabel[event.eventType] || event.eventType}</p><p className="mt-1 text-xs text-slate-500">{dateTime(event.occurredAt)}{event.memo ? ` · ${event.memo}` : ""}</p></div></div><div className="text-left"><p className="font-semibold">{money(event.grossAmount, event.currency)}</p><Badge className="mt-1" variant={event.status === "posted" ? "secondary" : "outline"}>{event.status}</Badge></div></div>)}</div> : <EmptyState icon={ReceiptTextIcon} title="دفترك جاهز لبدء التسجيل" description="بعد نشر أول إيداع أو مصروف أو تحويل ستظهر العملية هنا مع تاريخها وحالتها." />}</CardContent></Card></div></div></DashboardLayout>;
}

const ReceiptTextIcon = BookOpenCheck;

export function InvestmentsPage() {
  const utils = trpc.useUtils();
  const access = useFamilyPermissions();
  const list = trpc.family.instruments.list.useQuery();
  const [name, setName] = useState(""); const [symbol, setSymbol] = useState(""); const [currency, setCurrency] = useState("EGP"); const [assetType, setAssetType] = useState<"equity" | "fund" | "bond" | "gold" | "real_estate" | "cash_equivalent" | "other">("equity");
  const create = trpc.family.instruments.create.useMutation({ onSuccess: () => { toast.success("تمت إضافة الأداة الاستثمارية ضمن نطاقك."); void utils.family.instruments.list.invalidate(); setName(""); setSymbol(""); }, onError: error => toast.error(textError(error)) });
  const submit = (event: React.FormEvent) => { event.preventDefault(); create.mutate({ name, symbol: symbol || null, currency, assetType, isin: null }); };
  return <DashboardLayout><div dir="rtl" className="mx-auto max-w-6xl space-y-6"><header className="border-b pb-6"><p className="text-xs font-semibold uppercase tracking-[.15em] text-emerald-700">FAMILY / Investments</p><h1 className="mt-2 text-3xl font-bold">الاستثمارات</h1><p className="mt-2 text-sm text-slate-500">سجل الأدوات مستقل عن الحيازات؛ الشراء والبيع يغيران الحيازة ومتوسط التكلفة عبر محرك الدفتر.</p></header><div className="grid gap-6 lg:grid-cols-[.75fr_1.25fr]"><Card><CardHeader><CardTitle className="flex items-center gap-2"><Plus className="size-5 text-emerald-700" />إضافة أداة</CardTitle><CardDescription>يقتصر إنشاء الأداة وأسعارها على دور المالك أو المستشار.</CardDescription></CardHeader><CardContent><form onSubmit={submit} className="grid gap-4"><div className="grid gap-2"><Label htmlFor="instrument-name">الاسم</Label><Input id="instrument-name" value={name} onChange={e => setName(e.target.value)} disabled={!access.canAdvise} required minLength={2} /></div><div className="grid gap-2 sm:grid-cols-2"><div className="grid gap-2"><Label htmlFor="symbol">الرمز</Label><Input id="symbol" value={symbol} onChange={e => setSymbol(e.target.value.toUpperCase())} disabled={!access.canAdvise} maxLength={48} /></div><div className="grid gap-2"><Label htmlFor="instrument-currency">العملة</Label><Input id="instrument-currency" value={currency} onChange={e => setCurrency(e.target.value.toUpperCase())} disabled={!access.canAdvise} minLength={3} maxLength={3} required /></div></div><div className="grid gap-2"><Label>الفئة</Label><Select value={assetType} onValueChange={value => setAssetType(value as typeof assetType)} disabled={!access.canAdvise}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="equity">سهم</SelectItem><SelectItem value="fund">صندوق</SelectItem><SelectItem value="bond">سند</SelectItem><SelectItem value="gold">ذهب</SelectItem><SelectItem value="real_estate">عقار</SelectItem><SelectItem value="cash_equivalent">ما يعادل النقد</SelectItem><SelectItem value="other">أخرى</SelectItem></SelectContent></Select></div><Button type="submit" disabled={create.isPending || !access.canAdvise}>{create.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}{access.canAdvise ? "حفظ الأداة" : "تتطلب صلاحية مستشار"}</Button></form></CardContent></Card><Card><CardHeader><CardTitle className="flex items-center gap-2"><BriefcaseBusiness className="size-5 text-emerald-700" />سجل الأدوات</CardTitle><CardDescription>تظهر الحيازات وقيمها فقط بعد شراء فعلي وسعر موثق، لا قبل ذلك.</CardDescription></CardHeader><CardContent>{list.isLoading ? <Skeleton className="h-64" /> : list.error ? <InlineError message={textError(list.error)} /> : list.data?.length ? <div className="divide-y">{list.data.map(item => <div key={item.id} className="flex items-center justify-between py-4"><div><p className="font-semibold">{item.name}</p><p className="mt-1 text-xs text-slate-500">{item.symbol || "بدون رمز"} · {item.currency}</p></div><Badge variant="secondary">{item.assetType}</Badge></div>)}</div> : <EmptyState icon={BriefcaseBusiness} title="لا توجد أدوات استثمارية" description="أضف أداة أولًا، ثم يتم تسجيل الشراء أو البيع بسعر وكمية ورسوم وضرائب ضمن محرك العمليات." />}</CardContent></Card></div></div></DashboardLayout>;
}

export function TradingPage() {
  const utils = trpc.useUtils();
  const accounts = trpc.family.accounts.list.useQuery();
  const instruments = trpc.family.instruments.list.useQuery();
  const portfolio = trpc.family.portfolio.list.useQuery();
  const [side, setSide] = useState<"buy" | "sell">("buy");
  const [accountId, setAccountId] = useState("");
  const [instrumentId, setInstrumentId] = useState("");
  const [quantity, setQuantity] = useState("");
  const [unitPrice, setUnitPrice] = useState("");
  const [feeAmount, setFeeAmount] = useState("");
  const [taxAmount, setTaxAmount] = useState("");
  const [memo, setMemo] = useState("");
  const [quoteInstrumentId, setQuoteInstrumentId] = useState("");
  const [quotePrice, setQuotePrice] = useState("");
  const trade = trpc.family.ledger.trade.useMutation({
    onSuccess: () => {
      toast.success("تم نشر الصفقة وتحديث الحيازة ومتوسط التكلفة في معاملة واحدة.");
      void utils.family.dashboard.invalidate(); void utils.family.accounts.list.invalidate(); void utils.family.ledger.recent.invalidate(); void utils.family.portfolio.list.invalidate();
      setQuantity(""); setUnitPrice(""); setFeeAmount(""); setTaxAmount(""); setMemo("");
    },
    onError: error => toast.error(textError(error)),
  });
  const recordQuote = trpc.family.prices.recordManual.useMutation({
    onSuccess: () => { toast.success("تم حفظ سعر السوق كمصدر يدوي موثق."); void utils.family.portfolio.list.invalidate(); void utils.family.dashboard.invalidate(); setQuotePrice(""); },
    onError: error => toast.error(textError(error)),
  });
  const tradeAccounts = (accounts.data ?? []).filter(account => ["cash", "bank", "brokerage", "wallet"].includes(account.accountType));
  const selectedAccount = tradeAccounts.find(account => String(account.id) === accountId);
  const selectedInstrument = instruments.data?.find(instrument => String(instrument.id) === instrumentId);
  useEffect(() => {
    const raw = sessionStorage.getItem("family-trade-draft");
    if (!raw) return;
    try {
      const draft = JSON.parse(raw) as { instrumentId?: number; side?: "buy" | "sell" };
      if (Number.isInteger(draft.instrumentId) && (draft.side === "buy" || draft.side === "sell")) { setInstrumentId(String(draft.instrumentId)); setSide(draft.side); }
    } finally { sessionStorage.removeItem("family-trade-draft"); }
  }, []);
  const chargePreviewInput = useMemo(() => {
    const quantityValue = Number(quantity);
    const priceValue = Number(unitPrice);
    const grossAmount = Number.isFinite(quantityValue) && Number.isFinite(priceValue) && quantityValue > 0 && priceValue > 0 ? String(quantityValue * priceValue) : "1";
    return { side, grossAmount, currency: selectedInstrument?.currency || "USD" };
  }, [quantity, selectedInstrument?.currency, side, unitPrice]);
  const chargePreviewReady = chargePreviewInput.grossAmount !== "1" && Boolean(selectedInstrument);
  const chargePreview = trpc.family.feeTax.preview.useQuery(chargePreviewInput, { enabled: chargePreviewReady });
  const applySuggestedCharges = () => {
    if (!chargePreview.data) return;
    setFeeAmount(chargePreview.data.suggestedFeeAmount);
    setTaxAmount(chargePreview.data.suggestedTaxAmount);
    toast.message("تم تطبيق اقتراحات القواعد على النموذج. راجعها قبل نشر الصفقة.");
  };
  const submitTrade = (event: React.FormEvent) => {
    event.preventDefault();
    if (!selectedAccount || !selectedInstrument) return toast.error("اختر حسابًا وأداة استثمارية أولًا.");
    trade.mutate({ side, accountId: selectedAccount.id, instrumentId: selectedInstrument.id, quantity, unitPrice, feeAmount: feeAmount || null, taxAmount: taxAmount || null, occurredAt: Date.now(), memo: memo || null, idempotencyKey: crypto.randomUUID() });
  };
  const submitQuote = (event: React.FormEvent) => { event.preventDefault(); if (!quoteInstrumentId) return toast.error("اختر الأداة أولًا."); recordQuote.mutate({ instrumentId: Number(quoteInstrumentId), price: quotePrice, asOf: Date.now() }); };
  return <DashboardLayout><div dir="rtl" className="mx-auto max-w-7xl space-y-6"><header className="border-b pb-6"><p className="text-xs font-semibold uppercase tracking-[.15em] text-emerald-700">FAMILY / Trades & Holdings</p><h1 className="mt-2 text-3xl font-bold">التداول والحيازات</h1><p className="mt-2 text-sm text-slate-500">الصفقة تسجل القيمة والرسوم والضرائب، ثم تغيّر الحيازة ومتوسط التكلفة والدفتر معًا أو لا تسجل أي شيء.</p></header><div className="grid gap-6 xl:grid-cols-[.8fr_.8fr_1.35fr]"><Card><CardHeader><CardTitle>صفقة شراء أو بيع</CardTitle><CardDescription>تتحقق المنصة من الرصيد أو الكمية قبل النشر.</CardDescription></CardHeader><CardContent>{accounts.isLoading || instruments.isLoading ? <Skeleton className="h-80" /> : tradeAccounts.length && instruments.data?.length ? <form onSubmit={submitTrade} className="grid gap-4"><div className="grid gap-2"><Label>العملية</Label><Select value={side} onValueChange={value => setSide(value as "buy" | "sell")}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="buy">شراء</SelectItem><SelectItem value="sell">بيع</SelectItem></SelectContent></Select></div><div className="grid gap-2"><Label>حساب التسوية</Label><Select value={accountId} onValueChange={setAccountId}><SelectTrigger><SelectValue placeholder="اختر حسابًا" /></SelectTrigger><SelectContent>{tradeAccounts.map(account => <SelectItem key={account.id} value={String(account.id)}>{account.name} — {account.currency}</SelectItem>)}</SelectContent></Select></div><div className="grid gap-2"><Label>الأداة</Label><Select value={instrumentId} onValueChange={setInstrumentId}><SelectTrigger><SelectValue placeholder="اختر أداة" /></SelectTrigger><SelectContent>{instruments.data.map(instrument => <SelectItem key={instrument.id} value={String(instrument.id)}>{instrument.name} — {instrument.currency}</SelectItem>)}</SelectContent></Select></div><div className="grid grid-cols-2 gap-3"><div className="grid gap-2"><Label htmlFor="trade-quantity">الكمية</Label><Input id="trade-quantity" value={quantity} onChange={e => setQuantity(e.target.value)} inputMode="decimal" required /></div><div className="grid gap-2"><Label htmlFor="trade-price">سعر الوحدة</Label><Input id="trade-price" value={unitPrice} onChange={e => setUnitPrice(e.target.value)} inputMode="decimal" required /></div></div><div className="grid grid-cols-2 gap-3"><div className="grid gap-2"><Label htmlFor="trade-fee">الرسوم</Label><Input id="trade-fee" value={feeAmount} onChange={e => setFeeAmount(e.target.value)} inputMode="decimal" placeholder="0" /></div><div className="grid gap-2"><Label htmlFor="trade-tax">الضرائب</Label><Input id="trade-tax" value={taxAmount} onChange={e => setTaxAmount(e.target.value)} inputMode="decimal" placeholder="0" /></div></div>{chargePreviewReady && chargePreview.data?.rows.length ? <div className="rounded-lg border border-emerald-200 bg-emerald-50 p-3 text-xs text-emerald-950"><p>اقتراح القواعد: رسوم {money(chargePreview.data.suggestedFeeAmount, chargePreview.data.currency)} وضريبة {money(chargePreview.data.suggestedTaxAmount, chargePreview.data.currency)}. لا يغيّر النموذج حتى تختار التطبيق.</p><Button type="button" variant="outline" size="sm" className="mt-2" onClick={applySuggestedCharges}>تطبيق الاقتراح</Button></div> : null}<div className="grid gap-2"><Label htmlFor="trade-memo">مذكرة</Label><Textarea id="trade-memo" value={memo} onChange={e => setMemo(e.target.value)} maxLength={2000} placeholder="اختياري" /></div><Button type="submit" disabled={trade.isPending}>{trade.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}نشر الصفقة</Button></form> : <EmptyState icon={BriefcaseBusiness} title="جهّز الحساب والأداة أولًا" description="لإدخال صفقة فعلية، أضف حساب تسوية وأداة استثمارية بالعملة نفسها." />}</CardContent></Card><Card><CardHeader><CardTitle>سعر سوق موثق</CardTitle><CardDescription>لا يدخل تقييم الحيازة في الإجماليات إلا بسعر مسجل ومؤرخ.</CardDescription></CardHeader><CardContent>{instruments.isLoading ? <Skeleton className="h-64" /> : instruments.data?.length ? <form onSubmit={submitQuote} className="grid gap-4"><div className="grid gap-2"><Label>الأداة</Label><Select value={quoteInstrumentId} onValueChange={setQuoteInstrumentId}><SelectTrigger><SelectValue placeholder="اختر أداة" /></SelectTrigger><SelectContent>{instruments.data.map(instrument => <SelectItem key={instrument.id} value={String(instrument.id)}>{instrument.name} — {instrument.currency}</SelectItem>)}</SelectContent></Select></div><div className="grid gap-2"><Label htmlFor="market-quote">سعر الوحدة</Label><Input id="market-quote" value={quotePrice} onChange={e => setQuotePrice(e.target.value)} inputMode="decimal" required /></div><div className="rounded-lg bg-amber-50 p-3 text-xs leading-5 text-amber-800">يمكن جلب سعر السهم أو الصندوق أو الذهب ذي الرمز السوقي من Yahoo Finance في شاشة الأسعار. يظل السعر مؤرخًا وموسومًا بحالة تأخره.</div><Button type="submit" disabled={recordQuote.isPending}>{recordQuote.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}حفظ السعر اليدوي</Button></form> : <EmptyState icon={BriefcaseBusiness} title="لا توجد أداة لتسعيرها" description="أضف الأداة الاستثمارية من صفحة الاستثمارات أولًا." />}</CardContent></Card><Card><CardHeader><CardTitle className="flex items-center gap-2"><TrendingUp className="size-5 text-emerald-700" />الحيازات الفعلية</CardTitle><CardDescription>الكمية والتكلفة ناتجتان من الصفقات المنشورة؛ القيمة لا تظهر بلا سعر موثق.</CardDescription></CardHeader><CardContent>{portfolio.isLoading ? <Skeleton className="h-80" /> : portfolio.error ? <InlineError message={textError(portfolio.error)} /> : portfolio.data?.length ? <div className="overflow-x-auto"><table className="w-full min-w-[650px] text-right text-sm"><thead className="border-b text-xs text-slate-500"><tr><th className="pb-3 font-medium">الأداة</th><th className="pb-3 font-medium">الكمية</th><th className="pb-3 font-medium">متوسط التكلفة</th><th className="pb-3 font-medium">القيمة</th><th className="pb-3 font-medium">الحالة</th></tr></thead><tbody className="divide-y">{portfolio.data.map(position => <tr key={position.id}><td className="py-4"><p className="font-semibold">{position.instrumentName}</p><p className="mt-1 text-xs text-slate-500">{position.symbol || "بدون رمز"} · {position.currency}</p></td><td className="py-4">{position.quantity}</td><td className="py-4">{money(position.averageCost, position.costCurrency)}</td><td className="py-4 font-semibold">{position.marketValue ? money(position.marketValue, position.currency) : "—"}</td><td className="py-4"><Badge variant={position.quoteStatus === "unavailable" ? "outline" : "secondary"}>{position.quoteStatus === "unavailable" ? "يتطلب سعرًا" : position.quoteStatus}</Badge></td></tr>)}</tbody></table></div> : <EmptyState icon={TrendingUp} title="لا توجد حيازات منشورة" description="بعد تنفيذ أول صفقة شراء ستظهر الكمية ومتوسط التكلفة هنا. لا يتم إنشاء أي حيازة من مجرد واجهة." />}</CardContent></Card></div></div></DashboardLayout>;
}

export function TransfersPage() {
  const utils = trpc.useUtils();
  const accounts = trpc.family.accounts.list.useQuery();
  const [fromAccountId, setFromAccountId] = useState("");
  const [toAccountId, setToAccountId] = useState("");
  const [amount, setAmount] = useState("");
  const [memo, setMemo] = useState("");
  const from = accounts.data?.find(account => String(account.id) === fromAccountId);
  const to = accounts.data?.find(account => String(account.id) === toAccountId);
  const transfer = trpc.family.ledger.transfer.useMutation({
    onSuccess: () => { toast.success("تم نشر التحويل بقيد متوازن بين الحسابين."); void utils.family.dashboard.invalidate(); void utils.family.accounts.list.invalidate(); void utils.family.ledger.recent.invalidate(); setAmount(""); setMemo(""); },
    onError: error => toast.error(textError(error)),
  });
  const transferable = (accounts.data ?? []).filter(account => ["cash", "bank", "brokerage", "wallet"].includes(account.accountType) && account.status === "active");
  const submit = (event: React.FormEvent) => { event.preventDefault(); if (!from || !to) return toast.error("اختر حساب المصدر وحساب الوجهة."); transfer.mutate({ fromAccountId: from.id, toAccountId: to.id, amount, currency: from.currency, occurredAt: Date.now(), memo: memo || null, idempotencyKey: crypto.randomUUID() }); };
  return <DashboardLayout><div dir="rtl" className="mx-auto max-w-3xl space-y-6"><header className="border-b pb-6"><p className="text-xs font-semibold uppercase tracking-[.15em] text-emerald-700">FAMILY / Internal Transfer</p><h1 className="mt-2 text-3xl font-bold">تحويل بين الحسابات</h1><p className="mt-2 text-sm text-slate-500">ينقل التحويل القيمة بين حسابين ضمن نطاق FAMILY نفسه، ويمنع الخادم الرصيد السلبي والوصول خارج النطاق.</p></header><Card><CardHeader><CardTitle className="flex items-center gap-2"><ArrowLeftRight className="size-5 text-emerald-700" />تحويل آمن</CardTitle><CardDescription>تُقفل حسابات المصدر والوجهة بترتيب ثابت داخل معاملة قاعدة البيانات لتقليل تعارض التزامن.</CardDescription></CardHeader><CardContent>{accounts.isLoading ? <Skeleton className="h-64" /> : transferable.length >= 2 ? <form onSubmit={submit} className="grid gap-5"><div className="grid gap-4 sm:grid-cols-2"><div className="grid gap-2"><Label>من الحساب</Label><Select value={fromAccountId} onValueChange={setFromAccountId}><SelectTrigger><SelectValue placeholder="حساب المصدر" /></SelectTrigger><SelectContent>{transferable.map(account => <SelectItem key={account.id} value={String(account.id)}>{account.name} — {account.currency}</SelectItem>)}</SelectContent></Select></div><div className="grid gap-2"><Label>إلى الحساب</Label><Select value={toAccountId} onValueChange={setToAccountId}><SelectTrigger><SelectValue placeholder="حساب الوجهة" /></SelectTrigger><SelectContent>{transferable.filter(account => String(account.id) !== fromAccountId).map(account => <SelectItem key={account.id} value={String(account.id)}>{account.name} — {account.currency}</SelectItem>)}</SelectContent></Select></div></div><div className="grid gap-2"><Label htmlFor="transfer-amount">المبلغ {from ? `(${from.currency})` : ""}</Label><Input id="transfer-amount" value={amount} onChange={e => setAmount(e.target.value)} inputMode="decimal" required /></div><div className="grid gap-2"><Label htmlFor="transfer-memo">مذكرة</Label><Textarea id="transfer-memo" value={memo} onChange={e => setMemo(e.target.value)} maxLength={2000} placeholder="اختياري" /></div><Button type="submit" disabled={transfer.isPending}>{transfer.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}نشر التحويل</Button></form> : <EmptyState icon={ArrowLeftRight} title="أضف حسابين قابلين للتحويل" description="يلزم وجود حسابي نقد أو بنك أو وساطة نشطين على الأقل قبل إنشاء تحويل داخلي." action={<CreateAccountDialog />} />}</CardContent></Card></div></DashboardLayout>;
}

export function ValuationPage() {
  const utils = trpc.useUtils();
  const access = useFamilyPermissions();
  const summary = trpc.family.dashboard.useQuery();
  const [fromCurrency, setFromCurrency] = useState("");
  const [rate, setRate] = useState("");
  const record = trpc.family.fx.recordManual.useMutation({ onSuccess: () => { toast.success("تم حفظ سعر الصرف مع مصدره وتاريخه."); void utils.family.dashboard.invalidate(); void utils.family.accounts.list.invalidate(); void utils.family.portfolio.list.invalidate(); setRate(""); }, onError: error => toast.error(textError(error)) });
  const refreshYahoo = trpc.family.fx.refreshYahoo.useMutation({ onSuccess: data => { if (data.skipped) return toast.message(data.message); toast.success(`تم جلب سعر ${data.fromCurrency}/${data.toCurrency} من Yahoo Finance مع وقت المصدر.`); void utils.family.dashboard.invalidate(); void utils.family.accounts.list.invalidate(); void utils.family.portfolio.list.invalidate(); }, onError: error => toast.error(textError(error)) });
  const submit = (event: React.FormEvent) => { event.preventDefault(); const base = summary.data?.workspace.baseCurrency; if (!base) return; record.mutate({ fromCurrency, toCurrency: base, rate, asOf: Date.now() }); };
  const refresh = () => { if (!fromCurrency) return toast.error("أدخل عملة المصدر أولًا."); refreshYahoo.mutate({ fromCurrency }); };
  const gaps = summary.data ? [...summary.data.unvaluedCurrencies, ...summary.data.unvaluedInstruments, ...(summary.data.staleFxCurrencies ?? []).map(currency => `سعر صرف متقادم: ${currency}`)] : [];
  return <DashboardLayout><div dir="rtl" className="mx-auto max-w-5xl space-y-6"><header className="border-b pb-6"><p className="text-xs font-semibold uppercase tracking-[.15em] text-emerald-700">FAMILY / Valuation Control</p><h1 className="mt-2 text-3xl font-bold">التقييم والعملات</h1><p className="mt-2 text-sm text-slate-500">لا تظهر القيم الإجمالية إلا بسعر صرف أو سعر سوق موثق؛ لا يوجد تحويل تلقائي أو رقم مخفي.</p></header><div className="grid gap-6 lg:grid-cols-[.8fr_1.2fr]"><Card><CardHeader><CardTitle>سعر الصرف</CardTitle><CardDescription>يُجلب زوج العملات من Yahoo Finance ويُحفظ مع مصدره ووقته وحالة تأخره؛ يتوفر الإدخال اليدوي الموثق عند تعذر المصدر.</CardDescription></CardHeader><CardContent><form onSubmit={submit} className="grid gap-4"><div className="grid grid-cols-2 gap-3"><div className="grid gap-2"><Label htmlFor="fx-from">من العملة</Label><Input id="fx-from" value={fromCurrency} onChange={e => setFromCurrency(e.target.value.toUpperCase())} minLength={3} maxLength={3} placeholder="USD" required /></div><div className="grid gap-2"><Label>إلى العملة</Label><Input value={summary.data?.workspace.baseCurrency || "—"} disabled /></div></div><Button type="button" variant="outline" onClick={refresh} disabled={refreshYahoo.isPending || !access.canAdvise}>{refreshYahoo.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}تحديث من Yahoo Finance</Button><div className="grid gap-2"><Label htmlFor="fx-rate">سعر يدوي بديل</Label><Input id="fx-rate" value={rate} onChange={e => setRate(e.target.value)} inputMode="decimal" required /></div><Button type="submit" disabled={record.isPending || !access.canAdvise}>{record.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}{access.canAdvise ? "حفظ السعر اليدوي" : "تتطلب صلاحية مستشار"}</Button></form></CardContent></Card><Card><CardHeader><CardTitle>فجوات التقييم الحالية</CardTitle><CardDescription>إشارات محسوبة من الحسابات والحيازات المسجلة فقط.</CardDescription></CardHeader><CardContent>{summary.isLoading ? <Skeleton className="h-48" /> : summary.error ? <InlineError message={textError(summary.error)} /> : gaps.length ? <div className="space-y-3">{gaps.map(item => <div key={item} className="rounded-lg border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900">يلزم إدخال سعر موثق أو سعر صرف حديث للبيان: <strong>{item}</strong>.</div>)}</div> : <EmptyState icon={Landmark} title="لا توجد فجوات تقييم في البيانات الحالية" description="تتوفر الأسعار اللازمة للبيانات المسجلة. راجع تاريخ ومصدر كل سعر قبل اتخاذ قرار مالي." />}</CardContent></Card></div></div></DashboardLayout>;
}

const goalTypeLabel: Record<string, string> = { emergency_fund: "صندوق طوارئ", retirement: "تقاعد", education: "تعليم", legacy: "إرث", custom: "هدف مخصص" };
const goalMetricLabel: Record<string, string> = { net_worth: "صافي الثروة", liquid_assets: "السيولة", investments: "الاستثمارات" };

export function GoalsPage() {
  const utils = trpc.useUtils();
  const goals = trpc.family.goals.list.useQuery();
  const [name, setName] = useState("");
  const [goalType, setGoalType] = useState<"emergency_fund" | "retirement" | "education" | "legacy" | "custom">("emergency_fund");
  const [metric, setMetric] = useState<"net_worth" | "liquid_assets" | "investments">("liquid_assets");
  const [targetAmount, setTargetAmount] = useState("");
  const [targetDate, setTargetDate] = useState("");
  const create = trpc.family.goals.create.useMutation({
    onSuccess: () => { toast.success("تم إنشاء الهدف. تقدمه يُحسب مباشرة من قيودك وحيازاتك المقيمة."); void utils.family.goals.list.invalidate(); setName(""); setTargetAmount(""); setTargetDate(""); },
    onError: error => toast.error(textError(error)),
  });
  const submit = (event: React.FormEvent) => { event.preventDefault(); create.mutate({ name, goalType, metric, targetAmount, targetDate: targetDate ? Date.parse(`${targetDate}T00:00:00.000Z`) : null }); };
  return <DashboardLayout><div dir="rtl" className="mx-auto max-w-6xl space-y-6"><header className="border-b pb-6"><p className="text-xs font-semibold uppercase tracking-[.15em] text-emerald-700">FAMILY / Financial Planning</p><h1 className="mt-2 text-3xl font-bold">الأهداف والتخطيط</h1><p className="mt-2 text-sm text-slate-500">لا يحتوي الهدف على قيمة تقدم قابلة للتعديل. يتتبع تلقائيًا مقياسًا ماليًا من الدفتر والحيازات ذات الأسعار الموثقة.</p></header><div className="grid gap-6 lg:grid-cols-[.75fr_1.25fr]"><Card><CardHeader><CardTitle className="flex items-center gap-2"><Target className="size-5 text-emerald-700" />إنشاء هدف</CardTitle><CardDescription>الأهداف في الإصدار الحالي مقومة بعملة مساحة FAMILY الأساسية.</CardDescription></CardHeader><CardContent><form onSubmit={submit} className="grid gap-4"><div className="grid gap-2"><Label htmlFor="goal-name">اسم الهدف</Label><Input id="goal-name" value={name} onChange={e => setName(e.target.value)} placeholder="مثال: احتياطي طوارئ" required minLength={2} /></div><div className="grid gap-2"><Label>نوع الهدف</Label><Select value={goalType} onValueChange={value => setGoalType(value as typeof goalType)}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{Object.entries(goalTypeLabel).map(([value, label]) => <SelectItem key={value} value={value}>{label}</SelectItem>)}</SelectContent></Select></div><div className="grid gap-2"><Label>ما الذي يقيسه الهدف؟</Label><Select value={metric} onValueChange={value => setMetric(value as typeof metric)}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{Object.entries(goalMetricLabel).map(([value, label]) => <SelectItem key={value} value={value}>{label}</SelectItem>)}</SelectContent></Select></div><div className="grid gap-2"><Label htmlFor="goal-target">القيمة المستهدفة</Label><Input id="goal-target" value={targetAmount} onChange={e => setTargetAmount(e.target.value)} inputMode="decimal" required /></div><div className="grid gap-2"><Label htmlFor="goal-date">تاريخ مستهدف</Label><Input id="goal-date" type="date" value={targetDate} onChange={e => setTargetDate(e.target.value)} /></div><Button type="submit" disabled={create.isPending}>{create.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}حفظ الهدف</Button></form></CardContent></Card><Card><CardHeader><CardTitle>التقدم الفعلي</CardTitle><CardDescription>يُحدّث عند تغيير المقياس المالي المرتبط، لا عند تعديل بطاقة الواجهة.</CardDescription></CardHeader><CardContent>{goals.isLoading ? <Skeleton className="h-80" /> : goals.error ? <InlineError message={textError(goals.error)} /> : goals.data?.length ? <div className="space-y-5">{goals.data.map(goal => <div key={goal.id} className="rounded-xl border bg-white p-4"><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-start"><div><p className="font-semibold text-slate-900">{goal.name}</p><p className="mt-1 text-xs text-slate-500">{goalTypeLabel[goal.goalType]} · يتتبع {goalMetricLabel[goal.metric]}{goal.targetDate ? ` · مستهدف ${new Intl.DateTimeFormat("ar-EG", { dateStyle: "medium" }).format(new Date(goal.targetDate))}` : ""}</p></div><Badge variant={goal.progressPercent >= 100 ? "secondary" : "outline"}>{goal.progressPercent}%</Badge></div><div className="mt-4 h-2.5 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-emerald-600 transition-[width] duration-300" style={{ width: `${goal.progressPercent}%` }} /></div><div className="mt-3 flex items-center justify-between text-sm"><span className="font-medium text-emerald-700">{money(goal.currentAmount, goal.currency)}</span><span className="text-slate-500">من {money(goal.targetAmount, goal.currency)}</span></div></div>)}</div> : <EmptyState icon={Target} title="لم تحدد أهدافًا بعد" description="أنشئ هدفًا مقاسًا بالسيولة أو الاستثمارات أو صافي الثروة. ستتغير نسبة التقدم مع تغير البيانات المالية الحقيقية." />}</CardContent></Card></div></div></DashboardLayout>;
}

const goalFundingSourceLabel: Record<string, string> = { cash_flow: "التدفق النقدي", savings: "المدخرات", investments: "الاستثمارات", mixed: "مختلط", other: "مصدر آخر" };

const assetClassLabel: Record<string, string> = { cash: "سيولة", equity: "أسهم وصناديق", fixed_income: "دخل ثابت", alternatives: "بدائل", other: "أخرى" };
const riskLevelLabel: Record<string, string> = { conservative: "محافظ", moderate: "متوازن", growth: "نمو", aggressive: "مرتفع المخاطر" };
type AllocationDraft = { assetClass: "cash" | "equity" | "fixed_income" | "alternatives" | "other"; targetPercent: string; driftThresholdPercent: string };
const allocationClasses: AllocationDraft["assetClass"][] = ["cash", "equity", "fixed_income", "alternatives", "other"];
const emptyAllocationDraft = (): AllocationDraft[] => allocationClasses.map(assetClass => ({ assetClass, targetPercent: "", driftThresholdPercent: "" }));

export function RiskAllocationPage() {
  const utils = trpc.useUtils(); const access = useFamilyPermissions(); const summary = trpc.family.risk.summary.useQuery();
  const [riskLevel, setRiskLevel] = useState<"conservative" | "moderate" | "growth" | "aggressive">("moderate"); const [score, setScore] = useState(""); const [rationale, setRationale] = useState(""); const [targets, setTargets] = useState<AllocationDraft[]>(emptyAllocationDraft);
  useEffect(() => { const profile = summary.data?.riskProfile; if (profile) { setRiskLevel(profile.riskLevel); setScore(profile.questionnaireScore === null ? "" : String(profile.questionnaireScore)); setRationale(profile.rationale || ""); } const rows = summary.data?.allocation.classes; if (rows) setTargets(rows.map(row => ({ assetClass: row.assetClass as AllocationDraft["assetClass"], targetPercent: row.targetPercent ?? "", driftThresholdPercent: row.driftThresholdPercent ?? "" }))); }, [summary.data?.riskProfile?.id, summary.data?.allocation.totalValuedBase]);
  const saveProfile = trpc.family.risk.upsertProfile.useMutation({ onSuccess: () => { toast.success("تم حفظ ملف المخاطر داخل مساحة FAMILY الحالية."); void utils.family.risk.summary.invalidate(); }, onError: error => toast.error(textError(error)) });
  const saveTargets = trpc.family.risk.upsertAllocationTargets.useMutation({ onSuccess: () => { toast.success("تم حفظ التخصيص المستهدف. سيعاد حساب الانحراف من القيم المقيمة."); void utils.family.risk.summary.invalidate(); }, onError: error => toast.error(textError(error)) });
  const updateTarget = (assetClass: AllocationDraft["assetClass"], field: "targetPercent" | "driftThresholdPercent", value: string) => setTargets(current => current.map(target => target.assetClass === assetClass ? { ...target, [field]: value } : target));
  const saveRisk = (event: React.FormEvent) => { event.preventDefault(); saveProfile.mutate({ riskLevel, questionnaireScore: score ? Number(score) : null, rationale: rationale || null }); };
  const saveAllocation = (event: React.FormEvent) => { event.preventDefault(); saveTargets.mutate({ targets }); };
  return <DashboardLayout><div dir="rtl" className="mx-auto max-w-7xl space-y-6"><header className="border-b pb-6"><p className="text-xs font-semibold uppercase tracking-[.15em] text-emerald-700">FAMILY / Risk & allocation</p><h1 className="mt-2 text-3xl font-bold">ملف المخاطر والتخصيص</h1><p className="mt-2 max-w-3xl text-sm text-slate-500">التوزيع الفعلي يجمع السيولة والحيازات ذات الأسعار وسعر الصرف الموثقين فقط. «راجع الانحراف» تنبيه تحليلي لمراجعة الهدف، ولا ينفذ صفقة أو يقدم توصية شراء وبيع.</p></header>{summary.isLoading ? <PageLoading /> : summary.error ? <InlineError message={textError(summary.error)} /> : <><section className="grid gap-6 xl:grid-cols-[.8fr_1.2fr]"><Card><CardHeader><CardTitle>ملف المخاطر</CardTitle><CardDescription>هذا ملف معلن من المستخدم؛ لا تستنتج المنصة تحمل المخاطر من سجل المعاملات وحده.</CardDescription></CardHeader><CardContent><form onSubmit={saveRisk} className="grid gap-4"><div className="grid gap-2"><Label>المستوى المعلن</Label><Select value={riskLevel} onValueChange={value => setRiskLevel(value as typeof riskLevel)} disabled={!access.canEdit}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{Object.entries(riskLevelLabel).map(([value, label]) => <SelectItem key={value} value={value}>{label}</SelectItem>)}</SelectContent></Select></div><div className="grid gap-2"><Label htmlFor="risk-score">درجة استبيان موثقة (اختيارية، 0–100)</Label><Input id="risk-score" type="number" min="0" max="100" value={score} onChange={event => setScore(event.target.value)} disabled={!access.canEdit} /></div><div className="grid gap-2"><Label htmlFor="risk-rationale">سياق أو مبرر</Label><Textarea id="risk-rationale" maxLength={2000} value={rationale} onChange={event => setRationale(event.target.value)} disabled={!access.canEdit} placeholder="مثال: مدة الاستثمار والسيولة المطلوبة..." /></div><Button type="submit" disabled={saveProfile.isPending || !access.canEdit}>{saveProfile.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}{access.canEdit ? "حفظ ملف المخاطر" : "تتطلب صلاحية محرر"}</Button></form></CardContent></Card><Card><CardHeader><CardTitle>التخصيص الفعلي</CardTitle><CardDescription>إجمالي مقيم: {money(summary.data?.allocation.totalValuedBase, summary.data?.baseCurrency || "EGP")}</CardDescription></CardHeader><CardContent>{summary.data?.allocation.classes.some(row => row.actualAmount !== "0.000000") ? <div className="space-y-3">{summary.data.allocation.classes.map(row => <div key={row.assetClass} className="rounded-xl border bg-white p-4"><div className="flex flex-wrap items-center justify-between gap-2"><p className="font-semibold">{assetClassLabel[row.assetClass]}</p><Badge variant={row.status === "review" ? "destructive" : row.status === "within_band" ? "secondary" : "outline"}>{row.status === "review" ? "راجع الانحراف" : row.status === "within_band" ? "ضمن الحد" : "لا يوجد هدف"}</Badge></div><div className="mt-3 grid gap-2 text-sm sm:grid-cols-4"><span>القيمة: {money(row.actualAmount, summary.data.baseCurrency)}</span><span>الفعلي: {row.actualPercent}%</span><span>المستهدف: {row.targetPercent === null ? "—" : `${row.targetPercent}%`}</span><span>الانحراف: {row.driftPercent === null ? "—" : `${row.driftPercent}%`}</span></div></div>)}</div> : <EmptyState icon={TrendingUp} title="لا توجد قيم مقيمة للتخصيص" description="سجل حسابًا أو حيازة وسعرًا وFX موثقًا، ثم سيُحسب التوزيع الفعلي من البيانات المتاحة فقط." />}{summary.data?.unvaluedCurrencies.length || summary.data?.unvaluedInstruments.length ? <div className="mt-4 rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900">مستبعد من التوزيع إلى حين التقييم: {[...summary.data.unvaluedCurrencies, ...summary.data.unvaluedInstruments].join("، ")}.</div> : null}</CardContent></Card></section><Card><CardHeader><CardTitle>التخصيص المستهدف وحدود الانحراف</CardTitle><CardDescription>يجب أن يبلغ مجموع النسب 100% بالضبط. لا تُنشئ هذه الإعدادات أوامر إعادة توازن؛ هي معيار مراجعة شفاف للتوزيع الفعلي.</CardDescription></CardHeader><CardContent><form onSubmit={saveAllocation} className="space-y-4"><div className="overflow-x-auto"><table className="w-full min-w-[620px] text-right text-sm"><thead className="bg-slate-50 text-xs text-slate-500"><tr><th className="p-3">فئة الأصول</th><th className="p-3">هدف %</th><th className="p-3">حد الانحراف %</th></tr></thead><tbody>{targets.map(target => <tr key={target.assetClass} className="border-t"><td className="p-3 font-medium">{assetClassLabel[target.assetClass]}</td><td className="p-3"><Input value={target.targetPercent} onChange={event => updateTarget(target.assetClass, "targetPercent", event.target.value)} disabled={!access.canEdit} inputMode="decimal" required /></td><td className="p-3"><Input value={target.driftThresholdPercent} onChange={event => updateTarget(target.assetClass, "driftThresholdPercent", event.target.value)} disabled={!access.canEdit} inputMode="decimal" required /></td></tr>)}</tbody></table></div><Button type="submit" disabled={saveTargets.isPending || !access.canEdit}>{saveTargets.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}{access.canEdit ? "حفظ معايير التخصيص" : "تتطلب صلاحية محرر"}</Button></form></CardContent></Card></>}</div></DashboardLayout>;
}

export function ResearchPage() {
  const utils = trpc.useUtils(); const access = useFamilyPermissions(); const instruments = trpc.family.instruments.list.useQuery(); const watchlist = trpc.family.research.watchlist.useQuery(); const notes = trpc.family.research.notes.useQuery();
  const [watchInstrumentId, setWatchInstrumentId] = useState(""); const [watchNote, setWatchNote] = useState(""); const [noteInstrumentId, setNoteInstrumentId] = useState(""); const [title, setTitle] = useState(""); const [thesis, setThesis] = useState(""); const [risks, setRisks] = useState(""); const [sourceUrl, setSourceUrl] = useState(""); const [noteStatus, setNoteStatus] = useState<"draft" | "active">("draft");
  const addWatchlist = trpc.family.research.addWatchlistItem.useMutation({ onSuccess: () => { toast.success("تم حفظ عنصر المراقبة. لا ينفذ FAMILY أي صفقة من هذه الشاشة."); setWatchInstrumentId(""); setWatchNote(""); void utils.family.research.watchlist.invalidate(); }, onError: error => toast.error(textError(error)) });
  const archiveWatchlist = trpc.family.research.archiveWatchlistItem.useMutation({ onSuccess: () => { toast.success("تمت أرشفة عنصر المراقبة."); void utils.family.research.watchlist.invalidate(); }, onError: error => toast.error(textError(error)) });
  const addNote = trpc.family.research.createNote.useMutation({ onSuccess: () => { toast.success("تم حفظ مذكرة البحث داخل نطاقك المالي."); setTitle(""); setThesis(""); setRisks(""); setSourceUrl(""); setNoteInstrumentId(""); void utils.family.research.notes.invalidate(); }, onError: error => toast.error(textError(error)) });
  const saveWatch = (event: React.FormEvent) => { event.preventDefault(); if (!watchInstrumentId) return toast.error("اختر أداة قبل إضافتها للمراقبة."); addWatchlist.mutate({ instrumentId: Number(watchInstrumentId), note: watchNote || null }); };
  const saveNote = (event: React.FormEvent) => { event.preventDefault(); addNote.mutate({ instrumentId: noteInstrumentId ? Number(noteInstrumentId) : null, title, thesis, risks: risks || null, sourceUrl: sourceUrl || null, status: noteStatus }); };
  return <DashboardLayout><div dir="rtl" className="mx-auto max-w-7xl space-y-6"><header className="border-b pb-6"><p className="text-xs font-semibold uppercase tracking-[.15em] text-emerald-700">FAMILY / Research only</p><h1 className="mt-2 text-3xl font-bold">قائمة المراقبة والبحث</h1><p className="mt-2 max-w-3xl text-sm text-slate-500">هذه مساحة تحليلية منفصلة عن التنفيذ. تُعرض الأسعار المسجلة فقط مع مصدرها وحالتها ووقت تحديثها؛ عدم وجود سعر لا يُستبدل بتقدير ولا يظهر زر شراء أو بيع هنا.</p></header><section className="grid gap-6 xl:grid-cols-[.85fr_1.15fr]"><Card><CardHeader><CardTitle>إضافة إلى قائمة المراقبة</CardTitle><CardDescription>يمكنك حفظ ملاحظة داخلية. القائمة لا تُنشئ أي عملية استثمارية.</CardDescription></CardHeader><CardContent>{instruments.isLoading ? <Skeleton className="h-40" /> : <form onSubmit={saveWatch} className="grid gap-4"><div className="grid gap-2"><Label>الأداة</Label><Select value={watchInstrumentId || undefined} onValueChange={setWatchInstrumentId} disabled={!access.canEdit}><SelectTrigger><SelectValue placeholder="اختر أداة مسجلة" /></SelectTrigger><SelectContent>{instruments.data?.map(instrument => <SelectItem key={instrument.id} value={String(instrument.id)}>{instrument.symbol ? `${instrument.symbol} — ` : ""}{instrument.name} · {instrument.currency}</SelectItem>)}</SelectContent></Select></div><div className="grid gap-2"><Label htmlFor="watch-note">ملاحظة</Label><Textarea id="watch-note" value={watchNote} onChange={event => setWatchNote(event.target.value)} disabled={!access.canEdit} maxLength={2000} placeholder="اختياري" /></div><Button type="submit" disabled={addWatchlist.isPending || !access.canEdit}>{addWatchlist.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}{access.canEdit ? "إضافة للمراقبة" : "تتطلب صلاحية محرر"}</Button></form>}</CardContent></Card><Card><CardHeader><CardTitle>الأدوات المراقبة</CardTitle><CardDescription>حالة السعر أدناه تعكس ما سُجّل في FAMILY، وليست تغذية سوق مباشرة.</CardDescription></CardHeader><CardContent>{watchlist.isLoading ? <Skeleton className="h-64" /> : watchlist.error ? <InlineError message={textError(watchlist.error)} /> : watchlist.data?.length ? <div className="space-y-3">{watchlist.data.map(item => <div key={item.id} className="rounded-xl border bg-white p-4"><div className="flex flex-col justify-between gap-3 sm:flex-row"><div><div className="flex flex-wrap items-center gap-2"><p className="font-semibold text-slate-900">{item.symbol ? `${item.symbol} — ` : ""}{item.name}</p><Badge variant={item.status === "active" ? "secondary" : "outline"}>{item.status === "active" ? "مراقبة" : "مؤرشف"}</Badge></div><p className="mt-2 text-sm text-slate-600">{item.latestQuote ? `${money(item.latestQuote.price, item.latestQuote.currency)} · ${item.latestQuote.quoteStatus === "manual" ? "سعر يدوي" : item.latestQuote.quoteStatus} · ${dateTime(item.latestQuote.asOf)}` : "لا يوجد سعر مسجل لهذه الأداة."}</p>{item.note ? <p className="mt-2 text-xs text-slate-500">{item.note}</p> : null}</div>{item.status === "active" && access.canEdit ? <Button type="button" variant="outline" size="sm" disabled={archiveWatchlist.isPending} onClick={() => archiveWatchlist.mutate({ itemId: item.id })}>أرشفة</Button> : null}</div></div>)}</div> : <EmptyState icon={BriefcaseBusiness} title="لا توجد أدوات مراقبة" description="أضف أداة مسجلة إلى القائمة، ثم أضف مذكرة بحث أو سجل سعرًا يدويًا من صفحة التقييم عند توفر مصدر موثوق." />}</CardContent></Card></section><section className="grid gap-6 xl:grid-cols-[.85fr_1.15fr]"><Card><CardHeader><CardTitle>مذكرة بحث</CardTitle><CardDescription>دوّن الفرضية والمخاطر والمصدر. المذكرة لا تُترجم تلقائيًا إلى توصية أو أمر.</CardDescription></CardHeader><CardContent><form onSubmit={saveNote} className="grid gap-4"><div className="grid gap-2"><Label>أداة مرتبطة</Label><Select value={noteInstrumentId || undefined} onValueChange={setNoteInstrumentId} disabled={!access.canEdit}><SelectTrigger><SelectValue placeholder="اختياري" /></SelectTrigger><SelectContent>{instruments.data?.map(instrument => <SelectItem key={instrument.id} value={String(instrument.id)}>{instrument.symbol ? `${instrument.symbol} — ` : ""}{instrument.name}</SelectItem>)}</SelectContent></Select></div><div className="grid gap-2"><Label htmlFor="research-title">العنوان</Label><Input id="research-title" value={title} onChange={event => setTitle(event.target.value)} disabled={!access.canEdit} required minLength={2} /></div><div className="grid gap-2"><Label htmlFor="research-thesis">الفرضية أو الملاحظات</Label><Textarea id="research-thesis" value={thesis} onChange={event => setThesis(event.target.value)} disabled={!access.canEdit} required minLength={2} maxLength={12000} className="min-h-28" /></div><div className="grid gap-2"><Label htmlFor="research-risks">المخاطر والقيود</Label><Textarea id="research-risks" value={risks} onChange={event => setRisks(event.target.value)} disabled={!access.canEdit} maxLength={12000} /></div><div className="grid gap-2"><Label htmlFor="research-source">رابط المصدر</Label><Input id="research-source" type="url" value={sourceUrl} onChange={event => setSourceUrl(event.target.value)} disabled={!access.canEdit} placeholder="https://... (اختياري)" /></div><div className="grid gap-2"><Label>الحالة</Label><Select value={noteStatus} onValueChange={value => setNoteStatus(value as typeof noteStatus)} disabled={!access.canEdit}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="draft">مسودة</SelectItem><SelectItem value="active">مراجعة نشطة</SelectItem></SelectContent></Select></div><Button type="submit" disabled={addNote.isPending || !access.canEdit}>{addNote.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}{access.canEdit ? "حفظ مذكرة البحث" : "تتطلب صلاحية محرر"}</Button></form></CardContent></Card><Card><CardHeader><CardTitle>مذكراتك</CardTitle><CardDescription>المصادر والقيود محفوظة مع كل مذكرة حتى يسهل مراجعة أساس التحليل لاحقًا.</CardDescription></CardHeader><CardContent>{notes.isLoading ? <Skeleton className="h-64" /> : notes.error ? <InlineError message={textError(notes.error)} /> : notes.data?.length ? <div className="space-y-4">{notes.data.map(note => <article key={note.id} className="rounded-xl border bg-white p-4"><div className="flex flex-wrap items-center gap-2"><p className="font-semibold text-slate-900">{note.title}</p><Badge variant={note.status === "active" ? "secondary" : "outline"}>{note.status === "active" ? "نشطة" : "مسودة"}</Badge>{note.instrumentName ? <span className="text-xs text-slate-500">{note.symbol ? `${note.symbol} — ` : ""}{note.instrumentName}</span> : null}</div><p className="mt-3 whitespace-pre-wrap text-sm leading-6 text-slate-700">{note.thesis}</p>{note.risks ? <p className="mt-3 whitespace-pre-wrap rounded-lg bg-amber-50 p-3 text-sm leading-6 text-amber-900">المخاطر: {note.risks}</p> : null}<div className="mt-3 flex flex-wrap items-center gap-3 text-xs text-slate-500"><span>آخر تحديث: {dateTime(note.updatedAt)}</span>{note.sourceUrl ? <a className="font-medium text-emerald-700 underline underline-offset-4" href={note.sourceUrl} target="_blank" rel="noreferrer">فتح المصدر</a> : <span>لم يُسجل رابط مصدر</span>}</div></article>)}</div> : <EmptyState icon={BookOpenCheck} title="لا توجد مذكرات بحث" description="أنشئ مذكرة مرتبطة بأداة أو بمفهوم مالي، واحتفظ بالمصدر والمخاطر بدل تحويلها إلى قرار تلقائي." />}</CardContent></Card></section></div></DashboardLayout>;
}

export function RebalanceReviewPage() {
  const summary = trpc.family.risk.summary.useQuery();
  if (summary.isLoading) return <DashboardLayout><PageLoading /></DashboardLayout>;
  if (summary.error) return <DashboardLayout><InlineError message={textError(summary.error)} /></DashboardLayout>;
  const data = summary.data;
  if (!data) return <DashboardLayout><PageLoading /></DashboardLayout>;
  return <DashboardLayout><div dir="rtl" className="mx-auto max-w-6xl space-y-6"><header className="border-b pb-6"><p className="text-xs font-semibold uppercase tracking-[.15em] text-emerald-700">FAMILY / Allocation review</p><h1 className="mt-2 text-3xl font-bold">مراجعة إعادة التوازن</h1><p className="mt-2 max-w-3xl text-sm text-slate-500">تقارن هذه الشاشة التوزيع الفعلي المقيم بالهدف المحفوظ. لا تنشئ صفقة أو تحدد أداة أو توقيتًا للتداول؛ تعرض فقط مبلغ التقارب الحسابي المطلوب داخل كل فئة.</p></header>{data.allocation.totalValuedBase === "0.000000" ? <EmptyState icon={TrendingUp} title="لا توجد قيم مقيمة للمراجعة" description="أدخل حسابات أو حيازات مع أسعار وسعر صرف موثقين قبل حساب فجوة إعادة التوازن." /> : <Card><CardHeader><CardTitle>الانحراف ومبلغ التقارب</CardTitle><CardDescription>إجمالي الأصول المقيمة: {money(data.allocation.totalValuedBase, data.baseCurrency)}. كل مبلغ أدناه هو الفرق بين القيمة الفعلية وقيمة الهدف للحافظة ذاتها.</CardDescription></CardHeader><CardContent><div className="space-y-3">{data.allocation.classes.map(row => <div key={row.assetClass} className={`rounded-xl border p-4 ${row.status === "review" ? "border-amber-200 bg-amber-50" : "bg-white"}`}><div className="flex flex-col justify-between gap-3 sm:flex-row"><div><div className="flex flex-wrap items-center gap-2"><p className="font-semibold">{assetClassLabel[row.assetClass]}</p><Badge variant={row.status === "review" ? "destructive" : row.status === "within_band" ? "secondary" : "outline"}>{row.status === "review" ? "تتطلب مراجعة" : row.status === "within_band" ? "ضمن الحد" : "لا يوجد هدف"}</Badge></div><p className="mt-2 text-sm text-slate-600">فعلي {row.actualPercent}% مقابل مستهدف {row.targetPercent === null ? "—" : `${row.targetPercent}%`} · الانحراف {row.driftPercent === null ? "—" : `${row.driftPercent}%`}</p></div>{row.adjustmentToTarget === null ? <span className="text-sm text-slate-500">احفظ هدفًا لهذه الفئة.</span> : <div className="text-right"><p className="text-xs text-slate-500">مبلغ التقارب إلى الهدف</p><p className="mt-1 font-semibold text-slate-900">{Number(row.adjustmentToTarget) === 0 ? "لا فرق" : `${Number(row.adjustmentToTarget) > 0 ? "زيادة" : "خفض"} ${money(Math.abs(Number(row.adjustmentToTarget)).toFixed(6), data.baseCurrency)}`}</p></div>}</div><p className="mt-3 text-xs leading-5 text-slate-500">هذا مقياس توزيع حتمي، وليس توصية شخصية أو أمر تداول. راجع السيولة والضرائب والرسوم والقيود قبل أي قرار خارجي.</p></div>)}</div></CardContent></Card>}</div></DashboardLayout>;
}

export function WatchlistPricingPage() {
  const utils = trpc.useUtils();
  const [, setLocation] = useLocation();
  const access = useFamilyPermissions();
  const watchlist = trpc.family.research.watchlist.useQuery();
  const signals = trpc.family.planning.marketSignals.useQuery();
  const marketEmail = trpc.family.marketEmail.preference.useQuery();
  const [refreshingInstrumentId, setRefreshingInstrumentId] = useState<number | null>(null);
  const refreshYahoo = trpc.family.prices.refreshYahoo.useMutation({
    onSuccess: () => {
      toast.success("تم حفظ سعر Yahoo Finance مع مصدره ووقته وحالة تأخره.");
      void utils.family.research.watchlist.invalidate();
      void utils.family.planning.marketSignals.invalidate();
      void utils.family.portfolio.list.invalidate();
      void utils.family.dashboard.invalidate();
      setRefreshingInstrumentId(null);
    },
    onError: error => { toast.error(textError(error)); setRefreshingInstrumentId(null); },
  });
  const setMarketEmail = trpc.family.marketEmail.setPreference.useMutation({
    onSuccess: data => { toast.success(data.enabled ? "فُعّلت تنبيهات المراجعة البريدية للمالك. لن تتضمن أسعارًا أو أرصدة أو توصيات." : "أُوقفت تنبيهات المراجعة البريدية."); void utils.family.marketEmail.preference.invalidate(); },
    onError: error => toast.error(textError(error)),
  });
  const refresh = (instrumentId: number) => { setRefreshingInstrumentId(instrumentId); refreshYahoo.mutate({ instrumentId }); };
  return <DashboardLayout><div dir="rtl" className="mx-auto max-w-6xl space-y-6"><header className="border-b pb-6"><p className="text-xs font-semibold uppercase tracking-[.15em] text-emerald-700">FAMILY / Recorded prices</p><h1 className="mt-2 text-3xl font-bold">بيانات عناصر المراقبة</h1><p className="mt-2 max-w-3xl text-sm text-slate-500">تُحفظ أسعار Yahoo Finance مع مصدرها ووقتها وحالة تأخرها. لا تنشئ هذه الشاشة أي صفقة أو إعادة تقييم تلقائية.</p></header>{marketEmail.data?.canManage ? <Card className="border-sky-200 bg-sky-50/60"><CardHeader><CardTitle className="text-base">تنبيهات بريدية اختيارية</CardTitle><CardDescription>ترسل للمالك فقط عند ظهور إشارة مراجعة جديدة، ولا تحتوي على أسعار أو أرصدة أو توصيات ولا تنفذ عملية مالية.</CardDescription></CardHeader><CardContent className="flex flex-wrap items-center justify-between gap-3"><p className="text-sm text-slate-700">{marketEmail.data.enabled ? "التنبيهات مفعلة." : "التنبيهات غير مفعلة."}{!marketEmail.data.configured ? ` ${marketEmail.data.reason}` : ""}</p><Button type="button" variant={marketEmail.data.enabled ? "outline" : "default"} disabled={setMarketEmail.isPending} onClick={() => setMarketEmail.mutate({ enabled: !marketEmail.data.enabled, origin: window.location.origin })}>{setMarketEmail.isPending ? "جارٍ الحفظ…" : marketEmail.data.enabled ? "إيقاف التنبيهات" : "تفعيل التنبيهات"}</Button></CardContent></Card> : null}{signals.data?.length ? <Card className="border-amber-200 bg-amber-50"><CardHeader><CardTitle className="text-base">إشارات مراجعة السوق</CardTitle><CardDescription>إشارات تحليلية من متوسط التكلفة والسعر المسجل؛ لا تمثل أمرًا أو توصية شخصية.</CardDescription></CardHeader><CardContent className="grid gap-3 md:grid-cols-2">{signals.data.map(signal => <div key={signal.id} className="rounded-lg border border-amber-200 bg-white p-3 text-sm"><div className="flex items-center justify-between gap-3"><p className="font-semibold">{signal.title}</p><Badge variant="outline">{signal.percentFromCost === null ? "تحقق من السعر" : `${signal.percentFromCost}%`}</Badge></div><p className="mt-1 text-slate-600">{signal.detail}</p>{signal.kind !== "stale_quote" ? <Button type="button" variant="outline" size="sm" className="mt-2" onClick={() => { sessionStorage.setItem("family-trade-draft", JSON.stringify({ instrumentId: signal.instrumentId, side: signal.kind === "profit_review" ? "sell" : "buy" })); setLocation("/trades"); }}>فتح مسودة للمراجعة</Button> : null}</div>)}</CardContent></Card> : null}{watchlist.isLoading ? <PageLoading /> : watchlist.error ? <InlineError message={textError(watchlist.error)} /> : watchlist.data?.length ? <Card><CardContent className="p-0"><div className="overflow-x-auto"><table className="w-full min-w-[820px] text-right text-sm"><thead className="bg-slate-50 text-xs text-slate-500"><tr><th className="p-4">الأداة</th><th className="p-4">آخر سعر</th><th className="p-4">الحالة</th><th className="p-4">المصدر</th><th className="p-4">وقت السعر</th><th className="p-4">إجراء</th></tr></thead><tbody className="divide-y">{watchlist.data.map(item => { const canRefresh = ["equity", "fund", "gold"].includes(item.assetType) && Boolean(item.symbol); return <tr key={item.id}><td className="p-4 font-medium text-slate-900">{item.symbol ? `${item.symbol} — ` : ""}{item.name}</td>{item.latestQuote ? <><td className="p-4 font-semibold">{money(item.latestQuote.price, item.latestQuote.currency)}</td><td className="p-4"><Badge variant="outline">{item.latestQuote.quoteStatus}</Badge></td><td className="p-4 text-slate-600">{item.latestQuote.source}</td><td className="p-4 text-slate-600">{dateTime(item.latestQuote.asOf)}</td></> : <><td className="p-4 text-slate-500">—</td><td className="p-4 text-amber-700">لا يوجد سعر</td><td className="p-4 text-slate-500">—</td><td className="p-4 text-slate-500">—</td></>}<td className="p-4">{canRefresh ? <Button type="button" variant="outline" size="sm" disabled={!access.canAdvise || refreshYahoo.isPending} onClick={() => refresh(item.instrumentId)}>{refreshingInstrumentId === item.instrumentId ? <Loader2 className="size-4 animate-spin" /> : "تحديث Yahoo"}</Button> : <span className="text-xs text-slate-500">أضف رمزًا أولًا</span>}</td></tr>; })}</tbody></table></div></CardContent></Card> : <EmptyState icon={BriefcaseBusiness} title="لا توجد عناصر مراقبة" description="أضف عنصرًا من شاشة المراقبة والبحث ثم حدّد رمز Yahoo مثل GC=F للذهب قبل التحديث." />}</div></DashboardLayout>;
}

export function FeeTaxRulesPage() {
  const utils = trpc.useUtils(); const access = useFamilyPermissions(); const rules = trpc.family.feeTax.list.useQuery();
  const [name, setName] = useState(""); const [chargeType, setChargeType] = useState<"fee" | "tax">("fee"); const [appliesTo, setAppliesTo] = useState<"buy" | "sell" | "both">("both"); const [calculationMethod, setCalculationMethod] = useState<"flat" | "percentage">("percentage"); const [value, setValue] = useState(""); const [currency, setCurrency] = useState("EGP"); const [note, setNote] = useState("");
  const create = trpc.family.feeTax.create.useMutation({ onSuccess: () => { toast.success("تم حفظ القاعدة. لا تؤثر في العمليات السابقة."); setName(""); setValue(""); setNote(""); void utils.family.feeTax.list.invalidate(); }, onError: error => toast.error(textError(error)) });
  const archive = trpc.family.feeTax.archive.useMutation({ onSuccess: () => { toast.success("تمت أرشفة القاعدة؛ لا تُحذف من سجل التدقيق."); void utils.family.feeTax.list.invalidate(); }, onError: error => toast.error(textError(error)) });
  const submit = (event: React.FormEvent) => { event.preventDefault(); create.mutate({ name, chargeType, appliesTo, calculationMethod, value, currency: calculationMethod === "flat" ? currency : null, jurisdictionNote: note || null }); };
  return <DashboardLayout><div dir="rtl" className="mx-auto max-w-7xl space-y-6"><header className="border-b pb-6"><p className="text-xs font-semibold uppercase tracking-[.15em] text-emerald-700">FAMILY / Explicit charges</p><h1 className="mt-2 text-3xl font-bold">قواعد الرسوم والضرائب</h1><p className="mt-2 max-w-3xl text-sm text-slate-500">احفظ رسوم وسيط أو ضريبة معلنة كمبلغ ثابت أو نسبة للصفقات. القاعدة لا تحدد التزامًا قانونيًا ولا تحل محل مستشار ضريبي؛ يجب توثيق مصدرها أو ولايتها في الملاحظة.</p></header><section className="grid gap-6 lg:grid-cols-[.8fr_1.2fr]"><Card><CardHeader><CardTitle>قاعدة جديدة</CardTitle><CardDescription>تنطبق القواعد النشطة المختارة فقط على صفقة جديدة؛ لا تعيد المنصة حساب تاريخ العمليات السابقة.</CardDescription></CardHeader><CardContent><form onSubmit={submit} className="grid gap-4"><div className="grid gap-2"><Label htmlFor="charge-name">الاسم</Label><Input id="charge-name" value={name} onChange={event => setName(event.target.value)} disabled={!access.canEdit} required minLength={2} /></div><div className="grid gap-2 sm:grid-cols-2"><div className="grid gap-2"><Label>النوع</Label><Select value={chargeType} onValueChange={value => setChargeType(value as typeof chargeType)} disabled={!access.canEdit}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="fee">رسم</SelectItem><SelectItem value="tax">ضريبة</SelectItem></SelectContent></Select></div><div className="grid gap-2"><Label>ينطبق على</Label><Select value={appliesTo} onValueChange={value => setAppliesTo(value as typeof appliesTo)} disabled={!access.canEdit}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="buy">شراء</SelectItem><SelectItem value="sell">بيع</SelectItem><SelectItem value="both">شراء وبيع</SelectItem></SelectContent></Select></div></div><div className="grid gap-2 sm:grid-cols-2"><div className="grid gap-2"><Label>الاحتساب</Label><Select value={calculationMethod} onValueChange={value => setCalculationMethod(value as typeof calculationMethod)} disabled={!access.canEdit}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="percentage">نسبة من قيمة الصفقة</SelectItem><SelectItem value="flat">مبلغ ثابت</SelectItem></SelectContent></Select></div><div className="grid gap-2"><Label htmlFor="charge-value">القيمة {calculationMethod === "percentage" ? "%" : ""}</Label><Input id="charge-value" value={value} onChange={event => setValue(event.target.value)} disabled={!access.canEdit} inputMode="decimal" required /></div></div>{calculationMethod === "flat" && <div className="grid gap-2"><Label htmlFor="charge-currency">عملة المبلغ الثابت</Label><Input id="charge-currency" value={currency} onChange={event => setCurrency(event.target.value.toUpperCase())} disabled={!access.canEdit} minLength={3} maxLength={3} required /></div>}<div className="grid gap-2"><Label htmlFor="charge-note">ولاية أو مصدر القاعدة</Label><Textarea id="charge-note" value={note} onChange={event => setNote(event.target.value)} disabled={!access.canEdit} maxLength={4000} placeholder="مثال: جدول رسوم الجهة أو مرجع ضريبي موثق" /></div><Button type="submit" disabled={!access.canEdit || create.isPending}>{create.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}{access.canEdit ? "حفظ القاعدة" : "تتطلب صلاحية محرر"}</Button></form></CardContent></Card><Card><CardHeader><CardTitle>القواعد المسجلة</CardTitle><CardDescription>يُظهر كل سجل طريقة الاحتساب ونطاقه؛ القواعد المؤرشفة لا يمكن اختيارها للصفقات الجديدة.</CardDescription></CardHeader><CardContent>{rules.isLoading ? <Skeleton className="h-64" /> : rules.error ? <InlineError message={textError(rules.error)} /> : rules.data?.length ? <div className="space-y-3">{rules.data.map(rule => <div key={rule.id} className="rounded-xl border bg-white p-4"><div className="flex flex-col justify-between gap-3 sm:flex-row"><div><div className="flex flex-wrap items-center gap-2"><p className="font-semibold text-slate-900">{rule.name}</p><Badge variant={rule.status === "active" ? "secondary" : "outline"}>{rule.status === "active" ? "نشطة" : "مؤرشفة"}</Badge><Badge variant="outline">{rule.chargeType === "fee" ? "رسم" : "ضريبة"}</Badge></div><p className="mt-2 text-sm text-slate-600">{rule.calculationMethod === "percentage" ? `${rule.value}% من قيمة الصفقة` : `${money(rule.value, rule.currency || "EGP")} ثابت`} · {rule.appliesTo === "both" ? "شراء وبيع" : rule.appliesTo === "buy" ? "شراء" : "بيع"}</p>{rule.jurisdictionNote ? <p className="mt-2 text-xs text-slate-500">{rule.jurisdictionNote}</p> : null}</div>{rule.status === "active" && access.canEdit ? <Button type="button" variant="outline" size="sm" onClick={() => archive.mutate({ ruleId: rule.id })} disabled={archive.isPending}>أرشفة</Button> : null}</div></div>)}</div> : <EmptyState icon={ReceiptText} title="لا توجد قواعد رسوم أو ضرائب" description="يمكنك إدخال الرسم أو الضريبة يدويًا في الصفقة حاليًا، أو حفظ قاعدة معلنة قابلة للاختيار للصفقات القادمة." />}</CardContent></Card></section></div></DashboardLayout>;
}

const specialAssetTypeLabel: Record<string, string> = { real_estate: "عقار", gold: "ذهب", commodity: "سلعة", other: "أصل آخر" };
const policyTypeLabel: Record<string, string> = { health: "صحي", life: "حياة", property: "ممتلكات", motor: "مركبات", other: "أخرى" };
const premiumCadenceLabel: Record<string, string> = { monthly: "شهري", quarterly: "ربع سنوي", yearly: "سنوي", other: "أخرى" };

function isoDateInput(timestamp = Date.now()) {
  return new Date(timestamp).toISOString().slice(0, 10);
}

function arabicDate(timestamp: number) {
  return new Intl.DateTimeFormat("ar-EG", { dateStyle: "medium" }).format(new Date(timestamp));
}

const utcDateInput = arabicDate;

function GoldYahooSuggestionCard() {
  const access = useFamilyPermissions();
  const [, setLocation] = useLocation();
  const assets = trpc.family.specialAssets.list.useQuery();
  const [assetId, setAssetId] = useState("");
  const input = useMemo(() => ({ assetId: Number(assetId) || 1, symbol: "GC=F" }), [assetId]);
  const suggestion = trpc.family.specialAssets.suggestYahooGoldValue.useQuery(input, { enabled: false, retry: false });
  const recordValuation = trpc.family.specialAssets.recordYahooGoldValuation.useMutation({ onSuccess: data => { toast.success(`حُفظ snapshot الذهب من ${data.source} بحالة مراجعة؛ لم يُنشأ قيد.`); }, onError: error => toast.error(textError(error)) });
  const prepareDraft = async () => {
    const result = await suggestion.refetch();
    if (!result.data) return toast.error(textError(result.error));
    sessionStorage.setItem("family-gold-revaluation-draft", JSON.stringify({ assetId: result.data.assetId, targetValue: result.data.suggestedTargetValue, memo: `اقتراح ${result.data.symbol} من ${result.data.source} بتاريخ ${arabicDate(result.data.asOf)}؛ راجعه قبل النشر.` }));
    setLocation("/assets-insurance");
  };
  const goldAssets = (assets.data ?? []).filter(asset => asset.assetType === "gold" && asset.status === "active");
  return <Card className="border-amber-200 bg-amber-50"><CardHeader><CardTitle className="text-base">اقتراح إعادة تقييم الذهب</CardTitle><CardDescription>يحوّل سعر GC=F من Yahoo من الأونصة التروية إلى جرام عند تسجيل الوحدة بوضوح. لا ينشر هذا الاقتراح قيدًا ماليًا.</CardDescription></CardHeader><CardContent className="flex flex-col gap-3 sm:flex-row sm:items-end"><div className="grid flex-1 gap-2"><Label>أصل الذهب</Label><Select value={assetId} onValueChange={setAssetId} disabled={!access.canAdvise || !goldAssets.length}><SelectTrigger><SelectValue placeholder={goldAssets.length ? "اختر أصل الذهب" : "لا يوجد ذهب نشط بكمية ووحدة"} /></SelectTrigger><SelectContent>{goldAssets.map(asset => <SelectItem key={asset.id} value={String(asset.id)}>{asset.name} — {asset.quantity || "بدون كمية"} {asset.unit || ""}</SelectItem>)}</SelectContent></Select></div><div className="flex flex-wrap gap-2"><Button type="button" variant="outline" disabled={!assetId || !access.canAdvise || suggestion.isFetching} onClick={prepareDraft}>{suggestion.isFetching && <Loader2 className="ml-2 size-4 animate-spin" />}تعبئة مسودة Yahoo</Button><Button type="button" disabled={!assetId || !access.canAdvise || recordValuation.isPending} onClick={() => recordValuation.mutate({ assetId: Number(assetId), symbol: "GC=F", note: "تقييم Yahoo محفوظ للمراجعة؛ لا يُعتمد دفترًا تلقائيًا." })}>{recordValuation.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}حفظ snapshot للمراجعة</Button></div></CardContent></Card>;
}

export function AssetsInsurancePage() {
  const utils = trpc.useUtils();
  const [, setLocation] = useLocation();
  const access = useFamilyPermissions();
  const [assetTab, setAssetTab] = useState<"assets" | "insurance">("assets");
  const bootstrap = trpc.family.bootstrap.useQuery();
  const assets = trpc.family.specialAssets.list.useQuery();
  const policies = trpc.family.insurance.list.useQuery();
  const claims = trpc.family.insurance.claims.useQuery();
  const accounts = trpc.family.accounts.list.useQuery();
  const categories = trpc.family.cashFlow.categories.useQuery();
  const [assetAccountId, setAssetAccountId] = useState("");
  const [assetType, setAssetType] = useState<"real_estate" | "gold" | "commodity" | "other">("real_estate");
  const [assetName, setAssetName] = useState("");
  const [assetQuantity, setAssetQuantity] = useState("");
  const [assetUnit, setAssetUnit] = useState("");
  const [assetDetails, setAssetDetails] = useState("");
  const [assetOwnershipType, setAssetOwnershipType] = useState<"sole" | "joint" | "usufruct" | "other">("sole");
  const [assetOwnershipShare, setAssetOwnershipShare] = useState("100");
  const [assetAcquisitionDate, setAssetAcquisitionDate] = useState("");
  const [assetAcquisitionCost, setAssetAcquisitionCost] = useState("");
  const [assetAcquisitionCurrency, setAssetAcquisitionCurrency] = useState("EGP");
  const [assetLocation, setAssetLocation] = useState("");
  const [assetMarketSymbol, setAssetMarketSymbol] = useState("GC=F");
  const [assetPurity, setAssetPurity] = useState("");
  const [assetValuationMethod, setAssetValuationMethod] = useState<"ledger_balance" | "market_quote" | "manual" | "appraisal">("ledger_balance");
  const [assetValuationSource, setAssetValuationSource] = useState("");
  const [assetValuationDate, setAssetValuationDate] = useState("");
  const [assetValuationNote, setAssetValuationNote] = useState("");
  const [revaluationAssetId, setRevaluationAssetId] = useState("");
  const [revaluationTarget, setRevaluationTarget] = useState("");
  const [revaluationDate, setRevaluationDate] = useState(isoDateInput);
  const [revaluationMemo, setRevaluationMemo] = useState("");
  const [policyName, setPolicyName] = useState("");
  const [policyType, setPolicyType] = useState<"health" | "life" | "property" | "motor" | "other">("health");
  const [insurer, setInsurer] = useState("");
  const [policyNumber, setPolicyNumber] = useState("");
  const [coverageAmount, setCoverageAmount] = useState("");
  const [policyCurrency, setPolicyCurrency] = useState("EGP");
  const [premiumAmount, setPremiumAmount] = useState("");
  const [premiumCadence, setPremiumCadence] = useState<"monthly" | "quarterly" | "yearly" | "other">("yearly");
  const [policyCategoryId, setPolicyCategoryId] = useState("");
  const [policyStartsAt, setPolicyStartsAt] = useState("");
  const [policyEndsAt, setPolicyEndsAt] = useState("");
  const [beneficiaries, setBeneficiaries] = useState("");
  const [claimsNote, setClaimsNote] = useState("");
  const [policyRenewalAt, setPolicyRenewalAt] = useState("");
  const [policyDueDay, setPolicyDueDay] = useState("");
  const [deductibleAmount, setDeductibleAmount] = useState("");
  const [deductibleCurrency, setDeductibleCurrency] = useState("EGP");
  const [providerContact, setProviderContact] = useState("");
  const [policyTerms, setPolicyTerms] = useState("");
  const [claimPolicyId, setClaimPolicyId] = useState("");
  const [claimReference, setClaimReference] = useState("");
  const [claimAmount, setClaimAmount] = useState("");
  const [claimDate, setClaimDate] = useState(isoDateInput);
  const [claimExpectedDate, setClaimExpectedDate] = useState("");
  const [claimNote, setClaimNote] = useState("");
  const [premiumPolicyId, setPremiumPolicyId] = useState("");
  const [premiumAccountId, setPremiumAccountId] = useState("");
  const [paymentAmount, setPaymentAmount] = useState("");
  const [paymentMemo, setPaymentMemo] = useState("");
  useEffect(() => {
    const raw = sessionStorage.getItem("family-gold-revaluation-draft");
    if (!raw) return;
    try {
      const draft = JSON.parse(raw) as { assetId?: number; targetValue?: string; memo?: string };
      if (Number.isInteger(draft.assetId) && draft.targetValue) { setRevaluationAssetId(String(draft.assetId)); setRevaluationTarget(draft.targetValue); setRevaluationMemo(draft.memo || ""); }
    } finally { sessionStorage.removeItem("family-gold-revaluation-draft"); }
  }, []);

  const createAsset = trpc.family.specialAssets.create.useMutation({
    onSuccess: () => {
      toast.success("تم حفظ سجل الأصل ضمن نطاقك. تبقى قيمته مستمدة من حساب الأصل المقيد.");
      setAssetAccountId(""); setAssetName(""); setAssetQuantity(""); setAssetUnit(""); setAssetDetails(""); setAssetOwnershipType("sole"); setAssetOwnershipShare("100"); setAssetAcquisitionDate(""); setAssetAcquisitionCost(""); setAssetLocation(""); setAssetMarketSymbol("GC=F"); setAssetPurity(""); setAssetValuationMethod("ledger_balance"); setAssetValuationSource(""); setAssetValuationDate(""); setAssetValuationNote("");
      void utils.family.specialAssets.list.invalidate();
    },
    onError: error => toast.error(textError(error)),
  });
  const recordGoldValuation = trpc.family.specialAssets.recordYahooGoldValuation.useMutation({
    onSuccess: data => { toast.success(`حُفظ تقييم الذهب من ${data.source} بتاريخ ${arabicDate(data.asOf)}؛ لم يُنشأ قيد مالي.`); void utils.family.specialAssets.list.invalidate(); },
    onError: error => toast.error(textError(error)),
  });
  const revalueAsset = trpc.family.specialAssets.revalue.useMutation({
    onSuccess: data => {
      toast.success(data.direction === "increase" ? "نُشرت زيادة التقييم في دفتر الأصول." : "نُشرت خفضة التقييم في دفتر الأصول.");
      setRevaluationTarget(""); setRevaluationMemo("");
      void utils.family.specialAssets.list.invalidate(); void utils.family.accounts.list.invalidate(); void utils.family.dashboard.invalidate(); void utils.family.ledger.recent.invalidate();
    },
    onError: error => toast.error(textError(error)),
  });
  const createPolicy = trpc.family.insurance.create.useMutation({
    onSuccess: () => {
      toast.success("تم حفظ بوليصة التأمين. لا يُسجل قسط مالي إلا عند نشر دفعة صريحة.");
      setPolicyName(""); setInsurer(""); setPolicyNumber(""); setCoverageAmount(""); setPremiumAmount(""); setPolicyCategoryId(""); setPolicyStartsAt(""); setPolicyEndsAt(""); setBeneficiaries(""); setClaimsNote(""); setPolicyRenewalAt(""); setPolicyDueDay(""); setDeductibleAmount(""); setProviderContact(""); setPolicyTerms("");
      void utils.family.insurance.list.invalidate();
    },
    onError: error => toast.error(textError(error)),
  });
  const postPremium = trpc.family.insurance.postPremium.useMutation({
    onSuccess: () => {
      toast.success("نُشر قسط التأمين كمصروف مصنف وقيد متوازن.");
      setPremiumAccountId(""); setPaymentAmount(""); setPaymentMemo("");
      void utils.family.insurance.list.invalidate(); void utils.family.accounts.list.invalidate(); void utils.family.dashboard.invalidate(); void utils.family.ledger.recent.invalidate(); void utils.family.cashFlow.summary.invalidate();
    },
    onError: error => toast.error(textError(error)),
  });
  const createClaim = trpc.family.insurance.createClaim.useMutation({
    onSuccess: () => { toast.success("سُجلت المطالبة بحالة مقدمة؛ لا تُعتبر محصلة حتى ربطها بدخل منشور."); setClaimPolicyId(""); setClaimReference(""); setClaimAmount(""); setClaimExpectedDate(""); setClaimNote(""); void utils.family.insurance.claims.invalidate(); void utils.family.insurance.list.invalidate(); },
    onError: error => toast.error(textError(error)),
  });

  const assetAccounts = (accounts.data ?? []).filter(account => account.accountType === "asset");
  const usedAssetAccountIds = new Set((assets.data ?? []).map(asset => asset.accountId));
  const availableAssetAccounts = assetAccounts.filter(account => !usedAssetAccountIds.has(account.id));
  const expenseCategories = (categories.data ?? []).filter(category => category.direction === "expense");
  const selectedRevaluationAsset = assets.data?.find(asset => String(asset.id) === revaluationAssetId);
  const selectedPolicy = policies.data?.find(policy => String(policy.id) === premiumPolicyId);
  const workspaceBaseCurrency = bootstrap.data?.workspace.baseCurrency ?? "EGP";
  const eligiblePaymentAccounts = (accounts.data ?? []).filter(account => ["cash", "bank", "brokerage", "wallet"].includes(account.accountType) && account.currency === selectedPolicy?.currency);

  const submitAsset = (event: React.FormEvent) => {
    event.preventDefault();
    if (!assetAccountId) return toast.error("أنشئ أو اختر حساب أصل نشطًا قبل إضافة السجل.");
    createAsset.mutate({ assetAccountId: Number(assetAccountId), assetType, name: assetName, quantity: assetQuantity || null, unit: assetUnit || null, ownershipType: assetOwnershipType, ownershipShare: assetOwnershipShare, acquisitionDate: assetAcquisitionDate ? Date.parse(`${assetAcquisitionDate}T00:00:00.000Z`) : null, acquisitionCost: assetAcquisitionCost || null, acquisitionCurrency: assetAcquisitionCost ? assetAcquisitionCurrency : null, location: assetLocation || null, marketSymbol: assetMarketSymbol || null, purity: assetPurity || null, valuationMethod: assetValuationMethod, valuationSource: assetValuationSource || null, valuationAsOf: assetValuationDate ? Date.parse(`${assetValuationDate}T00:00:00.000Z`) : null, valuationNote: assetValuationNote || null, details: assetDetails || null });
  };
  const submitRevaluation = (event: React.FormEvent) => {
    event.preventDefault();
    if (!revaluationAssetId || !revaluationDate) return toast.error("اختر الأصل وتاريخ التقييم.");
    revalueAsset.mutate({ assetId: Number(revaluationAssetId), targetValue: revaluationTarget, occurredAt: Date.parse(`${revaluationDate}T00:00:00.000Z`), memo: revaluationMemo || null, idempotencyKey: crypto.randomUUID() });
  };
  const submitPolicy = (event: React.FormEvent) => {
    event.preventDefault();
    createPolicy.mutate({ name: policyName, policyType, insurer: insurer || null, policyNumber: policyNumber || null, coverageAmount: coverageAmount || null, currency: policyCurrency, premiumAmount: premiumAmount || null, premiumCadence: premiumAmount ? premiumCadence : null, cashFlowCategoryId: policyCategoryId ? Number(policyCategoryId) : null, startsAt: policyStartsAt ? Date.parse(`${policyStartsAt}T00:00:00.000Z`) : null, endsAt: policyEndsAt ? Date.parse(`${policyEndsAt}T00:00:00.000Z`) : null, renewalAt: policyRenewalAt ? Date.parse(`${policyRenewalAt}T00:00:00.000Z`) : null, premiumDueDay: policyDueDay ? Number(policyDueDay) : null, deductibleAmount: deductibleAmount || null, deductibleCurrency: deductibleAmount ? deductibleCurrency : null, providerContact: providerContact || null, policyTerms: policyTerms || null, beneficiaries: beneficiaries || null, claimsNote: claimsNote || null });
  };
  const submitPremium = (event: React.FormEvent) => {
    event.preventDefault();
    if (!selectedPolicy || !premiumAccountId) return toast.error("اختر بوليصة وحساب دفع متوافقين.");
    postPremium.mutate({ policyId: selectedPolicy.id, cashAccountId: Number(premiumAccountId), amount: paymentAmount, occurredAt: Date.now(), memo: paymentMemo || null, idempotencyKey: crypto.randomUUID() });
  };
  const submitClaim = (event: React.FormEvent) => {
    event.preventDefault();
    if (!claimPolicyId) return toast.error("اختر البوليصة المرتبطة بالمطالبة.");
    createClaim.mutate({ policyId: Number(claimPolicyId), referenceNumber: claimReference || null, claimedAmount: claimAmount, submittedAt: Date.parse(`${claimDate}T00:00:00.000Z`), expectedAt: claimExpectedDate ? Date.parse(`${claimExpectedDate}T00:00:00.000Z`) : null, note: claimNote || null });
  };

  return <DashboardLayout><div dir="rtl" className="family-assets-page mx-auto max-w-7xl space-y-6" data-active-tab={assetTab}><header className="border-b pb-6"><p className="text-xs font-semibold uppercase tracking-[.15em] text-emerald-700">FAMILY / Private assets & protection</p><h1 className="mt-2 text-3xl font-bold">الأصول الخاصة والتأمين</h1><p className="mt-2 max-w-4xl text-sm leading-6 text-slate-500">تربط هذه الوحدة الأصل بحساب أصول مقيد، وتوثق بوليصة التأمين بصورة مستقلة. إعادة التقييم أو دفع القسط ينشئان عملية دفتر وسجل تدقيق؛ لا تُضاف قيمة أو نفقة مباشرة من الواجهة.</p></header><Tabs value={assetTab} onValueChange={value => setAssetTab(value as "assets" | "insurance")}><TabsList className="family-assets-tabs-list"><TabsTrigger value="assets">الأصول الخاصة</TabsTrigger><TabsTrigger value="insurance">التأمين والوثائق</TabsTrigger></TabsList></Tabs>{assetTab === "assets" && <GoldYahooSuggestionCard />}
    <section className="grid gap-6 xl:grid-cols-2"><Card><CardHeader><CardTitle>إضافة أصل خاص</CardTitle><CardDescription>أنشئ حساب أصل برصيد افتتاحي مقيد من صفحة الحسابات، ثم اربطه بسجل عقار أو ذهب أو سلعة. لا يُستخدم السجل لإدخال قيمة مستقلة.</CardDescription></CardHeader><CardContent>{accounts.isLoading || assets.isLoading ? <Skeleton className="h-[31rem]" /> : <form onSubmit={submitAsset} className="grid gap-4"><div className="grid gap-2"><Label>حساب الأصل</Label><Select value={assetAccountId} onValueChange={setAssetAccountId} disabled={!access.canEdit || !availableAssetAccounts.length}><SelectTrigger><SelectValue placeholder={availableAssetAccounts.length ? "اختر حساب الأصل" : "لا يوجد حساب أصل متاح"} /></SelectTrigger><SelectContent>{availableAssetAccounts.map(account => <SelectItem value={String(account.id)} key={account.id}>{account.name} — {money(account.balance, account.currency)}</SelectItem>)}</SelectContent></Select>{!availableAssetAccounts.length && <div className="flex flex-wrap items-center gap-3 rounded-lg border border-amber-200 bg-amber-50 p-3 text-xs text-amber-900"><span>تحتاج إلى حساب نوعه «أصل» قبل إنشاء السجل، أو أن الحسابات المتاحة مرتبطة بالفعل بأصول أخرى.</span><Button type="button" size="sm" variant="outline" onClick={() => setLocation("/accounts")}>إلى الحسابات</Button></div>}</div><div className="grid gap-2 sm:grid-cols-2"><div className="grid gap-2"><Label htmlFor="private-asset-name">اسم الأصل</Label><Input id="private-asset-name" value={assetName} onChange={event => setAssetName(event.target.value)} disabled={!access.canEdit} minLength={2} required /></div><div className="grid gap-2"><Label>النوع</Label><Select value={assetType} onValueChange={value => setAssetType(value as typeof assetType)} disabled={!access.canEdit}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{Object.entries(specialAssetTypeLabel).map(([value, label]) => <SelectItem key={value} value={value}>{label}</SelectItem>)}</SelectContent></Select></div></div><div className="grid gap-2 sm:grid-cols-2"><div className="grid gap-2"><Label htmlFor="private-asset-quantity">الكمية</Label><Input id="private-asset-quantity" value={assetQuantity} onChange={event => setAssetQuantity(event.target.value)} disabled={!access.canEdit} inputMode="decimal" placeholder="اختياري" /></div><div className="grid gap-2"><Label htmlFor="private-asset-unit">الوحدة</Label><Input id="private-asset-unit" value={assetUnit} onChange={event => setAssetUnit(event.target.value)} disabled={!access.canEdit} maxLength={48} placeholder="مثال: جرام أو متر²" /></div></div><div className="grid gap-2 sm:grid-cols-3"><div className="grid gap-2"><Label>نوع الملكية</Label><Select value={assetOwnershipType} onValueChange={value => setAssetOwnershipType(value as typeof assetOwnershipType)} disabled={!access.canEdit}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="sole">ملكية كاملة</SelectItem><SelectItem value="joint">ملكية مشتركة</SelectItem><SelectItem value="usufruct">حق انتفاع</SelectItem><SelectItem value="other">أخرى</SelectItem></SelectContent></Select></div><div className="grid gap-2"><Label htmlFor="private-asset-share">نسبة الملكية %</Label><Input id="private-asset-share" value={assetOwnershipShare} onChange={event => setAssetOwnershipShare(event.target.value)} disabled={!access.canEdit} inputMode="decimal" required /></div><div className="grid gap-2"><Label htmlFor="private-asset-location">الموقع</Label><Input id="private-asset-location" value={assetLocation} onChange={event => setAssetLocation(event.target.value)} disabled={!access.canEdit} maxLength={255} placeholder="مدينة أو وصف مختصر" /></div></div><div className="grid gap-2 sm:grid-cols-3"><div className="grid gap-2"><Label htmlFor="private-asset-acquisition-date">تاريخ الاقتناء</Label><Input id="private-asset-acquisition-date" type="date" value={assetAcquisitionDate} onChange={event => setAssetAcquisitionDate(event.target.value)} disabled={!access.canEdit} /></div><div className="grid gap-2"><Label htmlFor="private-asset-acquisition-cost">تكلفة الاقتناء</Label><Input id="private-asset-acquisition-cost" value={assetAcquisitionCost} onChange={event => setAssetAcquisitionCost(event.target.value)} disabled={!access.canEdit} inputMode="decimal" /></div><div className="grid gap-2"><Label htmlFor="private-asset-acquisition-currency">عملة التكلفة</Label><Input id="private-asset-acquisition-currency" value={assetAcquisitionCurrency} onChange={event => setAssetAcquisitionCurrency(event.target.value.toUpperCase())} disabled={!access.canEdit || !assetAcquisitionCost} minLength={3} maxLength={3} /></div></div><div className="grid gap-2 sm:grid-cols-3"><div className="grid gap-2"><Label htmlFor="private-asset-market-symbol">رمز السوق</Label><Input id="private-asset-market-symbol" value={assetMarketSymbol} onChange={event => setAssetMarketSymbol(event.target.value.toUpperCase())} disabled={!access.canEdit} maxLength={48} placeholder="GC=F للذهب" /></div><div className="grid gap-2"><Label htmlFor="private-asset-purity">النقاوة %</Label><Input id="private-asset-purity" value={assetPurity} onChange={event => setAssetPurity(event.target.value)} disabled={!access.canEdit || assetType !== "gold"} inputMode="decimal" placeholder="مثال 99.9" /></div><div className="grid gap-2"><Label>منهجية التقييم</Label><Select value={assetValuationMethod} onValueChange={value => setAssetValuationMethod(value as typeof assetValuationMethod)} disabled={!access.canEdit}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="ledger_balance">رصيد الدفتر</SelectItem><SelectItem value="market_quote">سعر سوق</SelectItem><SelectItem value="manual">يدوي</SelectItem><SelectItem value="appraisal">تقييم خبير</SelectItem></SelectContent></Select></div></div><div className="grid gap-2"><Label htmlFor="private-asset-details">وصف أو ملاحظات الملكية</Label><Textarea id="private-asset-details" value={assetDetails} onChange={event => setAssetDetails(event.target.value)} disabled={!access.canEdit} maxLength={4000} placeholder="اختياري؛ لا تحفظ مستندات أو أرقام حساسة في هذا الحقل" /></div><div className="grid gap-2 sm:grid-cols-2"><div className="grid gap-2"><Label htmlFor="private-asset-valuation-source">مصدر التقييم</Label><Input id="private-asset-valuation-source" value={assetValuationSource} onChange={event => setAssetValuationSource(event.target.value)} disabled={!access.canEdit} maxLength={255} placeholder="مصدر أو اسم خبير" /></div><div className="grid gap-2"><Label htmlFor="private-asset-valuation-date">تاريخ التقييم</Label><Input id="private-asset-valuation-date" type="date" value={assetValuationDate} onChange={event => setAssetValuationDate(event.target.value)} disabled={!access.canEdit} /></div></div><div className="grid gap-2"><Label htmlFor="private-asset-valuation-note">ملاحظة التقييم</Label><Textarea id="private-asset-valuation-note" value={assetValuationNote} onChange={event => setAssetValuationNote(event.target.value)} disabled={!access.canEdit} maxLength={4000} placeholder="لماذا اختيرت هذه المنهجية؟" /></div><Button type="submit" disabled={createAsset.isPending || !access.canEdit || !availableAssetAccounts.length}>{createAsset.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}{access.canEdit ? "حفظ سجل الأصل" : "تتطلب صلاحية محرر"}</Button></form>}</CardContent></Card>
      <Card><CardHeader><CardTitle>إعادة تقييم موثقة</CardTitle><CardDescription>أدخل القيمة الجديدة للأصل في عملة حسابه. ينشئ النظام قيد مكسب أو خسارة تقييم متوازن ولا ينفذ شراءً أو بيعًا.</CardDescription></CardHeader><CardContent>{assets.isLoading ? <Skeleton className="h-[31rem]" /> : <form onSubmit={submitRevaluation} className="grid gap-4"><div className="grid gap-2"><Label>الأصل</Label><Select value={revaluationAssetId} onValueChange={value => { setRevaluationAssetId(value); const asset = assets.data?.find(item => String(item.id) === value); setRevaluationTarget(asset?.balance ?? ""); }} disabled={!access.canAdvise}><SelectTrigger><SelectValue placeholder="اختر أصلًا مسجلًا" /></SelectTrigger><SelectContent>{assets.data?.filter(asset => asset.status === "active").map(asset => <SelectItem key={asset.id} value={String(asset.id)}>{asset.name} — {money(asset.balance, asset.currency)}</SelectItem>)}</SelectContent></Select></div>{selectedRevaluationAsset ? <div className="rounded-xl border bg-slate-50 p-4 text-sm"><div className="flex flex-wrap items-center justify-between gap-3"><span className="font-semibold text-slate-900">القيمة المقيدة حاليًا</span><span className="font-semibold text-emerald-700">{money(selectedRevaluationAsset.balance, selectedRevaluationAsset.currency)}</span></div><p className="mt-2 text-xs leading-5 text-slate-500">التقييم بعملة الحساب: {selectedRevaluationAsset.currency}{selectedRevaluationAsset.baseValue !== null ? ` · القيمة بعملة الأساس: ${money(selectedRevaluationAsset.baseValue, workspaceBaseCurrency)}` : " · يلزم سعر صرف لتقييم القيمة الأساسية"}.</p></div> : <div className="rounded-xl border border-dashed p-4 text-sm text-slate-500">اختر أصلًا لعرض قيمته الدفترية قبل إنشاء التعديل.</div>}<div className="grid gap-2 sm:grid-cols-2"><div className="grid gap-2"><Label htmlFor="private-asset-target">قيمة التقييم الجديدة</Label><Input id="private-asset-target" value={revaluationTarget} onChange={event => setRevaluationTarget(event.target.value)} disabled={!access.canAdvise} inputMode="decimal" required /></div><div className="grid gap-2"><Label htmlFor="private-asset-date">تاريخ التقييم</Label><Input id="private-asset-date" type="date" value={revaluationDate} onChange={event => setRevaluationDate(event.target.value)} disabled={!access.canAdvise} required /></div></div><div className="grid gap-2"><Label htmlFor="private-asset-valuation-note">مرجع أو ملاحظة التقييم</Label><Textarea id="private-asset-valuation-note" value={revaluationMemo} onChange={event => setRevaluationMemo(event.target.value)} disabled={!access.canAdvise} maxLength={2000} placeholder="مثال: تقييم خبير مؤرخ أو سعر سوق موثق" /></div><Button type="submit" disabled={revalueAsset.isPending || !access.canAdvise || !revaluationAssetId}>{revalueAsset.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}{access.canAdvise ? "نشر إعادة التقييم" : "تتطلب صلاحية مستشار"}</Button></form>}</CardContent></Card></section>
    <section className="grid gap-6 xl:grid-cols-[.85fr_1.15fr]"><Card><CardHeader><CardTitle>سجل الأصول الخاصة</CardTitle><CardDescription>القيمة الظاهرة هي رصيد حساب الأصل من دفتر القيود، لا قيمة نصية مكررة في سجل الأصل.</CardDescription></CardHeader><CardContent>{assets.isLoading ? <Skeleton className="h-56" /> : assets.error ? <InlineError message={textError(assets.error)} /> : assets.data?.length ? <div className="space-y-3">{assets.data.map(asset => <div key={asset.id} className="rounded-xl border bg-white p-4"><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-start"><div><div className="flex flex-wrap items-center gap-2"><p className="font-semibold text-slate-900">{asset.name}</p><Badge variant="secondary">{specialAssetTypeLabel[asset.assetType]}</Badge></div><p className="mt-2 text-sm text-slate-600">الحساب: {asset.accountName} · {asset.currency}</p><p className="mt-1 text-xs text-slate-500">{asset.quantity ? `${asset.quantity} ${asset.unit || "وحدة"}` : "لا توجد كمية مسجلة"}{asset.details ? ` · ${asset.details}` : ""}</p>{asset.location ? <p className="mt-1 text-xs text-slate-500">الموقع: {asset.location}</p> : null}<p className="mt-1 text-xs text-slate-500">الملكية: {asset.ownershipShare}% · {asset.ownershipType === "sole" ? "كاملة" : asset.ownershipType === "joint" ? "مشتركة" : asset.ownershipType === "usufruct" ? "حق انتفاع" : "أخرى"}</p></div><div className="text-right sm:text-left"><p className="text-xs text-slate-500">الرصيد المقيد</p><p className="mt-1 font-semibold text-emerald-700">{money(asset.balance, asset.currency)}</p>{asset.baseValue !== null ? <p className="mt-1 text-xs text-slate-500">{money(asset.baseValue, workspaceBaseCurrency)}</p> : <p className="mt-1 text-xs text-amber-700">يحتاج سعر صرف</p>}{asset.latestValuation ? <p className="mt-1 text-xs text-slate-500">آخر تقييم: {money(asset.latestValuation.value, asset.latestValuation.currency)} · {asset.latestValuation.source}</p> : <p className="mt-1 text-xs text-amber-700">لا يوجد تقييم provenance</p>}<Badge className="mt-2" variant={asset.valuationStatus === "current" ? "secondary" : "outline"}>{asset.valuationStatus === "current" ? "تقييم حالي" : asset.valuationStatus === "review_required" ? "يتطلب مراجعة" : "دفتر/غير مقيم"}</Badge></div></div></div>)}</div> : <EmptyState icon={BriefcaseBusiness} title="لا توجد أصول خاصة مسجلة" description="أنشئ حساب أصل مقيد ثم أضف سجل العقار أو الذهب أو السلعة من نموذج الإدخال أعلاه." />}</CardContent></Card>
      <Card><CardHeader><CardTitle>إضافة بوليصة تأمين</CardTitle><CardDescription>البوليصة سجل تغطية فقط. اختر فئة مصروف إن أردت ربط أقساطها اللاحقة بالتدفق النقدي الفعلي.</CardDescription></CardHeader><CardContent>{categories.isLoading ? <Skeleton className="h-[35rem]" /> : <form onSubmit={submitPolicy} className="grid gap-4"><div className="grid gap-2 sm:grid-cols-2"><div className="grid gap-2"><Label htmlFor="policy-name">اسم البوليصة</Label><Input id="policy-name" value={policyName} onChange={event => setPolicyName(event.target.value)} disabled={!access.canEdit} required minLength={2} /></div><div className="grid gap-2"><Label>نوع التأمين</Label><Select value={policyType} onValueChange={value => setPolicyType(value as typeof policyType)} disabled={!access.canEdit}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{Object.entries(policyTypeLabel).map(([value, label]) => <SelectItem key={value} value={value}>{label}</SelectItem>)}</SelectContent></Select></div></div><div className="grid gap-2 sm:grid-cols-2"><div className="grid gap-2"><Label htmlFor="policy-insurer">شركة التأمين</Label><Input id="policy-insurer" value={insurer} onChange={event => setInsurer(event.target.value)} disabled={!access.canEdit} maxLength={160} /></div><div className="grid gap-2"><Label htmlFor="policy-number">رقم الوثيقة</Label><Input id="policy-number" value={policyNumber} onChange={event => setPolicyNumber(event.target.value)} disabled={!access.canEdit} maxLength={160} /></div></div><div className="grid gap-2 sm:grid-cols-3"><div className="grid gap-2"><Label htmlFor="policy-coverage">قيمة التغطية</Label><Input id="policy-coverage" value={coverageAmount} onChange={event => setCoverageAmount(event.target.value)} disabled={!access.canEdit} inputMode="decimal" /></div><div className="grid gap-2"><Label htmlFor="policy-currency">العملة</Label><Input id="policy-currency" value={policyCurrency} onChange={event => setPolicyCurrency(event.target.value.toUpperCase())} disabled={!access.canEdit} minLength={3} maxLength={3} required /></div><div className="grid gap-2"><Label htmlFor="policy-premium">القسط المتوقع</Label><Input id="policy-premium" value={premiumAmount} onChange={event => setPremiumAmount(event.target.value)} disabled={!access.canEdit} inputMode="decimal" /></div></div><div className="grid gap-2 sm:grid-cols-2"><div className="grid gap-2"><Label>دورية القسط</Label><Select value={premiumCadence} onValueChange={value => setPremiumCadence(value as typeof premiumCadence)} disabled={!access.canEdit || !premiumAmount}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{Object.entries(premiumCadenceLabel).map(([value, label]) => <SelectItem key={value} value={value}>{label}</SelectItem>)}</SelectContent></Select></div><div className="grid gap-2"><Label>فئة قسط التأمين</Label><Select value={policyCategoryId || undefined} onValueChange={setPolicyCategoryId} disabled={!access.canEdit}><SelectTrigger><SelectValue placeholder="اختياري؛ فئة مصروف" /></SelectTrigger><SelectContent>{expenseCategories.map(category => <SelectItem key={category.id} value={String(category.id)}>{category.name}</SelectItem>)}</SelectContent></Select></div></div><div className="grid gap-2 sm:grid-cols-2"><div className="grid gap-2"><Label htmlFor="policy-start">تاريخ البداية</Label><Input id="policy-start" type="date" value={policyStartsAt} onChange={event => setPolicyStartsAt(event.target.value)} disabled={!access.canEdit} /></div><div className="grid gap-2"><Label htmlFor="policy-end">تاريخ الانتهاء</Label><Input id="policy-end" type="date" value={policyEndsAt} onChange={event => setPolicyEndsAt(event.target.value)} disabled={!access.canEdit} min={policyStartsAt || undefined} /></div></div><div className="grid gap-2 sm:grid-cols-3"><div className="grid gap-2"><Label htmlFor="policy-renewal">تاريخ التجديد</Label><Input id="policy-renewal" type="date" value={policyRenewalAt} onChange={event => setPolicyRenewalAt(event.target.value)} disabled={!access.canEdit} min={policyStartsAt || undefined} max={policyEndsAt || undefined} /></div><div className="grid gap-2"><Label htmlFor="policy-due-day">يوم استحقاق القسط</Label><Input id="policy-due-day" type="number" min="1" max="31" value={policyDueDay} onChange={event => setPolicyDueDay(event.target.value)} disabled={!access.canEdit || !premiumAmount} placeholder="1–31" /></div><div className="grid gap-2"><Label htmlFor="policy-provider-contact">بيانات المزود</Label><Input id="policy-provider-contact" value={providerContact} onChange={event => setProviderContact(event.target.value)} disabled={!access.canEdit} maxLength={255} placeholder="هاتف أو بريد" /></div></div><div className="grid gap-2 sm:grid-cols-3"><div className="grid gap-2"><Label htmlFor="policy-deductible">قيمة التحمل</Label><Input id="policy-deductible" value={deductibleAmount} onChange={event => setDeductibleAmount(event.target.value)} disabled={!access.canEdit} inputMode="decimal" /></div><div className="grid gap-2"><Label htmlFor="policy-deductible-currency">عملة التحمل</Label><Input id="policy-deductible-currency" value={deductibleCurrency} onChange={event => setDeductibleCurrency(event.target.value.toUpperCase())} disabled={!access.canEdit || !deductibleAmount} minLength={3} maxLength={3} /></div><div className="grid gap-2"><Label htmlFor="policy-terms">الشروط المختصرة</Label><Input id="policy-terms" value={policyTerms} onChange={event => setPolicyTerms(event.target.value)} disabled={!access.canEdit} maxLength={12_000} placeholder="استثناءات أو ملاحظات" /></div></div><div className="grid gap-2"><Label htmlFor="policy-beneficiaries">المستفيدون</Label><Textarea id="policy-beneficiaries" value={beneficiaries} onChange={event => setBeneficiaries(event.target.value)} disabled={!access.canEdit} maxLength={4000} placeholder="اختياري" /></div><div className="grid gap-2"><Label htmlFor="policy-claims">ملاحظات المطالبات</Label><Textarea id="policy-claims" value={claimsNote} onChange={event => setClaimsNote(event.target.value)} disabled={!access.canEdit} maxLength={4000} placeholder="اختياري" /></div><Button type="submit" disabled={createPolicy.isPending || !access.canEdit}>{createPolicy.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}{access.canEdit ? "حفظ البوليصة" : "تتطلب صلاحية محرر"}</Button></form>}</CardContent></Card></section>
    <section className="grid gap-6 xl:grid-cols-2"><Card><CardHeader><CardTitle>وثائق التأمين</CardTitle><CardDescription>يعرض السجل التغطية والقسط المخطط وآخر قسط منشور فقط؛ لا يفترض استحقاقًا أو دفعًا تلقائيًا.</CardDescription></CardHeader><CardContent>{policies.isLoading ? <Skeleton className="h-64" /> : policies.error ? <InlineError message={textError(policies.error)} /> : policies.data?.length ? <div className="space-y-3">{policies.data.map(policy => <div key={policy.id} className="rounded-xl border bg-white p-4"><div className="flex flex-col justify-between gap-3 sm:flex-row"><div><div className="flex flex-wrap items-center gap-2"><p className="font-semibold text-slate-900">{policy.name}</p><Badge variant="secondary">{policyTypeLabel[policy.policyType]}</Badge><Badge variant="outline">{policy.status === "active" ? "نشطة" : policy.status === "expired" ? "منتهية" : "مؤرشفة"}</Badge></div><p className="mt-2 text-sm text-slate-600">{policy.insurer || "جهة غير محددة"}{policy.policyNumber ? ` · وثيقة ${policy.policyNumber}` : ""}</p><p className="mt-1 text-xs text-slate-500">تغطية: {policy.coverageAmount ? money(policy.coverageAmount, policy.currency) : "غير مسجلة"} · قسط متوقع: {policy.premiumAmount ? `${money(policy.premiumAmount, policy.currency)}${policy.premiumCadence ? ` / ${premiumCadenceLabel[policy.premiumCadence]}` : ""}` : "غير مسجل"}</p>{policy.startsAt || policy.endsAt ? <p className="mt-1 text-xs text-slate-500">{policy.startsAt ? `تبدأ ${utcDateInput(policy.startsAt)}` : ""}{policy.startsAt && policy.endsAt ? " · " : ""}{policy.endsAt ? `تنتهي ${utcDateInput(policy.endsAt)}` : ""}</p> : null}{policy.renewalAt ? <p className="mt-1 text-xs text-amber-700">التجديد: {utcDateInput(policy.renewalAt)}{policy.premiumDueDay ? ` · يوم الاستحقاق ${policy.premiumDueDay}` : ""}</p> : null}<p className="mt-1 text-xs text-slate-500">المطالبات: {policy.claimSummary.total} · مقدمة {policy.claimSummary.submitted} · محصلة {policy.claimSummary.paid}</p></div><div className="rounded-lg bg-slate-50 p-3 text-xs text-slate-600">{policy.latestPremiumPayment ? <><p>آخر قسط منشور</p><p className="mt-1 font-semibold text-slate-900">{money(policy.latestPremiumPayment.amount, policy.latestPremiumPayment.currency)}</p><p className="mt-1">{dateTime(policy.latestPremiumPayment.occurredAt)}</p></> : <p>لم يُنشر قسط مالي بعد.</p>}</div></div></div>)}</div> : <EmptyState icon={Umbrella} title="لا توجد وثائق تأمين" description="أضف وثيقة للتعامل مع التغطية والقسط المتوقع، ثم انشر أي قسط مدفوع من النموذج المجاور." />}</CardContent></Card>
      <Card><CardHeader><CardTitle>تسجيل قسط مدفوع</CardTitle><CardDescription>ينشر القسط كمصروف مصنف من حساب دفع وبالعملة نفسها، مع ربطه بالبوليصة. لا تُنشأ دفعات متكررة تلقائيًا من هذه الشاشة.</CardDescription></CardHeader><CardContent>{policies.isLoading || accounts.isLoading ? <Skeleton className="h-64" /> : <form onSubmit={submitPremium} className="grid gap-4"><div className="grid gap-2"><Label>البوليصة</Label><Select value={premiumPolicyId} onValueChange={value => { setPremiumPolicyId(value); setPremiumAccountId(""); const policy = policies.data?.find(item => String(item.id) === value); setPaymentAmount(policy?.premiumAmount ?? ""); }} disabled={!access.canEdit}><SelectTrigger><SelectValue placeholder="اختر بوليصة نشطة" /></SelectTrigger><SelectContent>{policies.data?.filter(policy => policy.status === "active").map(policy => <SelectItem key={policy.id} value={String(policy.id)}>{policy.name} — {policy.currency}</SelectItem>)}</SelectContent></Select></div>{selectedPolicy && !selectedPolicy.cashFlowCategoryId ? <div className="rounded-lg border border-amber-200 bg-amber-50 p-3 text-xs leading-5 text-amber-900">هذه البوليصة غير مرتبطة بفئة مصروف، لذا لا يمكن نشر قسط منها حتى تُنشأ بوليصة مرتبطة بفئة مناسبة. لا يضع النظام فئة افتراضية.</div> : null}<div className="grid gap-2"><Label>حساب الدفع</Label><Select value={premiumAccountId} onValueChange={setPremiumAccountId} disabled={!access.canEdit || !selectedPolicy || !selectedPolicy.cashFlowCategoryId}><SelectTrigger><SelectValue placeholder={selectedPolicy ? "اختر حساباً بنفس العملة" : "اختر البوليصة أولًا"} /></SelectTrigger><SelectContent>{eligiblePaymentAccounts.map(account => <SelectItem key={account.id} value={String(account.id)}>{account.name} — {money(account.balance, account.currency)}</SelectItem>)}</SelectContent></Select></div><div className="grid gap-2"><Label htmlFor="insurance-payment-amount">المبلغ المدفوع {selectedPolicy ? `(${selectedPolicy.currency})` : ""}</Label><Input id="insurance-payment-amount" value={paymentAmount} onChange={event => setPaymentAmount(event.target.value)} disabled={!access.canEdit || !selectedPolicy || !selectedPolicy.cashFlowCategoryId} inputMode="decimal" required /></div><div className="grid gap-2"><Label htmlFor="insurance-payment-memo">مذكرة الدفع</Label><Textarea id="insurance-payment-memo" value={paymentMemo} onChange={event => setPaymentMemo(event.target.value)} disabled={!access.canEdit || !selectedPolicy || !selectedPolicy.cashFlowCategoryId} maxLength={2000} placeholder="اختياري؛ اسم البوليصة يستخدم تلقائيًا عند تركه فارغًا" /></div><Button type="submit" disabled={postPremium.isPending || !access.canEdit || !selectedPolicy || !selectedPolicy.cashFlowCategoryId || !premiumAccountId}>{postPremium.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}{access.canEdit ? "نشر قسط التأمين" : "تتطلب صلاحية محرر"}</Button></form>}</CardContent></Card></section>
    <section className="grid gap-6 xl:grid-cols-[.8fr_1.2fr]"><Card><CardHeader><CardTitle>تسجيل مطالبة</CardTitle><CardDescription>تُحفظ المطالبة كسجل متابعة فقط. لا تصبح محصلة إلا عند ربطها بحركة دخل منشورة بنفس العملة.</CardDescription></CardHeader><CardContent><form onSubmit={submitClaim} className="grid gap-4"><div className="grid gap-2"><Label>البوليصة</Label><Select value={claimPolicyId} onValueChange={setClaimPolicyId} disabled={!access.canEdit}><SelectTrigger><SelectValue placeholder="اختر بوليصة نشطة" /></SelectTrigger><SelectContent>{policies.data?.filter(policy => policy.status === "active").map(policy => <SelectItem key={policy.id} value={String(policy.id)}>{policy.name} — {policy.currency}</SelectItem>)}</SelectContent></Select></div><div className="grid gap-3 sm:grid-cols-2"><div className="grid gap-2"><Label htmlFor="claim-amount">قيمة المطالبة</Label><Input id="claim-amount" value={claimAmount} onChange={event => setClaimAmount(event.target.value)} disabled={!access.canEdit} inputMode="decimal" required /></div><div className="grid gap-2"><Label htmlFor="claim-reference">المرجع</Label><Input id="claim-reference" value={claimReference} onChange={event => setClaimReference(event.target.value)} disabled={!access.canEdit} maxLength={160} /></div></div><div className="grid gap-3 sm:grid-cols-2"><div className="grid gap-2"><Label htmlFor="claim-date">تاريخ التقديم</Label><Input id="claim-date" type="date" value={claimDate} onChange={event => setClaimDate(event.target.value)} disabled={!access.canEdit} required /></div><div className="grid gap-2"><Label htmlFor="claim-expected-date">تاريخ متوقع</Label><Input id="claim-expected-date" type="date" value={claimExpectedDate} onChange={event => setClaimExpectedDate(event.target.value)} disabled={!access.canEdit} /></div></div><Textarea value={claimNote} onChange={event => setClaimNote(event.target.value)} disabled={!access.canEdit} maxLength={4000} placeholder="ملاحظات المطالبة أو رقم البلاغ" /><Button type="submit" disabled={createClaim.isPending || !access.canEdit || !claimPolicyId}>{createClaim.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}تسجيل المطالبة</Button></form></CardContent></Card><Card><CardHeader><CardTitle>سجل المطالبات</CardTitle><CardDescription>حالة كل مطالبة ومبلغها المستحق والمحصّل كما هو مرتبط بالدفتر.</CardDescription></CardHeader><CardContent>{claims.isLoading ? <Skeleton className="h-48" /> : claims.error ? <InlineError message={textError(claims.error)} /> : claims.data?.length ? <div className="space-y-3">{claims.data.map(claim => <div key={claim.id} className="rounded-xl border p-4"><div className="flex flex-wrap items-center justify-between gap-2"><div><p className="font-semibold">{claim.policyName}</p><p className="text-xs text-muted-foreground">{claim.referenceNumber || "بدون مرجع"} · {arabicDate(claim.submittedAt)}</p></div><Badge variant={claim.status === "paid" ? "secondary" : "outline"}>{claim.status === "paid" ? "مدفوعة" : "مقدمة"}</Badge></div><div className="mt-3 grid gap-2 text-sm sm:grid-cols-2"><span>المطالب به: <strong>{money(claim.claimedAmount, claim.currency)}</strong></span><span>المحصّل: <strong>{claim.receivedAmount ? money(claim.receivedAmount, claim.currency) : "لم يُحصّل"}</strong></span></div></div>)}</div> : <EmptyState icon={Umbrella} title="لا توجد مطالبات" description="أضف مطالبة من النموذج لتتبعها دون إنشاء دخل أو قيد تلقائي." />}</CardContent></Card></section>
  </div></DashboardLayout>;
}
export function GoalsPlanningPage() {
  const utils = trpc.useUtils();
  const access = useFamilyPermissions();
  const goals = trpc.family.goals.list.useQuery();
  const retirement = trpc.family.retirement.get.useQuery();
  const retirementProjection = trpc.family.retirement.projection.useQuery();
  const [name, setName] = useState(""); const [goalType, setGoalType] = useState<"emergency_fund" | "retirement" | "education" | "legacy" | "custom">("custom"); const [metric, setMetric] = useState<"net_worth" | "liquid_assets" | "investments">("liquid_assets"); const [targetAmount, setTargetAmount] = useState(""); const [targetDate, setTargetDate] = useState(""); const [priority, setPriority] = useState("3"); const [fundingSource, setFundingSource] = useState<"cash_flow" | "savings" | "investments" | "mixed" | "other">("cash_flow"); const [monthlyContribution, setMonthlyContribution] = useState("0"); const [assumedReturn, setAssumedReturn] = useState("0"); const [assumedInflation, setAssumedInflation] = useState("0");
  const [currentAge, setCurrentAge] = useState(""); const [retirementAge, setRetirementAge] = useState(""); const [retirementAssets, setRetirementAssets] = useState(""); const [retirementContribution, setRetirementContribution] = useState(""); const [annualSpending, setAnnualSpending] = useState(""); const [withdrawalRate, setWithdrawalRate] = useState("4"); const [retirementReturn, setRetirementReturn] = useState("0"); const [retirementInflation, setRetirementInflation] = useState("0");
  useEffect(() => { if (!retirement.data) return; setCurrentAge(String(retirement.data.currentAge)); setRetirementAge(String(retirement.data.retirementAge)); setRetirementAssets(retirement.data.currentRetirementAssets); setRetirementContribution(retirement.data.monthlyContribution); setAnnualSpending(retirement.data.annualSpending); setWithdrawalRate(retirement.data.safeWithdrawalRate); setRetirementReturn(retirement.data.assumedAnnualReturn); setRetirementInflation(retirement.data.assumedAnnualInflation); }, [retirement.data?.id]);
  const create = trpc.family.goals.create.useMutation({ onSuccess: () => { toast.success("تم حفظ الهدف بتمويل وافتراضات معلنة. تقدم الهدف يبقى مشتقًا من بيانات FAMILY الفعلية."); setName(""); setTargetAmount(""); void utils.family.goals.list.invalidate(); }, onError: error => toast.error(textError(error)) });
  const saveRetirement = trpc.family.retirement.upsert.useMutation({ onSuccess: () => { toast.success("تم حفظ خطة التقاعد. الإسقاط يعرض افتراضاته ولا يدّعي احتمال نجاح."); void utils.family.retirement.get.invalidate(); void utils.family.retirement.projection.invalidate(); }, onError: error => toast.error(textError(error)) });
  const submitGoal = (event: React.FormEvent) => { event.preventDefault(); create.mutate({ name, goalType, metric, targetAmount, targetDate: targetDate ? Date.parse(`${targetDate}T00:00:00.000Z`) : null, priority: Number(priority), fundingSource, monthlyContribution, assumedAnnualReturn: assumedReturn, assumedAnnualInflation: assumedInflation }); };
  const submitRetirement = (event: React.FormEvent) => { event.preventDefault(); saveRetirement.mutate({ currentAge: Number(currentAge), retirementAge: Number(retirementAge), currentRetirementAssets: retirementAssets, monthlyContribution: retirementContribution, annualSpending, safeWithdrawalRate: withdrawalRate, assumedAnnualReturn: retirementReturn, assumedAnnualInflation: retirementInflation }); };
  return <DashboardLayout><div dir="rtl" className="mx-auto max-w-7xl space-y-6"><header className="border-b pb-6"><p className="text-xs font-semibold uppercase tracking-[.15em] text-emerald-700">FAMILY / Deterministic planning</p><h1 className="mt-2 text-3xl font-bold">الأهداف والتقاعد والاستقلال المالي</h1><p className="mt-2 max-w-3xl text-sm text-slate-500">يحافظ FAMILY على التقدم الحالي المستمد من الدفتر، ثم يضيف إسقاطًا حتميًا يعتمد فقط على مساهمتك والعائد والتضخم ومعدل السحب التي تدخلها. لا يعرض احتمالات نجاح أو توصية شراء/بيع.</p></header><section className="grid gap-6 xl:grid-cols-[.85fr_1.15fr]"><Card><CardHeader><CardTitle>هدف مالي جديد</CardTitle><CardDescription>المساهمة والافتراضات اختيارية ولكنها تصبح جزءًا ظاهرًا من منهجية الإسقاط إذا حُدّد تاريخ مستهدف.</CardDescription></CardHeader><CardContent><form onSubmit={submitGoal} className="grid gap-4"><div className="grid gap-2"><Label htmlFor="planning-goal-name">اسم الهدف</Label><Input id="planning-goal-name" value={name} onChange={event => setName(event.target.value)} disabled={!access.canEdit} required minLength={2} /></div><div className="grid gap-2 sm:grid-cols-2"><div className="grid gap-2"><Label>النوع</Label><Select value={goalType} onValueChange={value => setGoalType(value as typeof goalType)} disabled={!access.canEdit}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{Object.entries(goalTypeLabel).map(([value, label]) => <SelectItem key={value} value={value}>{label}</SelectItem>)}</SelectContent></Select></div><div className="grid gap-2"><Label>المقياس الحالي</Label><Select value={metric} onValueChange={value => setMetric(value as typeof metric)} disabled={!access.canEdit}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{Object.entries(goalMetricLabel).map(([value, label]) => <SelectItem key={value} value={value}>{label}</SelectItem>)}</SelectContent></Select></div></div><div className="grid gap-2 sm:grid-cols-3"><div className="grid gap-2"><Label htmlFor="planning-goal-target">الهدف</Label><Input id="planning-goal-target" value={targetAmount} onChange={event => setTargetAmount(event.target.value)} disabled={!access.canEdit} inputMode="decimal" required /></div><div className="grid gap-2"><Label htmlFor="planning-goal-priority">الأولوية (1–5)</Label><Input id="planning-goal-priority" type="number" min="1" max="5" value={priority} onChange={event => setPriority(event.target.value)} disabled={!access.canEdit} required /></div><div className="grid gap-2"><Label htmlFor="planning-goal-date">التاريخ المستهدف</Label><Input id="planning-goal-date" type="date" value={targetDate} onChange={event => setTargetDate(event.target.value)} disabled={!access.canEdit} /></div></div><div className="grid gap-2"><Label>مصدر التمويل</Label><Select value={fundingSource} onValueChange={value => setFundingSource(value as typeof fundingSource)} disabled={!access.canEdit}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{Object.entries(goalFundingSourceLabel).map(([value, label]) => <SelectItem key={value} value={value}>{label}</SelectItem>)}</SelectContent></Select></div><div className="grid gap-2 sm:grid-cols-3"><div className="grid gap-2"><Label htmlFor="planning-goal-contribution">مساهمة شهرية</Label><Input id="planning-goal-contribution" value={monthlyContribution} onChange={event => setMonthlyContribution(event.target.value)} disabled={!access.canEdit} inputMode="decimal" required /></div><div className="grid gap-2"><Label htmlFor="planning-goal-return">عائد سنوي مفترض %</Label><Input id="planning-goal-return" value={assumedReturn} onChange={event => setAssumedReturn(event.target.value)} disabled={!access.canEdit} inputMode="decimal" required /></div><div className="grid gap-2"><Label htmlFor="planning-goal-inflation">تضخم سنوي مفترض %</Label><Input id="planning-goal-inflation" value={assumedInflation} onChange={event => setAssumedInflation(event.target.value)} disabled={!access.canEdit} inputMode="decimal" required /></div></div><Button type="submit" disabled={create.isPending || !access.canEdit}>{create.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}{access.canEdit ? "حفظ الهدف" : "تتطلب صلاحية محرر"}</Button></form></CardContent></Card><Card><CardHeader><CardTitle>تقدم وإسقاط الأهداف</CardTitle><CardDescription>القيمة الحالية والنسبة من الدفتر؛ القيم المستقبلية تتطلب تاريخًا وتستخدم الافتراضات المعروضة مع كل هدف.</CardDescription></CardHeader><CardContent>{goals.isLoading ? <Skeleton className="h-96" /> : goals.error ? <InlineError message={textError(goals.error)} /> : goals.data?.length ? <div className="space-y-4">{goals.data.map(goal => <div key={goal.id} className="rounded-xl border bg-white p-4"><div className="flex flex-col justify-between gap-3 sm:flex-row"><div><div className="flex flex-wrap items-center gap-2"><p className="font-semibold text-slate-900">{goal.name}</p><Badge variant="outline">أولوية {goal.priority}</Badge><Badge variant="secondary">{goal.progressPercent}% فعلي</Badge></div><p className="mt-2 text-xs text-slate-500">{goalTypeLabel[goal.goalType]} · مصدر التمويل: {goalFundingSourceLabel[goal.fundingSource]} · القيمة الحالية: {money(goal.currentAmount, goal.currency)} من {money(goal.targetAmount, goal.currency)}</p></div>{goal.projection.monthsToTarget ? <p className="text-sm font-semibold text-emerald-700">متوقع: {money(goal.projection.projectedAmount, goal.currency)}</p> : null}</div><div className="mt-4 h-2.5 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-emerald-600 transition-[width] duration-300" style={{ width: `${goal.progressPercent}%` }} /></div>{goal.projection.monthsToTarget ? <div className="mt-4 grid gap-2 text-xs text-slate-600 sm:grid-cols-3"><span>هدف بعد التضخم: {money(goal.projection.inflationAdjustedTargetAmount, goal.currency)}</span><span>فجوة متوقعة: {money(goal.projection.projectedGap, goal.currency)}</span><span>مساهمة لازمة: {money(goal.projection.requiredMonthlyContribution, goal.currency)}/شهر</span><span className="sm:col-span-3">الافتراضات: مساهمة {money(goal.monthlyContribution, goal.currency)} شهريًا، عائد {goal.assumedAnnualReturn}%، تضخم {goal.assumedAnnualInflation}%، وأفق {goal.projection.monthsToTarget} شهرًا.</span></div> : <p className="mt-3 text-xs text-slate-500">احفظ تاريخًا مستهدفًا لحساب الإسقاط؛ لا تخمن المنصة موعدًا من تلقاء نفسها.</p>}</div>)}</div> : <EmptyState icon={Target} title="لا توجد أهداف بعد" description="أنشئ هدفًا مرتبطًا بمقياس مالي حقيقي، ثم أضف تاريخًا ومساهمة وافتراضات معلنة إن أردت إسقاطًا حتميًا." />}</CardContent></Card></section><section className="grid gap-6 xl:grid-cols-[.85fr_1.15fr]"><Card><CardHeader><CardTitle>خطة تقاعد واستقلال مالي</CardTitle><CardDescription>تحسب قيمة الاستقلال من الإنفاق السنوي المتوقع عند التقاعد مقسومًا على معدل السحب المدخل. هذه ليست نصيحة أو احتمال نجاح.</CardDescription></CardHeader><CardContent><form onSubmit={submitRetirement} className="grid gap-4"><div className="grid gap-2 sm:grid-cols-2"><div className="grid gap-2"><Label htmlFor="retirement-current-age">العمر الحالي</Label><Input id="retirement-current-age" type="number" value={currentAge} onChange={event => setCurrentAge(event.target.value)} disabled={!access.canEdit} required /></div><div className="grid gap-2"><Label htmlFor="retirement-age">عمر التقاعد</Label><Input id="retirement-age" type="number" value={retirementAge} onChange={event => setRetirementAge(event.target.value)} disabled={!access.canEdit} required /></div></div><div className="grid gap-2 sm:grid-cols-2"><div className="grid gap-2"><Label htmlFor="retirement-assets">أصول التقاعد الحالية</Label><Input id="retirement-assets" value={retirementAssets} onChange={event => setRetirementAssets(event.target.value)} disabled={!access.canEdit} inputMode="decimal" required /></div><div className="grid gap-2"><Label htmlFor="retirement-contribution">مساهمة شهرية</Label><Input id="retirement-contribution" value={retirementContribution} onChange={event => setRetirementContribution(event.target.value)} disabled={!access.canEdit} inputMode="decimal" required /></div></div><div className="grid gap-2 sm:grid-cols-2"><div className="grid gap-2"><Label htmlFor="retirement-spending">إنفاق سنوي حالي</Label><Input id="retirement-spending" value={annualSpending} onChange={event => setAnnualSpending(event.target.value)} disabled={!access.canEdit} inputMode="decimal" required /></div><div className="grid gap-2"><Label htmlFor="retirement-withdrawal">معدل السحب %</Label><Input id="retirement-withdrawal" value={withdrawalRate} onChange={event => setWithdrawalRate(event.target.value)} disabled={!access.canEdit} inputMode="decimal" required /></div></div><div className="grid gap-2 sm:grid-cols-2"><div className="grid gap-2"><Label htmlFor="retirement-return">عائد سنوي مفترض %</Label><Input id="retirement-return" value={retirementReturn} onChange={event => setRetirementReturn(event.target.value)} disabled={!access.canEdit} inputMode="decimal" required /></div><div className="grid gap-2"><Label htmlFor="retirement-inflation">تضخم سنوي مفترض %</Label><Input id="retirement-inflation" value={retirementInflation} onChange={event => setRetirementInflation(event.target.value)} disabled={!access.canEdit} inputMode="decimal" required /></div></div><Button type="submit" disabled={saveRetirement.isPending || !access.canEdit}>{saveRetirement.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}{access.canEdit ? "حفظ خطة التقاعد" : "تتطلب صلاحية محرر"}</Button></form></CardContent></Card><Card><CardHeader><CardTitle>نتيجة السيناريو الحتمي</CardTitle><CardDescription>النتيجة تعرض القيم الاسمية المتوقعة وفق المعطيات المدخلة ولا تستخدم سعر سوق أو توزيع احتمالي.</CardDescription></CardHeader><CardContent>{retirementProjection.isLoading ? <Skeleton className="h-64" /> : retirementProjection.error ? <InlineError message={textError(retirementProjection.error)} /> : retirementProjection.data ? <div className="grid gap-3 sm:grid-cols-2"><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs text-slate-500">سنوات حتى التقاعد</p><p className="mt-1 text-xl font-semibold">{retirementProjection.data.projection.yearsToRetirement}</p></div><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs text-slate-500">أصول متوقعة عند التقاعد</p><p className="mt-1 text-xl font-semibold">{money(retirementProjection.data.projection.projectedAssetsAtRetirement, retirementProjection.data.plan.currency)}</p></div><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs text-slate-500">هدف الاستقلال المالي</p><p className="mt-1 text-xl font-semibold">{money(retirementProjection.data.projection.financialIndependenceTarget, retirementProjection.data.plan.currency)}</p></div><div className="rounded-xl bg-amber-50 p-4"><p className="text-xs text-amber-800">فجوة الاستقلال المالي</p><p className="mt-1 text-xl font-semibold text-amber-900">{money(retirementProjection.data.projection.gapToFinancialIndependence, retirementProjection.data.plan.currency)}</p></div><p className="text-xs leading-6 text-slate-500 sm:col-span-2">الافتراضات: عائد سنوي {retirementProjection.data.projection.nominalAnnualReturn}%، تضخم سنوي {retirementProjection.data.projection.annualInflation}%، ومعدل سحب {retirementProjection.data.projection.safeWithdrawalRate}%. لا تعبر هذه النتيجة عن احتمال نجاح أو توصية استثمار.</p></div> : <EmptyState icon={Target} title="أدخل خطة تقاعد" description="بعد حفظ العمر والأصول والمساهمة والإنفاق والافتراضات، تعرض المنصة فجوة حتمية قابلة للمراجعة." />}</CardContent></Card></section></div></DashboardLayout>;
}

function currentPeriodKey() {
  return new Intl.DateTimeFormat("en-CA", { year: "numeric", month: "2-digit", timeZone: "UTC" }).format(new Date());
}

export function CashFlowPage() {
  const utils = trpc.useUtils();
  const access = useFamilyPermissions();
  const [periodKey, setPeriodKey] = useState(currentPeriodKey);
  const [categoryName, setCategoryName] = useState("");
  const [categoryDirection, setCategoryDirection] = useState<"income" | "expense">("expense");
  const [budgetCategoryId, setBudgetCategoryId] = useState("");
  const [budgetAmount, setBudgetAmount] = useState("");
  const categories = trpc.family.cashFlow.categories.useQuery();
  const summary = trpc.family.cashFlow.summary.useQuery({ periodKey });
  const createCategory = trpc.family.cashFlow.createCategory.useMutation({ onSuccess: () => { toast.success("تمت إضافة الفئة المالية ضمن نطاق FAMILY."); setCategoryName(""); void utils.family.cashFlow.categories.invalidate(); }, onError: error => toast.error(textError(error)) });
  const upsertBudget = trpc.family.cashFlow.upsertBudget.useMutation({ onSuccess: () => { toast.success("تم حفظ الخطة الشهرية وربطها بالفئة."); setBudgetAmount(""); void utils.family.cashFlow.summary.invalidate(); }, onError: error => toast.error(textError(error)) });
  const addCategory = (event: React.FormEvent) => { event.preventDefault(); createCategory.mutate({ name: categoryName, direction: categoryDirection, color: categoryDirection === "income" ? "#047857" : "#b45309" }); };
  const saveBudget = (event: React.FormEvent) => { event.preventDefault(); if (!budgetCategoryId) return toast.error("اختر فئة للميزانية."); upsertBudget.mutate({ categoryId: Number(budgetCategoryId), periodKey, plannedAmountBase: budgetAmount }); };
  return <DashboardLayout><div dir="rtl" className="mx-auto max-w-7xl space-y-6"><header className="flex flex-col justify-between gap-4 border-b pb-6 sm:flex-row sm:items-end"><div><p className="text-xs font-semibold uppercase tracking-[.15em] text-emerald-700">FAMILY / Cash flow control</p><h1 className="mt-2 text-3xl font-bold">التدفق النقدي والميزانية</h1><p className="mt-2 max-w-3xl text-sm text-slate-500">المقارنة هنا تعتمد على مبالغ الأحداث المنشورة والقيم الأساسية في دفتر القيود. لن يظهر فعلي أو مخطط بلا بيانات مسجلة.</p></div><Input aria-label="الفترة الشهرية" type="month" value={periodKey} onChange={event => setPeriodKey(event.target.value)} className="w-full sm:w-44" /></header><section className="grid gap-4 sm:grid-cols-3"><Card><CardHeader className="pb-3"><CardDescription>دخل فعلي</CardDescription><CardTitle className="text-2xl text-emerald-700">{money(summary.data?.incomeActualBase, summary.data?.baseCurrency || "EGP")}</CardTitle></CardHeader><CardContent className="text-xs text-slate-500">مصنف ومقيد خلال الفترة</CardContent></Card><Card><CardHeader className="pb-3"><CardDescription>مصروف فعلي</CardDescription><CardTitle className="text-2xl text-amber-700">{money(summary.data?.expenseActualBase, summary.data?.baseCurrency || "EGP")}</CardTitle></CardHeader><CardContent className="text-xs text-slate-500">يُقارن بالخطة للفئات التي لها ميزانية</CardContent></Card><Card><CardHeader className="pb-3"><CardDescription>صافي التدفق</CardDescription><CardTitle className="text-2xl">{money(summary.data?.netCashFlowBase, summary.data?.baseCurrency || "EGP")}</CardTitle></CardHeader><CardContent className="text-xs text-slate-500">الدخل الفعلي ناقص المصروف الفعلي</CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[.8fr_1.2fr]"><Card><CardHeader><CardTitle>إعداد الفئات والخطة</CardTitle><CardDescription>الفئات تحدد اتجاه العملية، والخطة تحفظ بعملة الأساس للفترة المختارة.</CardDescription></CardHeader><CardContent className="space-y-6"><form onSubmit={addCategory} className="grid gap-3 rounded-xl border bg-slate-50 p-4"><Label htmlFor="category-name">فئة جديدة</Label><div className="flex gap-2"><Input id="category-name" value={categoryName} onChange={event => setCategoryName(event.target.value)} disabled={!access.canEdit} placeholder="مثال: إيجار أو راتب" required /><Select value={categoryDirection} onValueChange={value => setCategoryDirection(value as typeof categoryDirection)} disabled={!access.canEdit}><SelectTrigger className="w-32"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="income">دخل</SelectItem><SelectItem value="expense">مصروف</SelectItem></SelectContent></Select></div><Button type="submit" disabled={createCategory.isPending || !access.canEdit}>{createCategory.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}{access.canEdit ? "إضافة فئة" : "تتطلب صلاحية محرر"}</Button></form><form onSubmit={saveBudget} className="grid gap-3"><Label>خطة للفترة {periodKey}</Label><Select value={budgetCategoryId} onValueChange={setBudgetCategoryId} disabled={!access.canEdit}><SelectTrigger><SelectValue placeholder="اختر فئة" /></SelectTrigger><SelectContent>{categories.data?.map(category => <SelectItem value={String(category.id)} key={category.id}>{category.direction === "income" ? "دخل" : "مصروف"} · {category.name}</SelectItem>)}</SelectContent></Select><Input value={budgetAmount} onChange={event => setBudgetAmount(event.target.value)} disabled={!access.canEdit} inputMode="decimal" placeholder={`المبلغ بعملة الأساس (${summary.data?.baseCurrency || "EGP"})`} required /><Button type="submit" variant="outline" disabled={upsertBudget.isPending || !access.canEdit}>{upsertBudget.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}حفظ الخطة</Button></form></CardContent></Card><Card><CardHeader><CardTitle>فعلي مقابل مخطط</CardTitle><CardDescription>يتوسع الجدول فقط عند وجود أحداث مصنفة أو خطة لفئة في الفترة المختارة.</CardDescription></CardHeader><CardContent>{summary.isLoading ? <Skeleton className="h-60" /> : summary.error ? <InlineError message={textError(summary.error)} /> : summary.data?.categories.length ? <div className="overflow-x-auto"><table className="w-full min-w-[620px] text-right text-sm"><thead className="border-b text-xs text-slate-500"><tr><th className="pb-3">الفئة</th><th className="pb-3">الاتجاه</th><th className="pb-3">فعلي</th><th className="pb-3">مخطط</th><th className="pb-3">الفرق</th></tr></thead><tbody className="divide-y">{summary.data.categories.map(row => <tr key={row.categoryId}><td className="py-4 font-medium">{row.categoryName}</td><td className="py-4"><Badge variant={row.direction === "income" ? "secondary" : "outline"}>{row.direction === "income" ? "دخل" : "مصروف"}</Badge></td><td className="py-4">{money(row.actualAmountBase, summary.data.baseCurrency)}</td><td className="py-4">{money(row.plannedAmountBase, summary.data.baseCurrency)}</td><td className={`py-4 font-semibold ${Number(row.actualAmountBase) > Number(row.plannedAmountBase) && row.direction === "expense" ? "text-red-700" : "text-emerald-700"}`}>{money(String(Number(row.plannedAmountBase) - Number(row.actualAmountBase)), summary.data.baseCurrency)}</td></tr>)}</tbody></table></div> : <EmptyState icon={WalletCards} title="لا توجد بيانات تدفق مصنفة لهذه الفترة" description="أنشئ فئة، ثم اخترها عند تسجيل دخل أو مصروف، وحدد خطة شهرية عند الحاجة." />}</CardContent></Card></section></div></DashboardLayout>;
}

export function CashFlowRegisterPage() {
  const utils = trpc.useUtils();
  const access = useFamilyPermissions();
  const accounts = trpc.family.accounts.list.useQuery();
  const categories = trpc.family.cashFlow.categories.useQuery();
  const [eventType, setEventType] = useState<"income" | "expense">("expense");
  const [accountId, setAccountId] = useState("");
  const [categoryId, setCategoryId] = useState("");
  const [amount, setAmount] = useState("");
  const [memo, setMemo] = useState("");
  const post = trpc.family.ledger.postCash.useMutation({ onSuccess: () => { toast.success("تم نشر التدفق وتصنيفه في الدفتر."); setAmount(""); setMemo(""); void utils.family.ledger.recent.invalidate(); void utils.family.dashboard.invalidate(); void utils.family.cashFlow.summary.invalidate(); }, onError: error => toast.error(textError(error)) });
  const eligibleCategories = (categories.data ?? []).filter(category => category.direction === eventType);
  const selectedAccount = accounts.data?.find(account => String(account.id) === accountId);
  const submit = (event: React.FormEvent) => { event.preventDefault(); if (!selectedAccount || !categoryId) return toast.error("اختر الحساب وفئة التدفق."); post.mutate({ eventType, accountId: selectedAccount.id, categoryId: Number(categoryId), amount, currency: selectedAccount.currency, occurredAt: Date.now(), memo: memo || null, idempotencyKey: crypto.randomUUID() }); };
  return <DashboardLayout><div dir="rtl" className="mx-auto max-w-3xl space-y-6"><header className="border-b pb-6"><p className="text-xs font-semibold uppercase tracking-[.15em] text-emerald-700">FAMILY / Categorized cash flow</p><h1 className="mt-2 text-3xl font-bold">تسجيل دخل أو مصروف مصنف</h1><p className="mt-2 text-sm text-slate-500">ستغذي هذه العملية دفتر القيود والحساب والتدفق النقدي الفعلي ومقارنة الميزانية، ولا تعدّل أي رصيد مباشرة.</p></header><Card><CardHeader><CardTitle>عملية تدفق جديدة</CardTitle><CardDescription>اختر الفئة أولًا؛ لا تقبل المنصة فئة دخل لمصروف أو فئة مصروف لدخل.</CardDescription></CardHeader><CardContent>{accounts.isLoading || categories.isLoading ? <Skeleton className="h-72" /> : <form onSubmit={submit} className="grid gap-5"><div className="grid gap-2"><Label>الاتجاه</Label><Select value={eventType} onValueChange={value => { setEventType(value as typeof eventType); setCategoryId(""); }} disabled={!access.canEdit}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="income">دخل</SelectItem><SelectItem value="expense">مصروف</SelectItem></SelectContent></Select></div><div className="grid gap-2"><Label>الحساب</Label><Select value={accountId} onValueChange={setAccountId} disabled={!access.canEdit}><SelectTrigger><SelectValue placeholder="اختر حسابًا" /></SelectTrigger><SelectContent>{accounts.data?.filter(account => ["cash", "bank", "brokerage", "wallet"].includes(account.accountType)).map(account => <SelectItem key={account.id} value={String(account.id)}>{account.name} — {account.currency}</SelectItem>)}</SelectContent></Select></div><div className="grid gap-2"><Label>الفئة</Label><Select value={categoryId} onValueChange={setCategoryId} disabled={!access.canEdit}><SelectTrigger><SelectValue placeholder={`اختر فئة ${eventType === "income" ? "دخل" : "مصروف"}`} /></SelectTrigger><SelectContent>{eligibleCategories.map(category => <SelectItem key={category.id} value={String(category.id)}>{category.name}</SelectItem>)}</SelectContent></Select></div><div className="grid gap-2"><Label htmlFor="classified-flow-amount">المبلغ {selectedAccount ? `(${selectedAccount.currency})` : ""}</Label><Input id="classified-flow-amount" value={amount} onChange={event => setAmount(event.target.value)} disabled={!access.canEdit} inputMode="decimal" required /></div><div className="grid gap-2"><Label htmlFor="classified-flow-memo">مذكرة</Label><Textarea id="classified-flow-memo" value={memo} onChange={event => setMemo(event.target.value)} disabled={!access.canEdit} maxLength={2000} placeholder="اختياري" /></div><Button type="submit" disabled={post.isPending || !access.canEdit}>{post.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}{access.canEdit ? "نشر التدفق المصنف" : "تتطلب صلاحية محرر"}</Button></form>}</CardContent></Card></div></DashboardLayout>;
}

const recurringCadenceLabel: Record<string, string> = { weekly: "أسبوعي", monthly: "شهري", quarterly: "ربع سنوي", yearly: "سنوي" };

export function RecurringRulesPage() {
  const utils = trpc.useUtils();
  const access = useFamilyPermissions();
  const accounts = trpc.family.accounts.list.useQuery();
  const categories = trpc.family.cashFlow.categories.useQuery();
  const rules = trpc.family.cashFlow.recurring.list.useQuery();
  const [eventType, setEventType] = useState<"income" | "expense" | "deposit" | "withdrawal">("expense");
  const [accountId, setAccountId] = useState("");
  const [categoryId, setCategoryId] = useState("");
  const [amount, setAmount] = useState("");
  const [cadence, setCadence] = useState<"weekly" | "monthly" | "quarterly" | "yearly">("monthly");
  const [nextRunAt, setNextRunAt] = useState("");
  const [endsAt, setEndsAt] = useState("");
  const [memo, setMemo] = useState("");
  const create = trpc.family.cashFlow.recurring.create.useMutation({
    onSuccess: () => { toast.success("تمت جدولة القاعدة وتفعيلها. ستُنشر العملية عند موعدها بقيد متوازن ومصدر نظامي قابل للتدقيق."); setAmount(""); setMemo(""); setEndsAt(""); void utils.family.cashFlow.recurring.list.invalidate(); },
    onError: error => toast.error(textError(error)),
  });
  const pause = trpc.family.cashFlow.recurring.pause.useMutation({ onSuccess: () => { toast.success("تم إيقاف القاعدة المتكررة."); void utils.family.cashFlow.recurring.list.invalidate(); }, onError: error => toast.error(textError(error)) });
  const resume = trpc.family.cashFlow.recurring.resume.useMutation({ onSuccess: () => { toast.success("تم استئناف القاعدة المتكررة."); void utils.family.cashFlow.recurring.list.invalidate(); }, onError: error => toast.error(textError(error)) });
  const selectedAccount = accounts.data?.find(account => String(account.id) === accountId);
  const requiresCategory = eventType === "income" || eventType === "expense";
  const eligibleCategories = (categories.data ?? []).filter(category => category.direction === eventType);
  const submit = (event: React.FormEvent) => {
    event.preventDefault();
    if (!selectedAccount || !nextRunAt) return toast.error("اختر الحساب وموعد أول تشغيل.");
    if (requiresCategory && !categoryId) return toast.error("اختر فئة تدفق متوافقة مع نوع العملية.");
    create.mutate({ accountId: selectedAccount.id, categoryId: categoryId ? Number(categoryId) : null, eventType, amount, currency: selectedAccount.currency, cadence, nextRunAt: new Date(nextRunAt).getTime(), endsAt: endsAt ? new Date(endsAt).getTime() : null, memo: memo || null });
  };
  return <DashboardLayout><div dir="rtl" className="mx-auto max-w-7xl space-y-6"><header className="flex flex-col justify-between gap-4 border-b pb-6 sm:flex-row sm:items-end"><div><p className="text-xs font-semibold uppercase tracking-[.15em] text-emerald-700">FAMILY / Scheduled cash flow</p><h1 className="mt-2 text-3xl font-bold">المعاملات المتكررة</h1><p className="mt-2 max-w-3xl text-sm text-slate-500">كل قاعدة تنشئ مهمة دورية موثقة. عند الاستحقاق تستخدم المنصة مفتاح منع تكرار ثابتًا وتنشر العملية عبر دفتر القيود نفسه؛ لا توجد أرصدة معدّلة مباشرة.</p></div><Badge variant="outline" className="w-fit gap-2"><CalendarClock className="size-4" />الجدولة بتوقيت UTC</Badge></header><section className="grid gap-6 lg:grid-cols-[.85fr_1.15fr]"><Card><CardHeader><CardTitle>قاعدة جديدة</CardTitle><CardDescription>حدد أول موعد وتيرة التنفيذ. يبقى تاريخ العملية هو الموعد المجدول حتى لو أُعيدت المحاولة لاحقًا.</CardDescription></CardHeader><CardContent>{accounts.isLoading || categories.isLoading ? <Skeleton className="h-[36rem]" /> : <form onSubmit={submit} className="grid gap-4"><div className="grid gap-2"><Label>نوع العملية</Label><Select value={eventType} onValueChange={value => { setEventType(value as typeof eventType); setCategoryId(""); }} disabled={!access.canEdit}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="income">دخل</SelectItem><SelectItem value="expense">مصروف</SelectItem><SelectItem value="deposit">إيداع</SelectItem><SelectItem value="withdrawal">سحب</SelectItem></SelectContent></Select></div><div className="grid gap-2"><Label>الحساب</Label><Select value={accountId} onValueChange={setAccountId} disabled={!access.canEdit}><SelectTrigger><SelectValue placeholder="اختر حسابًا نقديًا أو مصرفيًا" /></SelectTrigger><SelectContent>{accounts.data?.filter(account => ["cash", "bank", "brokerage", "wallet"].includes(account.accountType)).map(account => <SelectItem key={account.id} value={String(account.id)}>{account.name} — {account.currency}</SelectItem>)}</SelectContent></Select></div>{requiresCategory && <div className="grid gap-2"><Label>الفئة</Label><Select value={categoryId || undefined} onValueChange={setCategoryId} disabled={!access.canEdit}><SelectTrigger><SelectValue placeholder="اختر فئة متوافقة" /></SelectTrigger><SelectContent>{eligibleCategories.map(category => <SelectItem key={category.id} value={String(category.id)}>{category.name}</SelectItem>)}</SelectContent></Select></div>}<div className="grid gap-2"><Label htmlFor="recurring-amount">المبلغ {selectedAccount ? `(${selectedAccount.currency})` : ""}</Label><Input id="recurring-amount" value={amount} onChange={event => setAmount(event.target.value)} disabled={!access.canEdit} inputMode="decimal" required /></div><div className="grid gap-2"><Label>الوتيرة</Label><Select value={cadence} onValueChange={value => setCadence(value as typeof cadence)} disabled={!access.canEdit}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{Object.entries(recurringCadenceLabel).map(([value, label]) => <SelectItem value={value} key={value}>{label}</SelectItem>)}</SelectContent></Select></div><div className="grid gap-2"><Label htmlFor="recurring-next-run">أول موعد تشغيل</Label><Input id="recurring-next-run" type="datetime-local" value={nextRunAt} onChange={event => setNextRunAt(event.target.value)} disabled={!access.canEdit} required /></div><div className="grid gap-2"><Label htmlFor="recurring-ends">ينتهي في</Label><Input id="recurring-ends" type="datetime-local" value={endsAt} onChange={event => setEndsAt(event.target.value)} disabled={!access.canEdit} /></div><div className="grid gap-2"><Label htmlFor="recurring-memo">مذكرة</Label><Textarea id="recurring-memo" value={memo} onChange={event => setMemo(event.target.value)} disabled={!access.canEdit} maxLength={2000} placeholder="اختياري" /></div><Button type="submit" disabled={create.isPending || !access.canEdit}>{create.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}{access.canEdit ? "إنشاء وتفعيل القاعدة" : "تتطلب صلاحية محرر"}</Button></form>}</CardContent></Card><Card><CardHeader><CardTitle>القواعد ضمن مساحة FAMILY الحالية</CardTitle><CardDescription>الإيقاف والاستئناف يحدّثان المهمة المنشورة والحالة المحلية معًا، ولا يحذفا أثر القواعد السابقة.</CardDescription></CardHeader><CardContent>{rules.isLoading ? <Skeleton className="h-80" /> : rules.error ? <InlineError message={textError(rules.error)} /> : rules.data?.length ? <div className="space-y-3">{rules.data.map(rule => <div className="rounded-xl border bg-white p-4" key={rule.id}><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-start"><div><div className="flex flex-wrap items-center gap-2"><p className="font-semibold text-slate-900">{eventLabel[rule.eventType]} · {money(rule.amount, rule.currency)}</p><Badge variant={rule.status === "active" ? "secondary" : "outline"}>{rule.status === "active" ? "نشطة" : rule.status === "paused" ? "موقوفة" : "مكتملة"}</Badge></div><p className="mt-2 text-sm text-slate-600">{rule.accountName}{rule.categoryName ? ` · ${rule.categoryName}` : ""} · {recurringCadenceLabel[rule.cadence]}</p><p className="mt-1 text-xs text-slate-500">التشغيل التالي: {dateTime(rule.nextRunAt)}{rule.endsAt ? ` · ينتهي: ${dateTime(rule.endsAt)}` : ""}</p>{rule.memo && <p className="mt-3 rounded-lg bg-slate-50 p-3 text-xs text-slate-600">{rule.memo}</p>}</div>{access.canEdit && <div className="flex shrink-0 gap-2">{rule.status === "active" && <Button type="button" size="sm" variant="outline" onClick={() => pause.mutate({ ruleId: rule.id })} disabled={pause.isPending}>إيقاف</Button>}{rule.status === "paused" && <Button type="button" size="sm" onClick={() => resume.mutate({ ruleId: rule.id })} disabled={resume.isPending}>استئناف</Button>}</div>}</div></div>)}</div> : <EmptyState icon={CalendarClock} title="لا توجد قواعد متكررة" description="أنشئ قاعدة لإيجار أو دخل منتظم أو إيداع دوري. لا تُنشأ أي عملية حتى يحين الموعد المحدد." />}</CardContent></Card></section></div></DashboardLayout>;
}

const debtTypeLabel: Record<string, string> = { loan: "قرض", credit_card: "بطاقة ائتمان", mortgage: "تمويل عقاري", personal: "دين شخصي", other: "التزام آخر" };

export function DebtsPage() {
  const utils = trpc.useUtils();
  const access = useFamilyPermissions();
  const debts = trpc.family.debts.list.useQuery();
  const accounts = trpc.family.accounts.list.useQuery();
  const categories = trpc.family.cashFlow.categories.useQuery();
  const [name, setName] = useState(""); const [lender, setLender] = useState(""); const [debtType, setDebtType] = useState<"loan" | "credit_card" | "mortgage" | "personal" | "other">("loan"); const [principal, setPrincipal] = useState(""); const [currency, setCurrency] = useState("EGP"); const [rate, setRate] = useState("0"); const [minimumPayment, setMinimumPayment] = useState(""); const [paymentDay, setPaymentDay] = useState(""); const [startDate, setStartDate] = useState(""); const [maturityDate, setMaturityDate] = useState(""); const [fundingAccountId, setFundingAccountId] = useState(""); const [categoryId, setCategoryId] = useState(""); const [memo, setMemo] = useState("");
  const [paymentDebtId, setPaymentDebtId] = useState(""); const [paymentAccountId, setPaymentAccountId] = useState(""); const [paymentPrincipal, setPaymentPrincipal] = useState(""); const [paymentInterest, setPaymentInterest] = useState("0"); const [paymentFee, setPaymentFee] = useState("0"); const [paymentMemo, setPaymentMemo] = useState(""); const [projectionDebtId, setProjectionDebtId] = useState(""); const [extraPayment, setExtraPayment] = useState("");
  const projectionInput = useMemo(() => projectionDebtId ? { debtId: Number(projectionDebtId), horizonMonths: 120, extraPrincipal: extraPayment || undefined } : undefined, [projectionDebtId, extraPayment]);
  const projection = trpc.family.debts.projection.useQuery(projectionInput!, { enabled: Boolean(projectionInput) });
  const create = trpc.family.debts.create.useMutation({ onSuccess: () => { toast.success("تم إنشاء الالتزام وقيد رصيده الافتتاحي في دفتر الأستاذ."); setName(""); setLender(""); setPrincipal(""); setMinimumPayment(""); setMemo(""); void utils.family.debts.list.invalidate(); void utils.family.dashboard.invalidate(); void utils.family.accounts.list.invalidate(); }, onError: error => toast.error(textError(error)) });
  const postPayment = trpc.family.debts.postPayment.useMutation({ onSuccess: () => { toast.success("تم ترحيل سداد الدين: أصل وفائدة ورسوم ضمن قيد متوازن."); setPaymentPrincipal(""); setPaymentInterest("0"); setPaymentFee("0"); setPaymentMemo(""); void utils.family.debts.list.invalidate(); void utils.family.dashboard.invalidate(); void utils.family.accounts.list.invalidate(); void utils.family.ledger.recent.invalidate(); }, onError: error => toast.error(textError(error)) });
  const cashAccounts = (accounts.data ?? []).filter(account => ["cash", "bank", "brokerage", "wallet"].includes(account.accountType));
  const expenseCategories = (categories.data ?? []).filter(category => category.direction === "expense");
  const selectedDebt = debts.data?.find(debt => String(debt.id) === paymentDebtId);
  const createDebt = (event: React.FormEvent) => { event.preventDefault(); if (!startDate) return toast.error("حدد تاريخ بداية الالتزام."); create.mutate({ name, lender: lender || null, debtType, originalPrincipal: principal, currency, annualInterestRate: rate, minimumPayment, paymentDay: paymentDay ? Number(paymentDay) : null, startDate: Date.parse(`${startDate}T00:00:00.000Z`), maturityDate: maturityDate ? Date.parse(`${maturityDate}T00:00:00.000Z`) : null, cashAccountId: fundingAccountId ? Number(fundingAccountId) : null, cashFlowCategoryId: categoryId ? Number(categoryId) : null, memo: memo || null, idempotencyKey: crypto.randomUUID() }); };
  const submitPayment = (event: React.FormEvent) => { event.preventDefault(); if (!selectedDebt || !paymentAccountId) return toast.error("اختر الدين وحساب السداد."); postPayment.mutate({ debtId: selectedDebt.id, cashAccountId: Number(paymentAccountId), principalAmount: paymentPrincipal, interestAmount: paymentInterest || null, feeAmount: paymentFee || null, occurredAt: Date.now(), memo: paymentMemo || null, idempotencyKey: crypto.randomUUID() }); };
  return <DashboardLayout><div dir="rtl" className="mx-auto max-w-7xl space-y-6"><header className="border-b pb-6"><p className="text-xs font-semibold uppercase tracking-[.15em] text-emerald-700">FAMILY / Liability control</p><h1 className="mt-2 text-3xl font-bold">الديون والالتزامات</h1><p className="mt-2 max-w-3xl text-sm text-slate-500">الرصيد المستحق معروض من الحساب الالتزامي المقيد، لا من حقل قابل للتعديل. يحسب النموذج جدول التكلفة من معدل الفائدة والقسط المدخلين، ولا يقارن بعوائد استثمارية مفترضة.</p></header><section className="grid gap-6 xl:grid-cols-[.9fr_1.1fr]"><Card><CardHeader><CardTitle>إضافة دين أو بطاقة</CardTitle><CardDescription>إن كان الدين قائمًا قبل استخدام FAMILY، اترك حساب صرف القيمة فارغًا ليُسجل كرصد افتتاحي مقابل حقوق الملكية. إذا صُرفت قيمته الآن، اختر الحساب الذي استلمها.</CardDescription></CardHeader><CardContent><form onSubmit={createDebt} className="grid gap-4"><div className="grid gap-2"><Label htmlFor="debt-name">الاسم</Label><Input id="debt-name" value={name} onChange={event => setName(event.target.value)} disabled={!access.canEdit} required /></div><div className="grid gap-2 sm:grid-cols-2"><div className="grid gap-2"><Label htmlFor="debt-lender">المقرض / الجهة</Label><Input id="debt-lender" value={lender} onChange={event => setLender(event.target.value)} disabled={!access.canEdit} /></div><div className="grid gap-2"><Label>النوع</Label><Select value={debtType} onValueChange={value => setDebtType(value as typeof debtType)} disabled={!access.canEdit}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{Object.entries(debtTypeLabel).map(([value, label]) => <SelectItem key={value} value={value}>{label}</SelectItem>)}</SelectContent></Select></div></div><div className="grid gap-2 sm:grid-cols-2"><div className="grid gap-2"><Label htmlFor="debt-principal">الأصل</Label><Input id="debt-principal" value={principal} onChange={event => setPrincipal(event.target.value)} inputMode="decimal" disabled={!access.canEdit} required /></div><div className="grid gap-2"><Label htmlFor="debt-currency">العملة</Label><Input id="debt-currency" value={currency} onChange={event => setCurrency(event.target.value.toUpperCase())} maxLength={3} disabled={!access.canEdit} required /></div></div><div className="grid gap-2 sm:grid-cols-3"><div className="grid gap-2"><Label htmlFor="debt-rate">فائدة سنوية %</Label><Input id="debt-rate" value={rate} onChange={event => setRate(event.target.value)} inputMode="decimal" disabled={!access.canEdit} required /></div><div className="grid gap-2"><Label htmlFor="debt-minimum">القسط الأدنى</Label><Input id="debt-minimum" value={minimumPayment} onChange={event => setMinimumPayment(event.target.value)} inputMode="decimal" disabled={!access.canEdit} required /></div><div className="grid gap-2"><Label htmlFor="debt-day">يوم الاستحقاق</Label><Input id="debt-day" value={paymentDay} onChange={event => setPaymentDay(event.target.value)} inputMode="numeric" min="1" max="31" disabled={!access.canEdit} /></div></div><div className="grid gap-2 sm:grid-cols-2"><div className="grid gap-2"><Label htmlFor="debt-start">تاريخ البداية</Label><Input id="debt-start" type="date" value={startDate} onChange={event => setStartDate(event.target.value)} disabled={!access.canEdit} required /></div><div className="grid gap-2"><Label htmlFor="debt-maturity">الاستحقاق النهائي</Label><Input id="debt-maturity" type="date" value={maturityDate} onChange={event => setMaturityDate(event.target.value)} disabled={!access.canEdit} /></div></div><div className="grid gap-2"><Label>حساب صرف قيمة الدين</Label><Select value={fundingAccountId || undefined} onValueChange={setFundingAccountId} disabled={!access.canEdit}><SelectTrigger><SelectValue placeholder="اختياري لدين قائم سابق" /></SelectTrigger><SelectContent>{cashAccounts.map(account => <SelectItem value={String(account.id)} key={account.id}>{account.name} — {account.currency}</SelectItem>)}</SelectContent></Select></div><div className="grid gap-2"><Label>فئة خدمة الدين</Label><Select value={categoryId || undefined} onValueChange={setCategoryId} disabled={!access.canEdit}><SelectTrigger><SelectValue placeholder="اختياري؛ فئة مصروف" /></SelectTrigger><SelectContent>{expenseCategories.map(category => <SelectItem value={String(category.id)} key={category.id}>{category.name}</SelectItem>)}</SelectContent></Select></div><div className="grid gap-2"><Label htmlFor="debt-memo">مذكرة</Label><Textarea id="debt-memo" value={memo} onChange={event => setMemo(event.target.value)} disabled={!access.canEdit} maxLength={2000} /></div><Button type="submit" disabled={create.isPending || !access.canEdit}>{create.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}{access.canEdit ? "تسجيل الالتزام" : "تتطلب صلاحية محرر"}</Button></form></CardContent></Card><Card><CardHeader><CardTitle>الرصيد والجدولة</CardTitle><CardDescription>ينشأ سداد فعلي فقط عبر نموذج السداد أدناه. هذه القائمة لا تتيح تعديل الرصيد يدويًا.</CardDescription></CardHeader><CardContent>{debts.isLoading ? <Skeleton className="h-96" /> : debts.error ? <InlineError message={textError(debts.error)} /> : debts.data?.length ? <div className="space-y-3">{debts.data.map(debt => <div key={debt.id} className="rounded-xl border bg-white p-4"><div className="flex flex-col justify-between gap-3 sm:flex-row"><div><div className="flex flex-wrap items-center gap-2"><p className="font-semibold text-slate-900">{debt.name}</p><Badge variant={debt.status === "active" ? "secondary" : "outline"}>{debt.status === "active" ? "نشط" : debt.status === "paid" ? "مسدد" : "مؤرشف"}</Badge><Badge variant="outline">{debtTypeLabel[debt.debtType]}</Badge></div><p className="mt-2 text-sm text-slate-600">المستحق: <span className="font-semibold">{money(debt.outstanding, debt.currency)}</span>{debt.baseOutstanding ? ` · ${money(debt.baseOutstanding, "EGP")}` : " · يحتاج FX للتقييم"}</p><p className="mt-1 text-xs text-slate-500">القسط الأدنى: {money(debt.minimumPayment, debt.currency)} · الفائدة السنوية: {debt.annualInterestRate}%{debt.paymentDay ? ` · استحقاق يوم ${debt.paymentDay}` : ""}</p></div><div className="flex shrink-0 gap-2">{debt.status === "active" && access.canEdit && <Button type="button" size="sm" variant="outline" onClick={() => { setPaymentDebtId(String(debt.id)); setPaymentPrincipal(""); setPaymentInterest("0"); setPaymentFee("0"); }}>سداد</Button>}<Button type="button" size="sm" variant="ghost" onClick={() => setProjectionDebtId(String(debt.id))}>الجدول</Button></div></div></div>)}</div> : <EmptyState icon={CreditCard} title="لا توجد التزامات مسجلة" description="أضف قرضًا أو بطاقة ائتمان. لن يظهر أي رصيد دين في تقارير FAMILY قبل تسجيله بقيد افتتاحي أو صرف فعلي." />}</CardContent></Card></section>{paymentDebtId && <Card><CardHeader><CardTitle>ترحيل سداد الدين</CardTitle><CardDescription>أدخل فقط مبلغ الأصل المسدد والفائدة والرسوم الفعلية. يمنع النظام السداد فوق رصيد الأصل القائم أو السحب فوق رصيد الحساب.</CardDescription></CardHeader><CardContent><form onSubmit={submitPayment} className="grid gap-4 md:grid-cols-4"><Select value={paymentDebtId} onValueChange={setPaymentDebtId} disabled={!access.canEdit}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{debts.data?.filter(debt => debt.status === "active").map(debt => <SelectItem key={debt.id} value={String(debt.id)}>{debt.name}</SelectItem>)}</SelectContent></Select><Select value={paymentAccountId || undefined} onValueChange={setPaymentAccountId} disabled={!access.canEdit}><SelectTrigger><SelectValue placeholder="حساب السداد" /></SelectTrigger><SelectContent>{cashAccounts.filter(account => account.currency === selectedDebt?.currency).map(account => <SelectItem value={String(account.id)} key={account.id}>{account.name}</SelectItem>)}</SelectContent></Select><Input value={paymentPrincipal} onChange={event => setPaymentPrincipal(event.target.value)} disabled={!access.canEdit} inputMode="decimal" placeholder="الأصل" required /><Input value={paymentInterest} onChange={event => setPaymentInterest(event.target.value)} disabled={!access.canEdit} inputMode="decimal" placeholder="الفائدة" /><Input value={paymentFee} onChange={event => setPaymentFee(event.target.value)} disabled={!access.canEdit} inputMode="decimal" placeholder="الرسوم" /><Textarea value={paymentMemo} onChange={event => setPaymentMemo(event.target.value)} disabled={!access.canEdit} placeholder="مذكرة اختيارية" /><Button type="submit" disabled={postPayment.isPending || !access.canEdit}>{postPayment.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}نشر السداد</Button></form></CardContent></Card>}{projectionDebtId && <Card><CardHeader><CardTitle>جدول تكلفة الدين</CardTitle><CardDescription>حساب تعاقدي حتى 120 شهرًا وفق الرصيد والفائدة والقسط الحاليين. السداد الإضافي أدناه يقارن تكلفة الدين فقط.</CardDescription></CardHeader><CardContent><div className="mb-4 flex max-w-sm gap-2"><Input value={extraPayment} onChange={event => setExtraPayment(event.target.value)} inputMode="decimal" placeholder="سداد إضافي شهري اختياري" /><Button type="button" variant="outline" onClick={() => void projection.refetch()}>حساب</Button></div>{projection.isLoading ? <Skeleton className="h-32" /> : projection.error ? <InlineError message={textError(projection.error)} /> : projection.data ? <div className="grid gap-3 sm:grid-cols-4"><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs text-slate-500">إجمالي الفائدة المعروضة</p><p className="mt-1 font-semibold">{money(projection.data.schedule.totalInterest, projection.data.currency)}</p></div><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs text-slate-500">الرصيد بعد الأفق</p><p className="mt-1 font-semibold">{money(projection.data.schedule.endingBalance, projection.data.currency)}</p></div><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs text-slate-500">إهلاك سلبي</p><p className="mt-1 font-semibold">{projection.data.schedule.negativeAmortization ? "القسط لا يغطي الفائدة" : "لا"}</p></div>{projection.data.comparison && <div className="rounded-xl bg-emerald-50 p-4"><p className="text-xs text-emerald-700">أثر السداد الإضافي</p><p className="mt-1 font-semibold text-emerald-800">يوفر {money(projection.data.comparison.interestSaved, projection.data.currency)} و{projection.data.comparison.monthsSaved} شهرًا</p></div>}</div> : null}</CardContent></Card>}</div></DashboardLayout>;
}

export function EmergencyFundPage() {
  const utils = trpc.useUtils(); const access = useFamilyPermissions(); const summary = trpc.family.emergencyFund.summary.useQuery(); const categories = trpc.family.cashFlow.categories.useQuery(); const [targetMonths, setTargetMonths] = useState("3"); const [lookbackMonths, setLookbackMonths] = useState("3"); const [targetDate, setTargetDate] = useState("");
  const savePlan = trpc.family.emergencyFund.upsertPlan.useMutation({ onSuccess: () => { toast.success("تم حفظ خطة الاحتياطي. سيعاد احتساب الغطاء من البيانات الفعلية."); void utils.family.emergencyFund.summary.invalidate(); }, onError: error => toast.error(textError(error)) });
  const setEssential = trpc.family.cashFlow.setEssential.useMutation({ onSuccess: () => { void utils.family.cashFlow.categories.invalidate(); void utils.family.emergencyFund.summary.invalidate(); }, onError: error => toast.error(textError(error)) });
  const submit = (event: React.FormEvent) => { event.preventDefault(); savePlan.mutate({ targetMonths, lookbackMonths: Number(lookbackMonths), targetDate: targetDate ? Date.parse(`${targetDate}T00:00:00.000Z`) : null }); };
  const plan = summary.data?.plan;
  useEffect(() => {
    if (!plan) return;
    setTargetMonths(plan.targetMonths);
    setLookbackMonths(String(plan.lookbackMonths));
    setTargetDate(plan.targetDate ? new Date(plan.targetDate).toISOString().slice(0, 10) : "");
  }, [plan?.targetMonths, plan?.lookbackMonths, plan?.targetDate]);
  return <DashboardLayout><div dir="rtl" className="mx-auto max-w-7xl space-y-6"><header className="border-b pb-6"><p className="text-xs font-semibold uppercase tracking-[.15em] text-emerald-700">FAMILY / Resilience</p><h1 className="mt-2 text-3xl font-bold">صندوق الطوارئ</h1><p className="mt-2 max-w-3xl text-sm text-slate-500">تُقاس التغطية بالسيولة المقيمة مقابل متوسط المصروفات الأساسية المسجلة خلال فترة النظر، مضافًا إليه الحد الأدنى للديون. لا تفترض المنصة دخلًا أو عائدًا لم يُسجل.</p></header>{summary.isLoading ? <PageLoading /> : summary.error ? <InlineError message={textError(summary.error)} /> : <><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Card><CardHeader className="pb-3"><CardDescription>السيولة المتاحة</CardDescription><CardTitle className="text-2xl text-emerald-700">{money(summary.data?.liquidReserveBase, summary.data?.baseCurrency || "EGP")}</CardTitle></CardHeader><CardContent className="text-xs text-slate-500">حسابات نقدية ومصرفية ومحافظ ووساطة مقيمة فقط</CardContent></Card><Card><CardHeader className="pb-3"><CardDescription>مصروف أساسي شهري</CardDescription><CardTitle className="text-2xl">{money(summary.data?.essentialExpenseMonthlyBase, summary.data?.baseCurrency || "EGP")}</CardTitle></CardHeader><CardContent className="text-xs text-slate-500">متوسط آخر {plan?.lookbackMonths ?? 3} أشهر</CardContent></Card><Card><CardHeader className="pb-3"><CardDescription>التزام الدين الأدنى</CardDescription><CardTitle className="text-2xl">{money(summary.data?.debtMinimumPaymentBase, summary.data?.baseCurrency || "EGP")}</CardTitle></CardHeader><CardContent className="text-xs text-slate-500">من عقود الدين النشطة</CardContent></Card><Card><CardHeader className="pb-3"><CardDescription>مدة التغطية</CardDescription><CardTitle className="text-2xl">{summary.data?.coverageMonths === null ? "غير قابلة للحساب" : `${summary.data?.coverageMonths} شهر`}</CardTitle></CardHeader><CardContent className="text-xs text-slate-500">مقابل الإنفاق الأساسي والأقساط الدنيا</CardContent></Card></section><Card className={summary.data?.recommendation.status === "funded" ? "border-emerald-200 bg-emerald-50" : "border-amber-200 bg-amber-50"}><CardContent className={`flex gap-3 p-5 text-sm ${summary.data?.recommendation.status === "funded" ? "text-emerald-900" : "text-amber-900"}`}><Umbrella className="mt-0.5 size-5 shrink-0" /><div><p className="font-semibold">الإجراء المحسوب</p><p className="mt-1 leading-6">{summary.data?.recommendation.message}</p></div></CardContent></Card><section className="grid gap-6 lg:grid-cols-[.85fr_1.15fr]"><Card><CardHeader><CardTitle>خطة الاحتياطي</CardTitle><CardDescription>حدد الهدف والفترة التي تعكس نمط الإنفاق لديك. المساهمة الشهرية المطلوبة تظهر فقط عند تحديد تاريخ هدف مستقبلي.</CardDescription></CardHeader><CardContent><form onSubmit={submit} className="grid gap-4"><div className="grid gap-2"><Label htmlFor="emergency-target">أشهر التغطية المستهدفة</Label><Input id="emergency-target" value={targetMonths} onChange={event => setTargetMonths(event.target.value)} disabled={!access.canEdit} inputMode="decimal" required /></div><div className="grid gap-2"><Label htmlFor="emergency-lookback">فترة متوسط المصروفات بالأشهر</Label><Input id="emergency-lookback" type="number" min="1" max="24" value={lookbackMonths} onChange={event => setLookbackMonths(event.target.value)} disabled={!access.canEdit} required /></div><div className="grid gap-2"><Label htmlFor="emergency-date">تاريخ الهدف</Label><Input id="emergency-date" type="date" value={targetDate} onChange={event => setTargetDate(event.target.value)} disabled={!access.canEdit} /></div><Button type="submit" disabled={savePlan.isPending || !access.canEdit}>{savePlan.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}{access.canEdit ? "حفظ الخطة" : "تتطلب صلاحية محرر"}</Button></form></CardContent></Card><Card><CardHeader><CardTitle>فجوة التمويل ومصدر الإنفاق</CardTitle><CardDescription>يجب أن يعتمد الاحتياطي على فئات مصروف أساسية صريحة، لا على تصنيف افتراضي.</CardDescription></CardHeader><CardContent className="space-y-5">{summary.data?.plan ? <div className="grid gap-3 sm:grid-cols-3"><div className="rounded-xl border bg-slate-50 p-4"><p className="text-xs text-slate-500">الهدف</p><p className="mt-1 font-semibold">{money(summary.data.targetReserveBase, summary.data.baseCurrency)}</p></div><div className="rounded-xl border bg-slate-50 p-4"><p className="text-xs text-slate-500">الفجوة</p><p className="mt-1 font-semibold">{money(summary.data.fundingGapBase, summary.data.baseCurrency)}</p></div><div className="rounded-xl border bg-slate-50 p-4"><p className="text-xs text-slate-500">مساهمة شهرية</p><p className="mt-1 font-semibold">{summary.data.monthlyContributionNeededBase ? money(summary.data.monthlyContributionNeededBase, summary.data.baseCurrency) : "حدد تاريخ هدف"}</p></div></div> : <div className="rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900">لم تحفظ هدف احتياطي بعد؛ يمكن للمنصة عرض السيولة ومتوسط الالتزامات فقط حتى تحدد عدد أشهر التغطية.</div>}{summary.data?.unvaluedCurrencies.length ? <div className="rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900">عملات سيولة غير مقيمة في عملة الأساس: {summary.data.unvaluedCurrencies.join("، ")}. لن تدخل في قيمة الاحتياطي إلى أن تسجل FX موثقًا.</div> : null}<div className="space-y-2"><p className="text-sm font-semibold text-slate-900">فئات المصروف الأساسية</p>{categories.data?.filter(category => category.direction === "expense").length ? categories.data.filter(category => category.direction === "expense").map(category => <div key={category.id} className="flex items-center justify-between rounded-lg border p-3"><span className="text-sm">{category.name}</span><Button type="button" variant={category.isEssential === "yes" ? "default" : "outline"} size="sm" disabled={!access.isOwner || setEssential.isPending} onClick={() => setEssential.mutate({ categoryId: category.id, isEssential: category.isEssential !== "yes" })}>{category.isEssential === "yes" ? "أساسي" : "غير أساسي"}</Button></div>) : <p className="text-sm text-slate-500">أضف فئات مصروف أولًا من صفحة التدفق والميزانية.</p>} {!access.isOwner && <p className="text-xs text-slate-500">تغيير وسم «أساسي» محفوظ للمالك لأنّه يغيّر مقياس المرونة المالي.</p>}</div></CardContent></Card></section></>}</div></DashboardLayout>;
}

const workspaceRoleLabel: Record<string, string> = { owner: "مالك", advisor: "مستشار", editor: "محرر", viewer: "مشاهد" };
const invitationStatusLabel: Record<string, string> = { pending: "معلق", accepted: "مقبولة", cancelled: "ملغاة", expired: "منتهية" };
const auditActionLabel: Record<string, string> = {
  workspace_invitation_created: "إنشاء دعوة مساحة عمل",
  workspace_invitation_cancelled: "إلغاء دعوة مساحة عمل",
  account_created: "إنشاء حساب",
  cash_event_posted: "نشر حركة نقدية",
  transfer_posted: "نشر تحويل",
  trade_posted: "نشر صفقة تداول",
  price_quote_recorded: "تسجيل سعر سوق",
  fx_rate_recorded: "تسجيل سعر صرف",
  approval_requested: "إنشاء طلب اعتماد",
  approval_decided: "تسجيل قرار اعتماد",
  period_closed: "إغلاق فترة",
};
const auditTargetLabel: Record<string, string> = { workspace: "مساحة العمل", account: "حساب", financial_event: "عملية مالية", price_quote: "سعر سوق", fx_rate: "سعر صرف", approval_request: "طلب اعتماد", invitation: "دعوة", special_asset: "أصل خاص", insurance_policy: "بوليصة تأمين" };
const humanizeAuditAction = (value: string) => auditActionLabel[value] || value.replaceAll("_", " ");
const humanizeAuditTarget = (value: string) => auditTargetLabel[value] || value.replaceAll("_", " ");

export function MembersPage() {
  const utils = trpc.useUtils();
  const bootstrap = trpc.family.bootstrap.useQuery();
  const members = trpc.family.members.list.useQuery();
  const workspaces = trpc.family.workspaces.list.useQuery();
  const [email, setEmail] = useState("");
  const [role, setRole] = useState<"advisor" | "editor" | "viewer">("viewer");
  const invite = trpc.family.members.invite.useMutation({ onSuccess: data => { toast.success(`تم تسجيل دعوة ${data.email}. تُقبل تلقائيًا عند دخول صاحب البريد.`); void utils.family.members.list.invalidate(); setEmail(""); }, onError: error => toast.error(textError(error)) });
  const cancelInvitation = trpc.family.members.cancelInvitation.useMutation({ onSuccess: () => { toast.success("تم إلغاء الدعوة وتسجيل الإجراء في التدقيق."); void utils.family.members.list.invalidate(); }, onError: error => toast.error(textError(error)) });
  const setActive = trpc.family.workspaces.setActive.useMutation({ onSuccess: () => { toast.success("تم تغيير مساحة FAMILY النشطة."); void utils.invalidate(); }, onError: error => toast.error(textError(error)) });
  const submit = (event: React.FormEvent) => { event.preventDefault(); invite.mutate({ email, role }); };
  return <DashboardLayout><div dir="rtl" className="mx-auto max-w-6xl space-y-6"><header className="border-b pb-6"><p className="text-xs font-semibold uppercase tracking-[.15em] text-emerald-700">FAMILY / Access governance</p><h1 className="mt-2 text-3xl font-bold">الأعضاء ومساحات FAMILY</h1><p className="mt-2 text-sm text-slate-500">العضوية والصلاحية تُفرضان من الخادم. يُقبل البريد المدعو عند تسجيل صاحبه الدخول، ولا تمنح الدعوة الوصول قبل ذلك.</p></header><div className="grid gap-6 lg:grid-cols-[.8fr_1.2fr]"><Card><CardHeader><CardTitle>مساحة العمل النشطة</CardTitle><CardDescription>يمكنك التنقل فقط بين المساحات التي تملك عضوية فعالة فيها.</CardDescription></CardHeader><CardContent>{workspaces.isLoading ? <Skeleton className="h-24" /> : <div className="grid gap-3">{workspaces.data?.map(space => <button key={space.id} type="button" onClick={() => setActive.mutate({ workspaceId: space.id })} disabled={setActive.isPending || bootstrap.data?.workspace.id === space.id} className={`rounded-xl border p-4 text-right transition-colors ${bootstrap.data?.workspace.id === space.id ? "border-emerald-600 bg-emerald-50" : "hover:border-emerald-300 hover:bg-slate-50"}`}><div className="flex items-center justify-between gap-3"><span className="font-semibold">{space.name}</span><Badge variant="outline">{workspaceRoleLabel[space.role]}</Badge></div><p className="mt-2 text-xs text-slate-500">عملة الأساس: {space.baseCurrency}{bootstrap.data?.workspace.id === space.id ? " · نشطة الآن" : ""}</p></button>)}</div>}</CardContent></Card><Card><CardHeader><CardTitle className="flex items-center gap-2"><UsersRound className="size-5 text-emerald-700" />أعضاء النطاق</CardTitle><CardDescription>المالك فقط يستطيع إنشاء دعوات. تتطلب الدعوة تسجيل الدخول بالبريد نفسه خلال سبعة أيام.</CardDescription></CardHeader><CardContent>{members.isLoading || bootstrap.isLoading ? <Skeleton className="h-72" /> : members.error ? <InlineError message={textError(members.error)} /> : <div className="space-y-6">{bootstrap.data?.membership.role === "owner" && <form onSubmit={submit} className="grid gap-3 rounded-xl border bg-slate-50 p-4 sm:grid-cols-[1fr_150px_auto]"><Input type="email" value={email} onChange={event => setEmail(event.target.value)} placeholder="email@example.com" required /><Select value={role} onValueChange={value => setRole(value as typeof role)}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="advisor">مستشار</SelectItem><SelectItem value="editor">محرر</SelectItem><SelectItem value="viewer">مشاهد</SelectItem></SelectContent></Select><Button type="submit" disabled={invite.isPending}>{invite.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}دعوة عضو</Button></form>}<div className="divide-y rounded-xl border">{members.data?.activeMembers.map(member => <div key={member.id} className="flex items-center justify-between gap-4 p-4"><div><p className="font-semibold">{member.name || "عضو FAMILY"}</p><p className="mt-1 text-xs text-slate-500">{member.email || "بريد غير متاح"}</p></div><Badge variant="secondary">{workspaceRoleLabel[member.role]}</Badge></div>)}</div>{members.data?.invitations.length ? <div><p className="mb-3 text-sm font-semibold text-slate-800">دعوات معلقة أو سابقة</p><div className="space-y-2">{members.data.invitations.map(invitation => <div className="flex items-center justify-between gap-3 rounded-lg border border-dashed p-3 text-sm" key={invitation.id}><span>{invitation.email}</span><div className="flex items-center gap-2"><Badge variant="outline">{workspaceRoleLabel[invitation.role]} · {invitation.status}</Badge>{bootstrap.data?.membership.role === "owner" && invitation.status === "pending" && <Button type="button" size="sm" variant="ghost" className="text-red-700 hover:text-red-800" disabled={cancelInvitation.isPending} onClick={() => cancelInvitation.mutate({ invitationId: invitation.id })}>إلغاء</Button>}</div></div>)}</div></div> : null}</div>}</CardContent></Card></div></div></DashboardLayout>;
}

export function ReportsPage() {
  const dashboard = trpc.family.dashboard.useQuery();
  if (dashboard.isLoading) return <DashboardLayout><PageLoading /></DashboardLayout>;
  if (dashboard.error) return <DashboardLayout><InlineError message={textError(dashboard.error)} /></DashboardLayout>;
  const data = dashboard.data;
  const gaps = [
    ...(data?.unvaluedCurrencies?.length ? [`أرصدة بعملات غير مقيمة: ${data.unvaluedCurrencies.join("، ")}`] : []),
    ...(data?.unvaluedInstruments?.length ? [`حيازات بلا سعر سوق موثق: ${data.unvaluedInstruments.join("، ")}`] : []),
  ];
  return <DashboardLayout><div dir="rtl" className="mx-auto max-w-5xl space-y-6"><header className="flex flex-col gap-4 border-b pb-6 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.15em] text-emerald-700">FAMILY / Financial Report</p><h1 className="mt-2 text-3xl font-bold">التقرير المالي الحالي</h1><p className="mt-2 text-sm text-slate-500">لقطة شفافة من المصدر المالي المركزي عند فتح هذه الصفحة، وليست كشفًا ثابتًا أو تقديرًا محاكى.</p></div><Button variant="outline" onClick={() => window.print()} className="gap-2"><FileChartColumn className="size-4" />طباعة / حفظ PDF</Button></header><section className="grid gap-4 sm:grid-cols-3"><Card><CardHeader className="pb-3"><CardDescription>صافي الثروة المقيم</CardDescription><CardTitle className="text-2xl">{money(data?.netWorthBase, data?.workspace.baseCurrency || "EGP")}</CardTitle></CardHeader><CardContent className="text-xs text-slate-500">سيولة وحيازات بسعر متاح فقط</CardContent></Card><Card><CardHeader className="pb-3"><CardDescription>السيولة المقيمة</CardDescription><CardTitle className="text-2xl">{money(data?.liquidBalanceBase, data?.workspace.baseCurrency || "EGP")}</CardTitle></CardHeader><CardContent className="text-xs text-slate-500">ناتج دفتر الحسابات</CardContent></Card><Card><CardHeader className="pb-3"><CardDescription>ربح/خسارة غير محققة</CardDescription><CardTitle className={`text-2xl ${Number(data?.unrealizedPnlBase || 0) >= 0 ? "text-emerald-700" : "text-red-700"}`}>{money(data?.unrealizedPnlBase, data?.workspace.baseCurrency || "EGP")}</CardTitle></CardHeader><CardContent className="text-xs text-slate-500">للحيازات ذات سعر سوق موثق</CardContent></Card></section><Card><CardHeader><CardTitle>جودة البيانات والاهتمام المطلوب</CardTitle><CardDescription>هذه إشارات سياقية محسوبة من حالة البيانات الحالية، وليست تنبيهات وهمية أو وعودًا بالتشغيل الخلفي.</CardDescription></CardHeader><CardContent>{gaps.length ? <div className="space-y-3">{gaps.map(gap => <div className="flex gap-3 rounded-lg border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900" key={gap}><CircleAlert className="mt-0.5 size-5 shrink-0" />{gap}</div>)}</div> : <div className="rounded-lg border border-emerald-200 bg-emerald-50 p-4 text-sm text-emerald-900">تتوفر أسعار الصرف وأسعار السوق اللازمة لتقييم البيانات المسجلة حاليًا. راجع مصدر الأسعار وتاريخها قبل اتخاذ قرار مالي.</div>}</CardContent></Card></div></DashboardLayout>;
}

export function AuditPage() {
  const audit = trpc.family.audit.recent.useQuery();
  return <DashboardLayout><div dir="rtl" className="mx-auto max-w-6xl space-y-6"><header className="border-b pb-6"><p className="text-xs font-semibold uppercase tracking-[.15em] text-emerald-700">FAMILY / Audit trail</p><h1 className="mt-2 text-3xl font-bold">سجل التدقيق</h1><p className="mt-2 text-sm text-slate-500">أثر تغييرات قابل للمراجعة ضمن نطاقك؛ لا تعرض الواجهة سجلات نطاق آخر.</p></header>{audit.isLoading ? <PageLoading /> : audit.error ? <InlineError message={textError(audit.error)} /> : audit.data?.length ? <Card><CardContent className="p-0"><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-right text-sm"><thead className="bg-slate-50 text-xs text-slate-600"><tr><th className="p-4 font-semibold">التاريخ والوقت</th><th className="p-4 font-semibold">نوع العملية</th><th className="p-4 font-semibold">المستخدم</th><th className="p-4 font-semibold">التفاصيل</th></tr></thead><tbody className="divide-y">{audit.data.map(record => <tr key={record.id} className="align-top hover:bg-emerald-50/40"><td className="p-4 whitespace-nowrap text-slate-600">{dateTime(record.occurredAt)}</td><td className="p-4 font-semibold text-slate-900">{humanizeAuditAction(record.action)}</td><td className="p-4"><Badge variant="outline">المستخدم #{record.actorUserId}</Badge></td><td className="p-4 text-slate-600">{humanizeAuditTarget(record.targetType)} <span className="font-mono text-xs">#{record.targetId}</span></td></tr>)}</tbody></table></div></CardContent></Card> : <EmptyState icon={ShieldCheck} title="لا توجد أحداث تدقيق حتى الآن" description="سيظهر هنا إنشاء الحسابات والأسعار ونشر العمليات من مصدرها الفعلي." />}</div></DashboardLayout>;
}
