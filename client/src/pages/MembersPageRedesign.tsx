import DashboardLayout from "@/components/DashboardLayout";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Loader2, UsersRound } from "lucide-react";
import { useState } from "react";
import { toast } from "sonner";
import { trpc } from "@/lib/trpc";
import { useAuth } from "@/_core/hooks/useAuth";
import { Link } from "wouter";

const roleLabel: Record<string, string> = { owner: "مالك", advisor: "مستشار", editor: "محرر", viewer: "مشاهد" };
const invitationLabel: Record<string, string> = { pending: "معلق", accepted: "مقبولة", cancelled: "ملغاة", expired: "منتهية" };
const errorText = (error: unknown) => error instanceof Error ? error.message : "تعذر إكمال العملية الآن.";

export default function MembersPageRedesign() {
  const { user } = useAuth();
  const utils = trpc.useUtils();
  const bootstrap = trpc.family.bootstrap.useQuery();
  const members = trpc.family.members.list.useQuery();
  const workspaces = trpc.family.workspaces.list.useQuery();
  const [email, setEmail] = useState("");
  const [role, setRole] = useState<"advisor" | "editor" | "viewer">("viewer");
  const invite = trpc.family.members.invite.useMutation({ onSuccess: data => { toast.success(`تم تسجيل دعوة ${data.email}. تُقبل عند دخول صاحب البريد.`); void utils.family.members.list.invalidate(); setEmail(""); }, onError: error => toast.error(errorText(error)) });
  const cancelInvitation = trpc.family.members.cancelInvitation.useMutation({ onSuccess: () => { toast.success("تم إلغاء الدعوة وتسجيل الإجراء في التدقيق."); void utils.family.members.list.invalidate(); }, onError: error => toast.error(errorText(error)) });
  const setActive = trpc.family.workspaces.setActive.useMutation({ onSuccess: () => { toast.success("تم تغيير مساحة FAMILY النشطة."); void utils.invalidate(); }, onError: error => toast.error(errorText(error)) });
  const isOwner = bootstrap.data?.membership.role === "owner";
  return <DashboardLayout><main className="mx-auto max-w-6xl space-y-6" dir="rtl">
    <header className="border-b pb-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[.15em] text-emerald-700">FAMILY / Access governance</p><h1 className="mt-2 text-3xl font-bold">الأعضاء ومساحات FAMILY</h1><p className="mt-2 text-sm text-slate-500">العضوية والصلاحية تُفرضان من الخادم. يُقبل البريد المدعو عند تسجيل صاحبه الدخول، ولا تمنح الدعوة الوصول قبل ذلك.</p></div>{user?.role === "admin" && <Link href="/admin/users" className="inline-flex h-10 items-center rounded-lg border border-primary/25 bg-primary/5 px-4 text-sm font-semibold text-primary transition-colors hover:bg-primary/10">إدارة مستخدمي المنصة</Link>}</div></header>
    <section className="grid gap-6 lg:grid-cols-[.8fr_1.2fr]"><Card className="fintech-surface-card"><CardHeader><CardTitle>مساحة العمل النشطة</CardTitle><CardDescription>يمكنك التنقل فقط بين المساحات التي تملك عضوية فعالة فيها.</CardDescription></CardHeader><CardContent>{workspaces.isLoading ? <p className="py-8 text-center text-sm text-muted-foreground">جارٍ تحميل المساحات…</p> : <div className="space-y-3">{workspaces.data?.map(space => <button key={space.id} type="button" onClick={() => setActive.mutate({ workspaceId: space.id })} disabled={setActive.isPending || bootstrap.data?.workspace.id === space.id} className={`members-workspace-row ${bootstrap.data?.workspace.id === space.id ? "is-active" : ""}`}><span><strong>{space.name}</strong><small>عملة الأساس: {space.baseCurrency}{bootstrap.data?.workspace.id === space.id ? " · نشطة الآن" : ""}</small></span><Badge variant="outline">{roleLabel[space.role] || space.role}</Badge></button>)}</div>}</CardContent></Card>
      <Card className="fintech-surface-card"><CardHeader><CardTitle className="flex items-center gap-2"><UsersRound className="size-5 text-primary" />أعضاء النطاق</CardTitle><CardDescription>المالك فقط يستطيع إنشاء دعوات. تتطلب الدعوة تسجيل الدخول بالبريد نفسه خلال سبعة أيام.</CardDescription></CardHeader><CardContent className="space-y-6">{members.isLoading || bootstrap.isLoading ? <p className="py-8 text-center text-sm text-muted-foreground">جارٍ تحميل الأعضاء…</p> : members.error ? <p className="rounded-xl border border-destructive/30 bg-destructive/10 p-4 text-sm text-destructive">{errorText(members.error)}</p> : <><>{isOwner && <form onSubmit={event => { event.preventDefault(); invite.mutate({ email, role }); }} className="grid gap-3 rounded-xl border bg-muted/35 p-4 sm:grid-cols-[1fr_150px_auto]"><div className="grid gap-2"><Label htmlFor="member-email">البريد الإلكتروني</Label><Input id="member-email" type="email" value={email} onChange={event => setEmail(event.target.value)} placeholder="email@example.com" required /></div><div className="grid gap-2"><Label>الدور</Label><Select value={role} onValueChange={value => setRole(value as typeof role)}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="advisor">مستشار</SelectItem><SelectItem value="editor">محرر</SelectItem><SelectItem value="viewer">مشاهد</SelectItem></SelectContent></Select></div><Button className="self-end" type="submit" disabled={invite.isPending}>{invite.isPending && <Loader2 className="ml-2 size-4 animate-spin" />}دعوة عضو</Button></form>}</><div className="overflow-x-auto"><table className="w-full min-w-[520px] text-right text-sm"><thead className="bg-slate-50 text-xs text-slate-600"><tr><th className="p-3">العضو</th><th className="p-3">البريد</th><th className="p-3">الدور</th></tr></thead><tbody className="divide-y">{members.data?.activeMembers.map(member => <tr key={member.id}><td className="p-3 font-semibold">{member.name || "عضو FAMILY"}</td><td className="p-3 text-slate-600" dir="ltr">{member.email || "بريد غير متاح"}</td><td className="p-3"><Badge variant="secondary">{roleLabel[member.role] || member.role}</Badge></td></tr>)}</tbody></table></div>{members.data?.invitations.length ? <div><h2 className="mb-3 text-sm font-bold">الدعوات</h2><div className="overflow-x-auto"><table className="w-full min-w-[520px] text-right text-sm"><thead className="bg-slate-50 text-xs text-slate-600"><tr><th className="p-3">البريد</th><th className="p-3">الدور</th><th className="p-3">الحالة</th><th className="p-3">إجراء</th></tr></thead><tbody className="divide-y">{members.data.invitations.map(invitation => <tr key={invitation.id}><td className="p-3" dir="ltr">{invitation.email}</td><td className="p-3">{roleLabel[invitation.role] || invitation.role}</td><td className="p-3"><Badge variant={invitation.status === "pending" ? "outline" : "secondary"}>{invitationLabel[invitation.status] || invitation.status}</Badge></td><td className="p-3">{isOwner && invitation.status === "pending" ? <Button type="button" size="sm" variant="ghost" className="text-red-700 hover:text-red-800" disabled={cancelInvitation.isPending} onClick={() => cancelInvitation.mutate({ invitationId: invitation.id })}>إلغاء</Button> : "—"}</td></tr>)}</tbody></table></div></div> : null}</>}</CardContent></Card></section>
  </main></DashboardLayout>;
}
