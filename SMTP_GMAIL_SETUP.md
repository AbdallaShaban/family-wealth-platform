# إعداد Gmail SMTP لمنصة FAMILY

تستخدم المنصة **Nodemailer** على الخادم فقط. لا تضع بيانات Gmail أو كلمة مرور التطبيق في ملفات المصدر أو في ملف `.env` مُضاف إلى المستودع. أدخلها من لوحة **Secrets** الخاصة بالمشروع عند الجاهزية، حتى تُحقن في بيئة التطوير والإنتاج بشكل منفصل عن الشفرة.

| المتغير | القيمة المقترحة لحساب Gmail | الغرض |
| --- | --- | --- |
| `SMTP_USER` | عنوان Gmail الكامل | حساب الإرسال وعنوان From |
| `SMTP_PASS` | كلمة مرور تطبيق Google ذات 16 حرفًا | مصادقة SMTP؛ لا تستخدم كلمة مرور Gmail العادية |
| `SMTP_HOST` | `smtp.gmail.com` | خادم Gmail SMTP |
| `SMTP_PORT` | `587` | SMTP مع STARTTLS |
| `SMTP_SECURE` | `false` عند المنفذ 587، أو `true` عند 465 | اختياري؛ تضبطه المنصة تلقائيًا من المنفذ إذا غاب |
| `SMTP_SEND_ENABLED` | `no` أو غير مهيأ مؤقتًا، ثم `yes` بعد الموافقة والاختبار | مفتاح تشغيل صريح؛ يمنع الاتصال والإرسال عندما لا يكون `yes` |

تتطلب كلمات مرور التطبيقات تفعيل **التحقق بخطوتين** في حساب Google. أنشئ كلمة مرور تطبيق مخصصة ثم أدخلها مرة واحدة في إعدادات الأسرار؛ لا ترسلها في المحادثة ولا تحفظها في الشفرة. يظل الإرسال متوقفًا افتراضيًا حتى مع وجود بقية الإعدادات؛ بعد موافقة المالك، اضبط `SMTP_SEND_ENABLED=yes`، ثم شغّل فحص اتصال SMTP المحمي وأرسل دعوة اختبار إلى بريد يحدده ويوافق عليه صراحةً. اختبار الاتصال لا يرسل رسالة.

الرسائل السوقية اختيارية ولا تتضمن أسعارًا أو أرصدة أو توصيات شخصية. لا تُرسل إلا بعد موافقة مالك المساحة وتفعيل SMTP، وتستخدم بصمة idempotency تمنع التكرار عند إعادة محاولة Heartbeat. تُرسل كإشعار بوجود مراجعة مطلوبة داخل المنصة، ولا تنفذ أي صفقة أو قيد أو إعادة تقييم. عند غياب إعداد SMTP أو بقاء المفتاح على `no` لا تُنشأ محاولة تسليم.

## مراجع

[1] [Google Help — Sign in with app passwords](https://support.google.com/mail/answer/185833?hl=en)

[2] [Google Workspace — SMTP settings for apps](https://knowledge.workspace.google.com/admin/gmail/send-email-from-a-printer-scanner-or-app)

[3] [Nodemailer — Using Gmail](https://nodemailer.com/guides/using-gmail)
